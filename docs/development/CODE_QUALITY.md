# Code Quality Notes

- 2026-09-23 (resolved by deployment test stabilization): The native-package output test started its 250 ms deadline before fake Docker had emitted diagnostics. EasySDK's five-second command context was created before `repoRoot(t)` suspended it in the parallel scheduler. Delayed-start and delayed-admission process fixtures reproduced both failures; the repaired tests use explicit output observation before injected termination and start the command budget after admission. See the [repeatable validation report](../superpowers/reports/2026-09-23-deployment-test-stability.md).

- 2026-09-09 (resolved by the source-aligned event-sync and RedDot documentation update): The unchanged `afff0872e479a3c540525088f6b7791edd87a770` docs-site baseline fails two `product-http-complete-openapi.test.ts` contracts: 42 runtime Product HTTP registrations versus 41 documented, and the request header schema omits native `red_dot`. Reproduced with `bun test ./lib/product-http-complete-openapi.test.ts` from `docs-site`; the new migration guide does not alter these contracts. Update the OpenAPI source and generated references in a focused follow-up, without relaxing the runtime parity tests.

- 2026-09-08: C#/JS three-node migration, recovery-frontier and post-rejoin delivery findings are handed off in [server issue #927](https://github.com/WuKongIM/WuKongIM/issues/927), with exact released/experimental source identities and sanitized CI evidence. The SDK task does not change the server; full cluster acceptance remains blocked.


- 2026-09-07: The `docs-integration` JavaScript/Web gate is stale: `test/e2e/message/javascript_web_quickstart/javascript_web_quickstart_test.go` invokes `npm run test:e2e`, but the current sample has no such script or Playwright dependency, and `docs-site/lib/javascript-quickstart-example.test.ts` explicitly requires their absence. The browser prerequisite also runs an unpinned `npm exec -- playwright install chromium`, while receipt validation expects a fixed browser toolchain. Align the gate with the current sample and its receipt contract in a dedicated follow-up; do not restore the deliberately removed sample harness or report manual browser verification as this named check passing.

- 2026-09-21 (resolved): The four scheduled backup request codecs now carry only repository references and credential revisions. Targets resolve exact encrypted credentials from their own Controller state. Wire capture, rotation/refusal tests, app-wired S3 protocol I/O, focused race tests, and real single-node/three-node backup restore passed. The three-node scenario also proves Controller Leader loss and rejoin; live external OSS/COS qualification remains separate. Evidence: [project hardening report](../superpowers/reports/2026-09-21-project-hardening.md).
- 2026-06-11: The promoted recipient-authority e2e assertion that a group sender has no `/conversation/list` row failed both before and after moving the scenario to canonical `test/e2e`; the old pre-promotion path and the new path observed a sender-owned group conversation row. Confirm intended conversation semantics before changing the assertion.
- `TestSendStressThreeNode` can finish all sends and durable verification, then fail during `t.Cleanup` because `App.Stop()` uses one shared 5s stop context for every lifecycle component; under stress, late cluster RPC retries can consume the budget and surface as `context deadline exceeded`. Consider per-component shutdown budgets or clearer cleanup diagnostics.
- `pkg/channel/runtime.TestSessionLongPollRPCTimeoutUsesRecoveryBackoff` failed once during a broad package run with `long poll timeout should not immediately retry, got 2 sends`, then passed with `-run ... -count=5` and a fresh package run. The test appears timing-sensitive and should use deterministic scheduler hooks instead of wall-clock assumptions.
- 2026-09-21 (obsolete location verified): The historical duplicated return in `internal/app/deliveryrouting.go:isSenderDeliveryRoute` no longer has a current target: neither that file nor the symbol exists in the current `internal` tree. This closes the stale location note, not a claim about all delivery routing behavior.
- 2026-06-01: `go test -race ./internal/app` reported an existing race in `pkg/transport.(*Server).serveConn` between `server.go:149` and `server.go:151` during `TestStaticMultiNodeClusterStartsControllerVoters`. Presence bench snapshot targeted race tests passed; investigate transport connection callback state separately.
- 2026-06-05: `go test ./pkg/... ./internal/... -count=1` failed once in `pkg/cluster.TestCreateNodeOnboardingPlanPersistsPlannedJob` with an empty planned move list; an immediate targeted rerun of that test passed. Treat onboarding plan persistence as timing/order-sensitive until the planner fixture is made deterministic.
- 2026-06-06: `go test ./pkg/... ./internal/... -count=1` failed once in `pkg/cluster.TestControllerHandlerJoinClusterRejectsInvalidTokenWithoutProposal` because controller `LastIndex` advanced after an invalid-token join; an immediate targeted rerun passed. The test appears sensitive to background controller proposals and should isolate or quiesce raft state before asserting no proposal.
- 2026-07-10: A broad explicit-root unit run failed once in `internal/app.TestConversationListAPIPaginatesWithNextCursor` with HTTP 503 `retry required` / `channel: not ready`; the focused test passed with `-count=5`. Treat the cold-channel first-write path as timing-sensitive follow-up work rather than weakening or expanding the CI gate repair.
- 2026-07-19: Broad and package-wide runs failed in `pkg/cluster.TestClusterPluginBindingPluginNoScanRoutesToSlotLeader` with `raft proposal dropped`; an immediate isolated `-count=3` run passed, then a separate `-count=5` run failed twice. The fixture is timing-sensitive and should wait for stable slot leadership before proposing plugin bindings.
- 2026-07-19: Cloud Provision run `29661291538` printed the ephemeral OpenSSH private bootstrap key while registering its GitHub log mask. The key was subsequently revoked, but workflow masking must be installed without expanding secret file contents into the logged command stream.
- 2026-09-21 (resolved): `TestCloseSlotDuringApplyEnqueueHandoffRejectsTaskAndRetiresQueue` retained its Slot only after starting concurrent `CloseSlot`, which may already remove it from the runtime map. The fixture now captures that exact Slot before closing, retaining all enqueue rejection, queue retirement, and reopen assertions. The original failure reproduced in a 100-run baseline; the corrected test passed 100 repetitions with race detection and the full multiraft race suite.
- 2026-07-19: PR CI failed once in `internal/app.TestStaticMultiNodeClusterStartsControllerVoters` because the first post-start send returned `ReasonSystemError`; the same test had passed in the preceding local broad run and then passed an isolated `-count=5` rerun. The fixture should wait for write routing readiness instead of assuming startup completion makes the first send immediately successful.
- 2026-07-19: `go test -race ./internal/bench/workload` found `TestAutoRecvAckIdleReadDoesNotInjectReadDeadline` and `TestAutoRecvAckDrainOnlyYieldsBackgroundReaderWhileSendackWaiterIsPending` restoring the global AutoRecvAck yield hook while their background reader could still access it. The fixtures should wait for reader shutdown before cleanup restores shared test state.
- 2026-07-20: `go test -race ./internal/app` still reports unrelated startup/rollback races between test `recordingAppLogger` writes and pluginhost/wkrpc/gnet goroutines, plus wkrpc/gnet start-versus-stop state access. The focused channelappend owner-push observer race test passes; isolate plugin runtime startup before treating the broad app race run as a gate.
- 2026-07-21: Scheduled Cloud Simulation Monitor run `29835464857` treated released locator artifacts whose provider status returned `run not found` as `insufficient_evidence`, then failed despite no live Simulation Run. The patrol should skip confirmed-absent released locators instead of reporting an unhealthy running simulation.
- 2026-07-22: `scripts.TestWukongIMThreeNodeBenchScriptKeepsRunPprofPerQPSAttempt` failed under host load first with a missing second fake report and then with `run_completed_before_capture`; make the fake run/pprof handoff deterministic instead of relying on wall-clock capture timing.

- 2026-09-08 (resolved by HA78; see `docs/superpowers/reports/2026-09-08-v2-migration-history-pagination.md`): Native ordinary history pagination can report `more=0` before the oldest visible messages when recovery barriers consume the bounded raw scan. HA76 returned 174 visible rows at sequences 3–203 with `limit=200`, `start_message_seq=0`, `pull_mode=0`, and `more=0`; explicit ascending sequence windows still returned sequences 1–2 unchanged. The former `internal/usecase/message/page_reader.go` filtered SyncOnce without refilling. Bounded continuation and actual Demo paging now pass, including all-control pages; this is not data loss and must not be solved by changing storage or exposing barriers. The HA data verifier now scans explicit bounded sequence windows. See `docs/superpowers/reports/2026-09-08-native-prefix-recovery.md`.
- 2026-09-08: Manager browser smoke run `34206591399` on PR #920 passed its browser scenario but failed the final tracked-tree check: rebuilding unchanged Manager source reordered modulepreload imports and bytes in the existing index asset. This is a frontend build reproducibility finding, separate from the group fanout change; do not report this job as passed or change its safety gate to hide the generated diff.
