# 原生双机主机诊断：六窗方案和执行材料已冻结，尚未采购

本文是采购前的准备快照。后续明确授权后的六窗执行、诊断结论及零库存证明见
[执行报告](2026-09-22-rpc-native-host-diagnostic.md)；以下测前状态和固定计划保留原样。

本轮完成了用户同意的原生Linux双机诊断准备。已有探针、采集器和时钟关联工具没有变化；
执行脚本和退出/释放控制器已准备并完成本地检查，**尚未创建云资源、连接新SSH目标或执行原生窗口**。
上次吞吐/P99抖动仍然是证据不足，不能将准备完成当成根因已确认。

## 固定执行方案

| 项目 | 固定值 |
| --- | --- |
| 测量主机 | 两台不同HostID、同CPU型号的原生Linux amd64，至少4 vCPU/8 GiB，内网通信 |
| 采购形态 | 复用现有3服务节点+1负载节点租约，从3台服务节点中选2台运行RPC |
| 报价与预算 | 总上限300元；保守报价超过250元不Acquire；必须获取本轮新报价 |
| 探针源码 | `9c8032bb56011977ba5c5db66efcab9bd1e9c97b` |
| Linux amd64二进制SHA256 | `2fcd670126ffa5193ed8374e8768b982a2fc656eef74f6b5942fa7c60e8f511e` |
| 窗口 | 固定6窗，顺序执行；每窗新客户端进程，预热10秒、测量20秒、尾部2秒 |
| 负载 | 64B、单连接、16调用者、无请求预算、mutation echo、Observer关闭 |
| 运行时 | CPU亲和性0–3、GOMAXPROCS=4、客户端GC400、服务端GC100，无GODEBUG/GOMEMLIMIT覆盖 |
| 采集 | 双端1 Hz；每窗客户端采集器上限45秒，服务端连续采集上限300秒 |
| 进程与控制上限 | 服务端生命周期480秒；整个远程执行器上限900秒 |
| 重试/profile | 不自动重跑窗口，不混入profile；异常窗口全部保留 |
| 清理 | 成功、失败或取消后由外层控制器立即释放精确租约，核对选择器和账户匹配的认证零库存 |

这是一轮带观测开销的诊断。保留吞吐/P99完整分布用于描述异常，**不输出优化通过或重复性通过**。
工作负载失败、计数/身份不符或采样时间覆盖不足会停止本轮并保存现有证据；
仅观察到慢窗口不会挑选性重跑。可选指标缺失以缺口呈现，不擅自开启内核调度统计。

先核对CPU/PSI、TCP重传、网卡/softnet、线程切换和可用的调度等待能否读取。
三个待检验方向依次为主机调度压力、网络路径阻塞、进程内部等待；只有同时间轴证据支持时才做归因。
若本轮仍不足以解释，另行准备独立的有界Go profile窗口。

## 执行材料和本地检查

准备目录为 `/Users/tt/.codex/visualizations/2026/09/22/rpc-host-native-preparation`。
`plan.json`固定探针、辅助脚本及预算边界，脚本启动前重新核对文件摘要。

- `preflight_host.py`：只读检查原生架构、容器标记、CPU亲和性、内存级别、工具和指标能力。
  已在Linux arm64容器运行，正确拒绝作为原生amd64环境，并保留缺失调度统计的报告。
- `run_native_diag.py`：只接受已Acquire的精确活动租约和外层释放控制器标记；
  上传并校验相同二进制，限制探针端口来源，逐窗取回报告和客户端样本，再关联连续服务端样本。
  服务端停止使用pidfd加启动/可执行文件/命名空间身份复核。
- `control_authorized.py`：将验证过的一小时Cloud Shell临时凭据仅导入控制进程；下载副本在云调用前删除。
  负载子进程不继承云凭据。正常、失败或中断均进入精确释放流程；重复停止信号不能打断该清理段。
  新租约使用新的密钥和临时凭据。
- `reuse_bundle.py`：只按已验证的原始源码/摘要复用离线包，不重新标记其源码身份。
  没有采购授权或使用禁用断言的Python优化模式时，控制器在操作前拒绝执行。

已通过15个无效探针报告变体、无子进程dry-run、缺少精确释放控制器标记拒绝、
缺少采购标记拒绝，以及三个入口禁用Python优化模式的检查。
7个使用模拟子进程的控制器场景覆盖成功、创建状态前/后失败、负载失败、释放失败、
选择器不符和账户不符。它们验证控制分支，**不能代替云端执行与实际零库存核验**。
新编写的远程编排尚未在云上验证。

## 离线包与来源

本轮从 `6b85f349f07cacc89a9cf7a873574dde04676366` 尝试本地重建离线包，
应用构建完成后，GitHub Release依赖下载在 `release-assets.githubusercontent.com` 发生TLS连接失败。
因此采用之前已封存的完整包，并独立核验全部108个文件及内容摘要。

- 离线包和租约控制源码保持为 `ff37d6913164da65f852fe9af9047b497a1c9e89`。
- Bundle digest：`sha256:3d2350528d1ecb714b7a3d299b2d383b7a754b2bdee742feabe83c01e3bc18f8`。
- 压缩包SHA256：`0e571166ef364843a58381cec0124c873d0e4f088dba84c9dd1dea4be04427cb`。
- 该版本与准备版本的租约/部署控制脚本、云依赖锁、技能、AGENTS及Go依赖文件完全相同。
  实际执行时为此精确控制版本创建干净工作树。复用包只满足既有租约材料要求；本轮不部署其中的应用。
- 实际运行的RPC探针独立固定为上表的 `9c8032bb5` 二进制，不能混淆两种源码身份。

## 当前前置条件

本地只读preflight没有联系云厂商；工具齐备，但缺少新的短期凭据及精确生命周期授权标记。
五个历史请求均为released，已逐一复核保存的零库存选择器、账户摘要和观察时间。
历史零库存证明不代表这次已经采购或释放过资源。

当前没有新报价、新请求ID、新租约或新SSH配置。本轮采购须重新使用Chrome中的已登录账号验证
一小时Cloud Shell身份，获取新报价，并创建新的选择器与密钥；成功或失败后只释放该精确选择器。
现有云资源技能明确禁止由单独的“同意”或历史付费记录推断新购授权，因此付费步骤仍待明确授权。

[结构化准备证据](assets/rpc-native-host-preparation-2026-09-22.json)包含固定计划、完整执行脚本、
本地检查结果和历史释放核验摘要，可在采购前复核。
