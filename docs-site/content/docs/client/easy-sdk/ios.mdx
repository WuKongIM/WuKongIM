---
title: iOS 快速接入
description: iOS 原生应用的快速接入入口。
---

如果你的客户端是 iOS，最短路径就是先完成安装、初始化、监听事件、建立连接这四步。

## 第一步：安装 SDK

优先推荐两种方式：

- CocoaPods
- Swift Package Manager

最小 Podfile 示例：

```ruby
pod 'WuKongEasySDK', '~> 1.0.0'
```

## 第二步：初始化

```swift
import WuKongEasySDK

let config = WuKongConfig(
    serverUrl: "ws://your-wukongim-server.com:5200",
    uid: "your_user_id",
    token: "your_auth_token"
)

let easySDK = WuKongEasySDK(config: config)
```

## 第三步：监听事件

```swift
easySDK.onConnect { result in
    print("Connected: \(result)")
}

easySDK.onMessage { message in
    print("Received: \(message)")
}

easySDK.onError { error in
    print("Error: \(error.localizedDescription)")
}
```

## 第四步：建立连接

连接建立成功后，再继续发消息或同步会话。

## 最常见的错误

- 使用了错误的 WebSocket 地址
- 服务端没有先写入正确的 `uid` 和 `token`
- 生命周期里没有妥善管理事件监听

## 下一步

先确认服务端已经准备好 [`用户 Token`](/server/user/token)，再继续做会话和消息能力。
