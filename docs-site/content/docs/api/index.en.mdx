---
title: API & Protocols
description: Reference the Product HTTP compatibility target and Reason Codes for the v3 Beta JavaScript/Web golden path.
---

This area publishes executable contracts for application integration developers. Product HTTP still promises only the Beta subset required by the golden sample. Phase 13 additionally aligns Channel Type, Device Flag / Level, and Message Flags with current authoritative Go definitions.

## Goal and completion criteria

A developer should create a development identity, discover Gateway ingress, recover offline messages from a trusted backend, and handle HTTP, protocol outcomes, and shared wire values precisely without promoting other source routes or enums to published platform capabilities.

<Callout type="warn" title="Not the complete v3 API">
  This is a Beta golden-path subset. The repository's older OpenAPI still contains v2 material. Complete OpenAPI, client protocols, webhooks, operations APIs, and other domain references remain planned.
</Callout>

## Compatibility target

Server revision, SDK, Node.js, Chromium, and contract summary appear on [API Compatibility](/en/api/compatibility) and in `/compatibility.json`. URLs remain unversioned, but a snapshot does not promise compatibility with every revision.

## Prerequisites

- Product HTTP is open only to the localhost BFF or a trusted product backend;
- the default Gateway stored-token gap is explicitly accepted as a local-development limitation;
- the single-node cluster passes `/readyz`;
- the caller parses each compatible endpoint response separately.

## Reference entry points

<Cards>
  <Card title="Conventions" description="Base URL, JSON, identifiers, response shapes, and retry boundaries." href="/en/api/conventions" />
  <Card title="Authentication & Security" description="Development BFF, Product HTTP, and Gateway token-validation boundaries." href="/en/api/authentication" />
  <Card title="Product HTTP API" description="The three published token, route, and message-sync entries." href="/en/api/product-http" />
  <Card title="Shared Dictionaries" description="Source-aligned Channel, device, message-flag, and Reason Code values." href="/en/api/dictionaries" />
</Cards>

## Expected result

The golden BFF is the browser-side boundary for all three HTTP entries; realtime messages use the SDK; error classification preserves HTTP separately from CONNACK/SENDACK; shared protocol dictionaries remain automatically aligned with Go authorities.

## Troubleshooting

- **A required endpoint is absent from Product HTTP subset:** it remains unpublished; do not infer support from old OpenAPI or source location.
- **The browser calls the server directly:** replace it with a same-origin BFF call and tighten the network.
- **Fields disagree with the docs:** fail shared-contract validation and repair runtime, sample, and bilingual pages together.

## Security and responsibility boundary

Product HTTP has no general business-authentication middleware and the default Gateway has no stored-token CONNECT validation. Phase 12 does not describe its local development loop as a production security solution.

## Next step

Begin with [Conventions](/en/api/conventions) and [Shared Dictionaries](/en/api/dictionaries), or follow the [JavaScript/Web Quickstart](/en/sdk/javascript/quickstart) to observe how the three HTTP contracts cooperate.
