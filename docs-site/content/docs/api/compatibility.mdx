---
title: API 版本与兼容性
description: 核对黄金路径 Product HTTP 子集、客户端协议与服务端源码快照。
---

## 目标与完成标准

本页精确定义 API 兼容目标：同一构建 tuple 中的三个 Product HTTP 入口。只有匹配的成功 receipt 才能证明黄金样例和真实 smoke test 已覆盖该精确 tuple；它不代表仓库全部 HTTP 路由或正式 v3 稳定性承诺。

## 兼容快照

<CompatibilitySnapshot locale="zh" />

上方组件是当前构建验证状态的唯一权威。无 receipt 时为 `false/missing`；只有成功且精确匹配 tuple 的 receipt 才是 `true/verified`。声明的目标值、格式错误或发生漂移的 receipt 都不能证明构建已通过。

切片级 OpenAPI 合同、`compatibility.json`、页面表格、BFF 类型和 CI 必须来自同一技术事实。现有仓库中的旧 OpenAPI 仍包含 v2 内容，Phase 12 不会把它包装成完整 v3 规范。

## 前置条件

- 已区分旧完整 OpenAPI 与 Phase 12 切片级合同；
- 能读取当前构建 revision 和验证证明；
- Product HTTP 只允许受信后端访问；
- 合同、BFF、文档或运行时任何一处变化都会重新运行黄金场景。

## 发布范围

| 入口 | Phase 12 状态 | 场景证据目标 |
| --- | --- | --- |
| `POST /user/token` | 黄金路径子集 | BFF 合同测试 + 真实身份创建 |
| `GET /route` | 黄金路径子集 | BFF 合同测试 + 真实 WebSocket 发现 |
| `POST /channel/messagesync` | 黄金路径子集 | BFF 合同测试 + Bob 重连离线恢复 |

消息实时发送通过 JavaScript SDK 与 Gateway 完成，不把 `/message/send` 加入这份 HTTP 子集。其他 Product HTTP、运维 API、Webhook、TCP 细节、JSON-RPC Schema 和完整 OpenAPI 仍处于规划状态。

## URL 与版本策略

当前路径保持 `/{lang}/api/...`，不迁移到 `/v3/`。版本身份记录在构建 revision 和兼容清单中：

- “v3 Beta snapshot” 是可复现源码组合，不是正式语义化版本；
- URL 不变不表示任意源码 revision 都兼容；
- 更新服务端、SDK 或合同后，必须重新执行真实黄金场景；
- 未通过该场景的组合只能标为未验证。

## 预期结果

使用同一快照时，BFF 能创建开发身份、发现 WebSocket 地址，并在 Bob 重连时恢复已提交消息。合同变化会触发文档集成校验，而不是静默改变页面字段。

## 失败诊断

- **端点字段与页面不同**：以共享合同校验失败处理，不能人工让 BFF 和文档各自兼容一套。
- **HTTP 子集通过、SDK 发送失败**：继续检查 SDK/协议兼容性；HTTP 成功不能外推 Gateway 行为。
- **源码 revision 不在快照中**：将结果标记为未验证，并运行完整 smoke test。
- **旧 OpenAPI 与本页冲突**：旧文件不属于本阶段权威；不要用它扩大已发布承诺。

## 安全与责任边界

API 兼容不等于 API 可直接公网暴露。默认 Product HTTP 缺少通用业务鉴权，默认 Gateway 缺少 stored-token CONNECT 校验；兼容测试不会替代这些生产控制。

## 下一步

先阅读[通用约定](/zh/api/conventions)，再从[Product HTTP API](/zh/api/product-http)进入三个已发布合同。
