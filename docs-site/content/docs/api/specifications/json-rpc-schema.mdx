---
title: JSON-RPC 实验 Schema
description: 下载 JSON-RPC 数据形状，并理解 EasySDK 核心路径与其余实验边界。
---

<Callout type="info" title="Experimental · EasySDK 核心路径已支持">
  此 Schema 记录 WebSocket JSON-RPC 边界。当前 Product Gateway 支持固定 EasySDK 的 CONNECT、Ping 与在线双向消息；batch、订阅、离线同步和通用 JSON-RPC 仍是实验性或未实现范围。
</Callout>

[下载 JSON Schema 2020-12](/contracts/json-rpc.experimental.schema.json)

Schema 的 `x-wukongim-stability` 为 `experimental-easysdk-core-supported`。当前行为是：

- `connect` 是首个请求，并在鉴权、激活后返回关联 result/error；
- `send`、`ping` 使用请求 ID 关联 SENDACK/PONG；
- `recv` notification 与 `recvack` 完成在线投递确认；
- `subscribe`、`unsubscribe` 及多种 notification 只有 codec 结构或缺少 frame bridge；
- `[` 会选择 JSON 模式，但 batch 数组未实现；请求 ID 必须是字符串，SEND Payload 可为对象、Base64 或 `null`。

Schema 中不属于 EasySDK 核心路径的结构仍不产生运行时承诺。完整矩阵、四端固定版本和证据边界见 [WebSocket JSON-RPC](/zh/api/client-protocols/json-rpc)。
