---
title: 添加节点本地系统 UID 缓存
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 添加节点本地系统 UID 缓存
        id: 添加节点本地系统-uid-缓存
    contents:
      - content: 只修改当前进程缓存，不持久化也不复制该变更。
        heading: 添加节点本地系统-uid-缓存
      - content: |-
          POST /user/systemuids_add_to_cache
          addSystemUIDsToLocalCache
          添加节点本地系统 UID 缓存
          只修改当前进程缓存，不持久化也不复制该变更。
          SystemUIDsRequest
          系统身份变更参数。
          additionalProperties true
          uids `array | null` — 要添加或移除的系统 UID；可省略、为空或为 null，入口不校验元素。
          200 Mutation accepted
          StatusEnvelope
          旧式变更成功响应。
          additionalProperties false
          status `integer` const: `200` 固定为 200 的应用层状态。
          400 Input, validation, dependency, or downstream error
          CompatibilityError
          多个旧式入口共用的 HTTP 400 错误体。
          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。
          status `integer` const: `400` 固定为 400 的应用层状态。
          503 Controller restore maintenance is active
          MaintenanceError
          集群处于恢复维护期间的 503 错误体。
          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。
          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限当前节点的受保护运维边界
- **作用域**：只修改请求处理进程的内存缓存；不持久化，也不广播到其他节点。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/user/systemuids_add_to_cache","method":"post"}]} showDescription />
    </>
  );
}
