---
title: List active user routes
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: List active user routes
        id: list-active-user-routes
    contents:
      - content: Returns one row per active authority route; an empty UID array returns
          the legacy status object instead of an array.
        heading: list-active-user-routes
      - content: >-
          POST /user/onlinestatus

          listUserOnlineStatus

          List active user routes

          Returns one row per active authority route; an empty UID array returns
          the legacy status object instead of an array.

          OnlineStatusRequest

          Array of UID strings; JSON null or an empty array returns the legacy
          status object, and the current entry does not bound its size.

          200 Active routes or the empty-request status object

          OnlineStatusResponse

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          UserOnlineStatus

          uid `string` —

          device_flag `integer:uint8` 0–255

          online `integer` const: `1`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/user/onlinestatus","method":"post"}]} showDescription />
    </>
  );
}
