---
title: Clear a user device token
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Clear a user device token
        id: clear-a-user-device-token
    contents:
      - content: Clears one stored device token and schedules owner-local Session
          closure; device_flag -1 selects APP, Web, and PC.
        heading: clear-a-user-device-token
      - content: >-
          POST /user/device_quit

          quitUserDevice

          Clear a user device token

          Clears one stored device token and schedules owner-local Session
          closure; device_flag -1 selects APP, Web, and PC.

          DeviceQuitRequest

          Device sign-out parameters with legacy weak validation.

          additionalProperties true

          uid `string` default: `""` User ID whose device is signed out; the
          entry itself does not reject an empty value.

          device_flag `integer` default: `0` Device category to sign out; -1
          selects APP, Web, and PC together, while other integers are converted
          to uint8 by the use case.

          200 Mutation accepted

          StatusEnvelope

          Legacy mutation-success response.

          additionalProperties false

          status `integer` const: `200` Application-level status fixed at 200.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Protected operator boundary only
- **Scope**: The handling node performs session closure after about 2 seconds; device_flag=-1 processes APP, Web, and PC sequentially.
- **Atomicity**: Multi-device sign-out is not transactional; earlier device classes may be signed out before a later one fails.
- **Success**: A missing target device still returns 200.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/user/device_quit","method":"post"}]} showDescription />
    </>
  );
}
