---
title: 登记快速开始用户的开发 Token
full: true
_openapi:
  preload:
    - wukongim-product-http-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 登记快速开始用户的开发 Token
        id: 登记快速开始用户的开发-token
    contents:
      - content: 保存开发身份的设备 Token 元数据；它不会启用生产 CONNECT 鉴权。
        heading: 登记快速开始用户的开发-token
      - content: |-
          POST /user/token
          setQuickstartUserToken
          登记快速开始用户的开发 Token
          保存开发身份的设备 Token 元数据；它不会启用生产 CONNECT 鉴权。
          UpdateTokenRequest
          仅从受信后端边界接收的开发身份元数据。
          additionalProperties false
          uid `string` minLength: 1 要创建或更新的稳定业务身份；不能包含 @、# 或 &。
          token `string` minLength: 1 Beta 快速开始中的仅开发 Token；只保留在受信 BFF 一侧。
          device_flag `integer` 0–255 客户端设备类别；JavaScript/Web 快速开始使用 Web 值 1。
          device_level `integer` 0–255 设备冲突级别；快速开始使用从设备值 0。
          {"uid":"alice","token":"alice-dev-token","device_flag":1,"device_level":0}
          200 Token 变更已接受
          StatusEnvelope
          status `integer` const: `200` 兼容成功状态，固定为 200。
          {"status":200}
          400 兼容错误信封
          ErrorEnvelope
          msg `string` — 供人阅读的兼容错误消息。
          status `integer` 400–599 与 HTTP 对齐的错误状态。
          {"msg":"invalid request","status":400}
          503 集群处于恢复维护栅栏后；请退避并观察 /readyz
          MaintenanceError
          error `string` const: `"maintenance"` 机器可读的恢复维护类别。
          message `string` const: `"restore maintenance is active"` 当前维护详情。
          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-beta-zh" operations={[{"path":"/user/token","method":"post"}]} showDescription />
    </>
  );
}
