---
title: Register the quickstart user's development token
full: true
_openapi:
  preload:
    - wukongim-product-http-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Register the quickstart user's development token
        id: register-the-quickstart-users-development-token
    contents:
      - content: Stores device-token metadata for a development identity. It does not
          enable production CONNECT authentication.
        heading: register-the-quickstart-users-development-token
      - content: |-
          POST /user/token
          setQuickstartUserToken
          Register the quickstart user's development token
          Stores device-token metadata for a development identity. It does not enable production CONNECT authentication.
          UpdateTokenRequest
          Development identity metadata accepted only from the trusted backend boundary.
          additionalProperties false
          uid `string` minLength: 1 Stable product identity to create or update. It must not contain @, #, or &.
          token `string` minLength: 1 Development-only token in the Beta quickstart. Keep it on the trusted BFF side.
          device_flag `integer` 0–255 Client device category. The JavaScript/Web quickstart uses 1 for Web.
          device_level `integer` 0–255 Device conflict level. The quickstart uses 0 for a slave device.
          {"uid":"alice","token":"alice-dev-token","device_flag":1,"device_level":0}
          200 Token mutation accepted
          StatusEnvelope
          status `integer` const: `200` Compatibility success status, always 200.
          {"status":200}
          400 Compatibility error envelope
          ErrorEnvelope
          msg `string` — Human-readable compatibility error message.
          status `integer` 400–599 HTTP-compatible error status.
          {"msg":"invalid request","status":400}
          503 The cluster is behind the restore-maintenance fence; back off and observe /readyz
          MaintenanceError
          error `string` const: `"maintenance"` Machine-readable restore-maintenance category.
          message `string` const: `"restore maintenance is active"` Current maintenance detail.
          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-beta-en" operations={[{"path":"/user/token","method":"post"}]} showDescription />
    </>
  );
}
