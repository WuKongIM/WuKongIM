---
title: Add system UIDs
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Add system UIDs
        id: add-system-uids
    contents:
      - content: Persists system identities and adds them to the current process cache.
        heading: add-system-uids
      - content: >-
          POST /user/systemuids_add

          addSystemUIDs

          Add system UIDs

          Persists system identities and adds them to the current process cache.

          SystemUIDsRequest

          System-identity mutation parameters.

          additionalProperties true

          uids `array | null` — System UIDs to add or remove; the field may be
          omitted, empty, or null, and elements are not validated at entry.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/user/systemuids_add","method":"post"}]} showDescription />
    </>
  );
}
