---
title: 查询用户在线路由
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 查询用户在线路由
        id: 查询用户在线路由
    contents:
      - content: 每条活跃权威路由返回一行；空 UID 数组返回旧式 status 对象而不是数组。
        heading: 查询用户在线路由
      - content: |-
          POST /user/onlinestatus
          listUserOnlineStatus
          查询用户在线路由
          每条活跃权威路由返回一行；空 UID 数组返回旧式 status 对象而不是数组。
          OnlineStatusRequest
          UID 字符串数组；JSON null 或空数组返回旧式 status 对象，当前入口不限制数量。
          200 Active routes or the empty-request status object
          OnlineStatusResponse
          StatusEnvelope
          旧式变更成功响应。
          additionalProperties false
          status `integer` const: `200` 固定为 200 的应用层状态。
          UserOnlineStatus
          一个用户的活跃权威设备路由。
          uid `string` — 目标用户的唯一标识。
          device_flag `integer:uint8` 0–255 在线设备类别。
          online `integer` const: `1` 固定为 1，表示该路由在线。
          400 Input, validation, dependency, or downstream error
          CompatibilityError
          多个旧式入口共用的 HTTP 400 错误体。
          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。
          status `integer` const: `400` 固定为 400 的应用层状态。
          503 Controller restore maintenance is active
          MaintenanceError
          集群处于恢复维护期间的 503 错误体。
          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。
          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受保护的运维边界

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/user/onlinestatus","method":"post"}]} showDescription />
    </>
  );
}
