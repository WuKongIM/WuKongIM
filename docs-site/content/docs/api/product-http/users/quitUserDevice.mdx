---
title: 退出用户设备
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 退出用户设备
        id: 退出用户设备
    contents:
      - content: 清空一个已存设备 Token 并调度 owner-local Session 关闭；device_flag=-1 选择 APP、Web 与
          PC。
        heading: 退出用户设备
      - content: >-
          POST /user/device_quit

          quitUserDevice

          退出用户设备

          清空一个已存设备 Token 并调度 owner-local Session 关闭；device_flag=-1 选择 APP、Web 与
          PC。

          DeviceQuitRequest

          设备退出参数；入口保留旧式弱校验。

          additionalProperties true

          uid `string` default: `""` 要退出设备的用户 ID；入口本身不校验空值。

          device_flag `integer` default: `0` 要退出的设备类别；-1 同时选择 APP、Web 与
          PC，其他整数在用例层转为 uint8。

          200 Mutation accepted

          StatusEnvelope

          旧式变更成功响应。

          additionalProperties false

          status `integer` const: `200` 固定为 200 的应用层状态。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受保护的运维边界
- **作用域**：设备关闭由请求处理节点在约 2 秒后执行；device_flag=-1 会依次处理 APP、Web、PC。
- **原子性**：多设备退出不是事务；较早的设备可能已退出，而较后的设备失败。
- **成功判定**：目标设备不存在时仍返回 200。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/user/device_quit","method":"post"}]} showDescription />
    </>
  );
}
