---
title: 退出用户设备
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 退出用户设备
        id: 退出用户设备
    contents:
      - content: 清空一个已存设备 Token 并调度 owner-local Session 关闭；device_flag=-1 选择 APP、Web 与
          PC。
        heading: 退出用户设备
      - content: >-
          POST /user/device_quit

          quitUserDevice

          退出用户设备

          清空一个已存设备 Token 并调度 owner-local Session 关闭；device_flag=-1 选择 APP、Web 与
          PC。

          DeviceQuitRequest

          additionalProperties true

          uid `string` default: `""` The entry adapter does not validate this
          field.

          device_flag `integer` default: `0` -1 selects APP, Web, and PC; other
          values are converted to uint8 by the use case.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/user/device_quit","method":"post"}]} showDescription />
    </>
  );
}
