---
title: 提交消息
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 提交消息
        id: 提交消息
    contents:
      - content: 接受完整兼容 parser，包括旧别名、瞬时标志与请求级订阅者；HTTP 200 时仍需检查 reason。
        heading: 提交消息
      - content: >-
          POST /message/send

          sendChannelMessage

          提交消息

          接受完整兼容 parser，包括旧别名、瞬时标志与请求级订阅者；HTTP 200 时仍需检查 reason。

          SendMessageRequest

          完整的兼容消息发送参数。

          additionalProperties true

          from_uid `string` minLength: 1 消息发送者 UID；为空时才回退读取 sender_uid。

          sender_uid `string` minLength: 1 已废弃的发送者 UID 别名；仅在 from_uid 为空时使用。

          device_id `string` — 调用方提供的发送设备标识；可为空。

          channel_id `string` — 目标 Channel ID；普通发送时必须非空，请求级 subscribers 发送时必须为空。

          channel_type `integer:uint8` 0–255 目标 Channel 类型；普通发送时必须大于 0，请求级
          subscribers 发送时使用 0。

          client_msg_no `string` — 可选的客户端幂等键；空字符串会关闭幂等查找。

          setting `integer:uint8` 0–255 WKProto Setting 位图；应使用 SDK 常量组合，不要手写未知位。

          topic `string` — 可选 Topic；只有相应 Setting 位和客户端能力匹配时才有协议含义。

          expire `integer:uint32` 0–∞ 调用方提供的消息过期秒数；0 表示不设置过期。

          payload `string` minLength: 1; encoding: base64 Base64 编码后的消息
          Payload；解码失败会返回 400。

          subscribers `array | null` — 请求级投递 UID；非空时 channel_id 必须为空且 sync_once
          必须启用，可省略、为空或为 null。

          header `SendMessageHeaderRequest` | `null` — 可选兼容 Header 标志；可为对象、null
          或省略。

          no_persist `integer` default: `0` 顶层兼容标志；非 0 表示不持久化，并与
          header.no_persist 执行逻辑或。

          sync_once `integer` default: `0` 顶层兼容标志；非 0 表示一次性命令消息，并与
          header.sync_once 执行逻辑或。

          SendMessageHeaderRequest

          消息固定 Header 的兼容标志；同名顶层字段会与这些值执行逻辑或。

          no_persist `integer` default: `0` 非 0 表示不持久化该消息。

          sync_once `integer` default: `0` 非 0 表示按一次性命令消息处理。

          200 Transport completed; inspect reason

          SendMessageResponse

          additionalProperties false

          message_id `integer:int64` —

          message_seq `integer:uint64` 0–∞

          reason `integer:uint8` enum: `1`, `2`, `3`, `4`, `5`, `9`, `11`, `13`,
          `15`, `18`, `19`, `24`, `25`

          400 Invalid request or payload

          SendError

          error `string` —

          404 Channel not found

          408 Request cancelled or timed out

          500 Unmapped send failure

          503 Retry-required routing failure or restore maintenance

          RetryRequiredError

          error `string` const: `"retry required"`

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/message/send","method":"post"}]} showDescription />
    </>
  );
}
