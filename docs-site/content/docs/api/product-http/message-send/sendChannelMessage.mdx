---
title: 发送持久 Channel 消息
full: true
_openapi:
  preload:
    - wukongim-product-http-messaging-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 发送持久 Channel 消息
        id: 发送持久-channel-消息
    contents:
      - content: 受信后端提交一条普通持久消息。HTTP 200 仅表示传输成功，业务结果必须检查 reason。
        heading: 发送持久-channel-消息
      - content: |-
          POST /message/send
          sendChannelMessage
          发送持久 Channel 消息
          受信后端提交一条普通持久消息。HTTP 200 仅表示传输成功，业务结果必须检查 reason。
          SendChannelMessageRequest
          受支持的普通持久消息发送结构。
          additionalProperties false
          from_uid `string` minLength: 1; pattern: `\S` 调用方提供的发送者 UID；该路由不会鉴定其身份。
          channel_id `string` minLength: 1; pattern: `\S` 目标 Channel ID；个人 Channel 使用对端 UID。
          channel_type `integer:uint8` 1–255 共享协议字典中的 Channel Type。
          client_msg_no `string` minLength: 1; pattern: `\S` 稳定的客户端幂等键；同一 Payload 有界重试时复用。
          payload `string` minLength: 1; encoding: base64 消息 Payload 字节的 Base64 编码。
          {"from_uid":"system","channel_id":"alice","channel_type":1,"client_msg_no":"order-20260828-0001","payload":"eyJ0eXBlIjoib3JkZXJfdXBkYXRlIn0="}
          200 HTTP 请求已完成；业务结果必须检查 reason。
          SendChannelMessageResponse
          消息标识、Channel 序号与协议 Reason Code。
          message_id `integer:int64` — 服务端消息 ID；reason 成功时有效。
          message_seq `integer:uint64` 0–∞ 消息在该 Channel 内的已提交序号；reason 成功时有效。
          reason `integer:uint8` enum: `1`, `2`, `3`, `4`, `5`, `9`, `11`, `13`, `15`, `18`, `19`, `24`, `25`; 0–255 协议 Reason Code。HTTP 200 不代表业务成功；仅 ReasonSuccess（1）确认接受。
          {"message_id":123456789,"message_seq":42,"reason":1}
          400 请求、Payload 或 Channel 标识无效。
          SendError
          消息发送 HTTP 错误；未列出的文本不是稳定机器合同。
          error `string` — 供人阅读的错误详情。
          {"error":"invalid request"}
          404 未找到目标 Channel。
          {"error":"channel not found"}
          408 请求已取消或超时。
          {"error":"request canceled"}
          {"error":"request timeout"}
          500 内部发送依赖失败；错误文本不稳定。
          {"error":"message usecase not configured"}
          503 刷新路由后重试，或等待恢复维护结束。
          RetryRequiredError
          有界重试前必须刷新 Channel 权威路由。
          error `string` const: `"retry required"` 权威路由陈旧或不可用时的重试类别。
          MaintenanceError
          Controller 恢复维护当前阻止 Product HTTP 写入。
          error `string` const: `"maintenance"` 机器可读的维护类别。
          message `string` const: `"restore maintenance is active"` 当前维护详情。
          {"error":"retry required"}
          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-messaging-beta-zh" operations={[{"path":"/message/send","method":"post"}]} showDescription />
    </>
  );
}
