---
title: 提交消息
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 提交消息
        id: 提交消息
    contents:
      - content: 接受完整兼容 parser，包括旧别名、瞬时标志与请求级订阅者；HTTP 200 时仍需检查 reason。
        heading: 提交消息
      - content: >-
          POST /message/send

          sendChannelMessage

          提交消息

          接受完整兼容 parser，包括旧别名、瞬时标志与请求级订阅者；HTTP 200 时仍需检查 reason。

          SendMessageRequest

          additionalProperties true

          from_uid `string` minLength: 1

          sender_uid `string` minLength: 1 Used only when from_uid is empty.

          device_id `string` —

          channel_id `string` —

          channel_type `integer:uint8` 0–255

          client_msg_no `string` — Optional; an empty value disables idempotency
          lookup.

          setting `integer:uint8` 0–255

          topic `string` —

          expire `integer:uint32` 0–∞

          payload `string` minLength: 1; encoding: base64

          subscribers array<`string`> — A non-empty array selects request-scoped
          delivery, requires an empty channel_id, and requires sync_once.

          header `SendMessageHeaderRequest` —

          no_persist `integer` default: `0` Any non-zero value means true.

          sync_once `integer` default: `0` Any non-zero value means true.

          SendMessageHeaderRequest

          200 Transport completed; inspect reason

          SendMessageResponse

          additionalProperties false

          message_id `integer:int64` —

          message_seq `integer:uint64` 0–∞

          reason `integer:uint8` enum: `1`, `2`, `3`, `4`, `5`, `9`, `11`, `13`,
          `15`, `18`, `19`, `24`, `25`

          400 Invalid request or payload

          SendError

          error `string` —

          404 Channel not found

          408 Request cancelled or timed out

          500 Unmapped send failure

          503 Retry-required routing failure or restore maintenance

          RetryRequiredError

          error `string` const: `"retry required"`

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/message/send","method":"post"}]} showDescription />
    </>
  );
}
