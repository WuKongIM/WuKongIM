---
title: Send a durable Channel message
full: true
_openapi:
  preload:
    - wukongim-product-http-messaging-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Send a durable Channel message
        id: send-a-durable-channel-message
    contents:
      - content: A trusted backend submits one ordinary durable message. HTTP 200 is
          transport success; inspect reason for the business result.
        heading: send-a-durable-channel-message
      - content: |-
          POST /message/send
          sendChannelMessage
          Send a durable Channel message
          A trusted backend submits one ordinary durable message. HTTP 200 is transport success; inspect reason for the business result.
          SendChannelMessageRequest
          The supported ordinary durable-send shape.
          additionalProperties false
          from_uid `string` minLength: 1; pattern: `\S` Caller-supplied sender UID; it is not authenticated by this route.
          channel_id `string` minLength: 1; pattern: `\S` Target Channel ID. For a person Channel, use the peer UID.
          channel_type `integer:uint8` 1–255 Channel Type from the shared protocol dictionary.
          client_msg_no `string` minLength: 1; pattern: `\S` Stable client idempotency key. Reuse it for bounded retries of the same payload.
          payload `string` minLength: 1; encoding: base64 Base64 encoding of the message payload bytes.
          {"from_uid":"system","channel_id":"alice","channel_type":1,"client_msg_no":"order-20260828-0001","payload":"eyJ0eXBlIjoib3JkZXJfdXBkYXRlIn0="}
          200 HTTP request completed; inspect reason for the business result.
          SendChannelMessageResponse
          Message identity, Channel sequence, and protocol Reason Code.
          message_id `integer:int64` — Server message ID; meaningful when reason is success.
          message_seq `integer:uint64` 0–∞ Committed sequence in this Channel; meaningful when reason is success.
          reason `integer:uint8` enum: `1`, `2`, `3`, `4`, `5`, `9`, `11`, `13`, `15`, `18`, `19`, `24`, `25`; 0–255 Protocol Reason Code. HTTP 200 does not mean business success; only ReasonSuccess (1) confirms acceptance.
          {"message_id":123456789,"message_seq":42,"reason":1}
          400 Invalid request, payload, or Channel identity.
          SendError
          Message-send HTTP error. Unlisted text is not a stable machine contract.
          error `string` — Human-readable error detail.
          {"error":"invalid request"}
          404 The target Channel was not found.
          {"error":"channel not found"}
          408 The request was canceled or timed out.
          {"error":"request canceled"}
          {"error":"request timeout"}
          500 An internal send dependency failed. Error text is not stable.
          {"error":"message usecase not configured"}
          503 Refresh routing and retry, or wait for restore maintenance to end.
          RetryRequiredError
          The Channel authority route must be refreshed before a bounded retry.
          error `string` const: `"retry required"` Retry category for stale or unavailable authority routing.
          MaintenanceError
          Controller restore maintenance currently fences Product HTTP writes.
          error `string` const: `"maintenance"` Machine-readable maintenance category.
          message `string` const: `"restore maintenance is active"` Current maintenance detail.
          {"error":"retry required"}
          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-messaging-beta-en" operations={[{"path":"/message/send","method":"post"}]} showDescription />
    </>
  );
}
