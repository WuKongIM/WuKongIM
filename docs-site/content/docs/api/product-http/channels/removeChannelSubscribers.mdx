---
title: 移除持久订阅者
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 移除持久订阅者
        id: 移除持久订阅者
    contents:
      - content: 移除非空白订阅者；与 subscriber_add 不同，channel_type=0 不会被归一化。
        heading: 移除持久订阅者
      - content: >-
          POST /channel/subscriber_remove

          removeChannelSubscribers

          移除持久订阅者

          移除非空白订阅者；与 subscriber_add 不同，channel_type=0 不会被归一化。

          ChannelSubscriberRemoveRequest

          普通订阅者移除参数。

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^(?!.*[#@])(?=.*\S).+$`
          目标非空白 Channel ID；不得包含 # 或 @。

          channel_type `integer:uint8` 0–255; default: `0`; not: `1` 目标 Channel
          类型；类型 1 会被拒绝，省略或为 0 时不会归一化而是原样传入。

          reset `integer` default: `0` 兼容接收的整数标志；当前移除操作不会使用它。

          temp_subscriber `integer` — 兼容接收但被忽略的旧临时订阅者标志。

          subscribers array<`string`> minItems: 1 一个或多个要移除的非空白订阅者 UID。

          200 Mutation accepted

          StatusEnvelope

          旧式变更成功响应。

          additionalProperties false

          status `integer` const: `200` 固定为 200 的应用层状态。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受保护的运维边界
- **作用域**：只移除现有成员，不会隐式创建 Channel；channel_type=0 会原样传入。
- **原子性**：成员移除与 large 刷新是两个阶段，可能部分完成。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/subscriber_remove","method":"post"}]} showDescription />
    </>
  );
}
