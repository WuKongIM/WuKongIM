---
title: 替换 Channel 元数据
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: 替换 Channel 元数据
        id: 替换-channel-元数据
    contents:
      - content: 字段省略时仍把零值完整记录交给 UpdateInfo；该接口仅为兼容保留。
        heading: 替换-channel-元数据
      - content: >-
          POST /channel/info

          updateChannelInfo

          替换 Channel 元数据

          字段省略时仍把零值完整记录交给 UpdateInfo；该接口仅为兼容保留。

          ChannelInfoRequest

          additionalProperties true

          channel_id `string` default: `""`

          channel_type `integer:uint8` 0–255; default: `0`

          large `CompatibilityFlag` default: `0` Exactly 1 means true; every
          other integer means false.

          ban `CompatibilityFlag` default: `0` Exactly 1 means true; every other
          integer means false.

          disband `CompatibilityFlag` default: `0` Exactly 1 means true; every
          other integer means false.

          send_ban `CompatibilityFlag` default: `0` Exactly 1 means true; every
          other integer means false.

          allow_stranger `CompatibilityFlag` default: `0` Exactly 1 means true;
          every other integer means false.

          CompatibilityFlag

          Exactly 1 means true; every other integer means false.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/info","method":"post"}]} showDescription />
    </>
  );
}
