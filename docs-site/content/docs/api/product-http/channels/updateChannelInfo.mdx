---
title: 替换 Channel 元数据
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: 替换 Channel 元数据
        id: 替换-channel-元数据
    contents:
      - content: 字段省略时仍把零值完整记录交给 UpdateInfo；该接口仅为兼容保留。
        heading: 替换-channel-元数据
      - content: >-
          POST /channel/info

          updateChannelInfo

          替换 Channel 元数据

          字段省略时仍把零值完整记录交给 UpdateInfo；该接口仅为兼容保留。

          ChannelInfoRequest

          无必填字段且采用全量零值覆盖语义的旧式 Channel 元数据。

          additionalProperties true

          channel_id `string` default: `""` 要更新的 Channel ID；省略时为空字符串，入口不校验。

          channel_type `integer:uint8` 0–255; default: `0` 要更新的 Channel 类型；省略时为
          0，入口不校验。

          large `CompatibilityFlag` default: `0` 大群组兼容标志；仅 1 表示启用，其他整数均表示关闭。

          ban `CompatibilityFlag` default: `0` 全 Channel 禁用兼容标志；仅 1
          表示启用，其他整数均表示关闭。

          disband `CompatibilityFlag` default: `0` 终态解散兼容标志；仅 1 表示启用，其他整数均表示关闭。

          send_ban `CompatibilityFlag` default: `0` 禁止发送兼容标志；仅 1 表示启用，其他整数均表示关闭。

          allow_stranger `CompatibilityFlag` default: `0` 允许陌生人发送兼容标志；仅 1
          表示启用，其他整数均表示关闭。

          CompatibilityFlag

          大群组兼容标志；仅 1 表示启用，其他整数均表示关闭。

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/info","method":"post"}]} showDescription />
    </>
  );
}
