---
title: 替换临时 Channel 订阅者
full: true
_openapi:
  preload:
    - wukongim-product-http-management-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 替换临时 Channel 订阅者
        id: 替换临时-channel-订阅者
    contents:
      - content: 替换临时 Channel（Type 8）的全部成员；临时与持久订阅者相互独立。
        heading: 替换临时-channel-订阅者
      - content: >-
          POST /tmpchannel/subscriber_set

          setTemporaryChannelSubscribers

          替换临时 Channel 订阅者

          替换临时 Channel（Type 8）的全部成员；临时与持久订阅者相互独立。

          TemporarySubscriberSetRequest

          additionalProperties false

          channel_id `ChannelID` minLength: 1; pattern: `^[^#@]+$` 业务 Channel
          标识；已发布合同排除 # 与 @ namespace 分隔符。

          uids array<`UID`> minItems: 1 完整的临时订阅者快照。

          ChannelID

          业务 Channel 标识；已发布合同排除 # 与 @ namespace 分隔符。

          UID

          非空业务用户身份。

          200 变更已完成

          MutationStatus

          status `integer` const: `200` 兼容成功状态，固定为 200。

          {"status":200}

          400 输入格式错误、校验失败、路由不可用，或 usecase/存储错误

          CompatibilityError

          msg `string` — 供人阅读的运行时消息；精确文本不是稳定的机器合同。

          status `integer` const: `400` 这些入口的兼容业务错误状态固定为 400。

          {"msg":"invalid request","status":400}

          503 恢复维护已启用；请退避并观察 /readyz

          MaintenanceError

          error `string` const: `"maintenance"` 机器可读的恢复维护类别。

          message `string` const: `"restore maintenance is active"` 当前维护详情。

          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-management-beta-zh" operations={[{"path":"/tmpchannel/subscriber_set","method":"post"}]} showDescription />
    </>
  );
}
