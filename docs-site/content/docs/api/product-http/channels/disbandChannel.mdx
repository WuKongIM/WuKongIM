---
title: 终态解散 Channel
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 终态解散 Channel
        id: 终态解散-channel
    contents:
      - content: 设置持久化 disband 标志但不删除 Channel 身份；请求 Key 校验较弱。
        heading: 终态解散-channel
      - content: >-
          POST /channel/delete

          disbandChannel

          终态解散 Channel

          设置持久化 disband 标志但不删除 Channel 身份；请求 Key 校验较弱。

          WeakChannelKeyRequest

          兼容保留的弱校验 Channel Key。

          additionalProperties true

          channel_id `string` default: `""` 要解散的 Channel ID；省略时为空字符串，入口不校验。

          channel_type `integer:uint8` 0–255; default: `0` 要解散的 Channel 类型；1
          会被拒绝，其他值（包括 0）继续交给用例。

          200 Mutation accepted

          StatusEnvelope

          旧式变更成功响应。

          additionalProperties false

          status `integer` const: `200` 固定为 200 的应用层状态。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受保护的运维边界

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/delete","method":"post"}]} showDescription />
    </>
  );
}
