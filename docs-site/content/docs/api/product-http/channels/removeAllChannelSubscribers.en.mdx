---
title: Remove all durable subscribers
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Remove all durable subscribers
        id: remove-all-durable-subscribers
    contents:
      - content: Clears ordinary subscribers through bounded internal pages; person
          Channels are rejected.
        heading: remove-all-durable-subscribers
      - content: >-
          POST /channel/subscriber_remove_all

          removeAllChannelSubscribers

          Remove all durable subscribers

          Clears ordinary subscribers through bounded internal pages; person
          Channels are rejected.

          NonPersonChannelKeyRequest

          Non-person Channel key.

          additionalProperties true

          channel_id `string` minLength: 1 Logical identifier of the target
          Channel; for a person Channel this is normally the other UID.

          channel_type `integer:uint8` 2–255 Non-person Channel type, so the
          accepted range is 2–255.

          200 Mutation accepted

          StatusEnvelope

          Legacy mutation-success response.

          additionalProperties false

          status `integer` const: `200` Application-level status fixed at 200.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Trusted backend only
- **Scope**: Clears ordinary subscribers and then refreshes large; person Channels are not supported.
- **Atomicity**: Removal and large refresh are not one transaction.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/subscriber_remove_all","method":"post"}]} showDescription />
    </>
  );
}
