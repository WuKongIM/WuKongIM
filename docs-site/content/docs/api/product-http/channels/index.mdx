---
title: Channel
description: Channel 元数据、订阅者与允许或拒绝名单。
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

<Cards>
<Card href="/zh/api/product-http/channels/upsertChannel" title="创建或更新 Channel 元数据" description="创建或更新 Channel。reset=1 替换持久订阅者；个人 Channel 不接受订阅者；disband 为终态。" />
<Card href="/zh/api/product-http/channels/addChannelSubscribers" title="添加或替换持久订阅者" description="添加持久订阅者；Channel 不存在时创建元数据。channel_type 缺省或为 0 时使用类型 2；临时成员使用 /tmpchannel/subscriber_set。" />
<Card href="/zh/api/product-http/channels/removeAllChannelSubscribers" title="移除 Channel 的全部持久订阅者" description="移除全部持久订阅者，但不删除或解散 Channel；个人 Channel 会被拒绝。" />
<Card href="/zh/api/product-http/channels/setTemporaryChannelSubscribers" title="替换临时 Channel 订阅者" description="替换临时 Channel（Type 8）的全部成员；临时与持久订阅者相互独立。" />
<Card href="/zh/api/product-http/channels/addChannelDenylistMembers" title="添加 Channel 黑名单成员" description="向 Channel 拒绝列表添加 UID；路由沿用 blacklist 命名。" />
<Card href="/zh/api/product-http/channels/removeChannelDenylistMembers" title="移除 Channel 黑名单成员" description="从 Channel 范围的拒绝列表中移除给定 UID。" />
<Card href="/zh/api/product-http/channels/removeAllChannelDenylistMembers" title="移除 Channel 的全部黑名单成员" description="通过内部有界遍历清空 Channel 范围的拒绝列表。" />
<Card href="/zh/api/product-http/channels/addChannelAllowlistMembers" title="添加 Channel 白名单成员" description="向 Channel 允许列表添加非空白 UID；路由沿用 whitelist 命名。" />
<Card href="/zh/api/product-http/channels/removeChannelAllowlistMembers" title="移除 Channel 白名单成员" description="从 Channel 范围的允许列表中移除给定非空 UID。" />
<Card href="/zh/api/product-http/channels/removeAllChannelAllowlistMembers" title="移除 Channel 的全部白名单成员" description="通过内部有界遍历清空 Channel 范围的允许列表。" />
</Cards>

<Callout type="info" title="本 Beta 子集暂不发布">

- `POST /channel/info` — 缺少字段校验，且采用全量替换而非局部更新。
- `POST /channel/delete` — Key 校验较弱，且解散为终态。
- `POST /channel/subscriber_remove` — channel_type 为 0 时的行为与添加订阅者不一致。
- `POST /channel/blacklist_set`, `POST /channel/whitelist_set` — 全量替换的输入校验较弱。
- `GET /channel/whitelist` — 查询参数未校验，且返回无界完整列表。

</Callout>
