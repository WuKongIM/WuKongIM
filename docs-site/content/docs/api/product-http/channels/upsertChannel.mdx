---
title: 创建或更新 Channel 元数据
full: true
_openapi:
  preload:
    - wukongim-product-http-management-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 创建或更新 Channel 元数据
        id: 创建或更新-channel-元数据
    contents:
      - content: 创建或更新 Channel。reset=1 替换持久订阅者；个人 Channel 不接受订阅者；disband 为终态。
        heading: 创建或更新-channel-元数据
      - content: >-
          POST /channel

          upsertChannel

          创建或更新 Channel 元数据

          创建或更新 Channel。reset=1 替换持久订阅者；个人 Channel 不接受订阅者；disband 为终态。

          ChannelUpsertRequest

          additionalProperties false

          channel_id `ChannelID` minLength: 1; pattern: `^[^#@]+$` 业务 Channel
          标识；已发布合同排除 # 与 @ namespace 分隔符。

          channel_type `integer` 1–255 Channel Type。类型 1 是个人 Channel，不能包含
          subscribers。

          large `BinaryFlag` enum: `0`, `1`; default: `0` 兼容整数标志；只有 1 表示 true。

          ban `BinaryFlag` enum: `0`, `1`; default: `0` 兼容整数标志；只有 1 表示 true。

          disband `BinaryFlag` enum: `0`, `1`; default: `0` 兼容整数标志；只有 1 表示 true。

          send_ban `BinaryFlag` enum: `0`, `1`; default: `0` 兼容整数标志；只有 1 表示
          true。

          allow_stranger `BinaryFlag` enum: `0`, `1`; default: `0` 兼容整数标志；只有 1
          表示 true。

          reset `BinaryFlag` enum: `0`, `1`; default: `0` 1 表示在应用 subscribers
          前替换持久订阅者快照。

          subscribers array<`UID`> — 持久订阅者 UID；个人 Channel 不得提供。

          ChannelID

          业务 Channel 标识；已发布合同排除 # 与 @ namespace 分隔符。

          BinaryFlag

          兼容整数标志；只有 1 表示 true。

          UID

          非空业务用户身份。

          200 变更已完成

          MutationStatus

          status `integer` const: `200` 兼容成功状态，固定为 200。

          {"status":200}

          400 输入格式错误、校验失败、路由不可用，或 usecase/存储错误

          CompatibilityError

          msg `string` — 供人阅读的运行时消息；精确文本不是稳定的机器合同。

          status `integer` const: `400` 这些入口的兼容业务错误状态固定为 400。

          {"msg":"invalid request","status":400}

          503 恢复维护已启用；请退避并观察 /readyz

          MaintenanceError

          error `string` const: `"maintenance"` 机器可读的恢复维护类别。

          message `string` const: `"restore maintenance is active"` 当前维护详情。

          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-management-beta-zh" operations={[{"path":"/channel","method":"post"}]} showDescription />
    </>
  );
}
