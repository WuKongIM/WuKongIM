---
title: 创建或更新 Channel 元数据
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 创建或更新 Channel 元数据
        id: 创建或更新-channel-元数据
    contents:
      - content: 全量更新元数据标志并可重置订阅者；disband 为终态。
        heading: 创建或更新-channel-元数据
      - content: >-
          POST /channel

          upsertChannel

          创建或更新 Channel 元数据

          全量更新元数据标志并可重置订阅者；disband 为终态。

          ChannelUpsertRequest

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^[^#@]+$`

          channel_type `integer:uint8` 1–255

          large `CompatibilityFlag` default: `0` Exactly 1 means true; every
          other integer means false.

          ban `CompatibilityFlag` default: `0` Exactly 1 means true; every other
          integer means false.

          disband `CompatibilityFlag` default: `0` Exactly 1 means true; every
          other integer means false.

          send_ban `CompatibilityFlag` default: `0` Exactly 1 means true; every
          other integer means false.

          allow_stranger `CompatibilityFlag` default: `0` Exactly 1 means true;
          every other integer means false.

          reset `CompatibilityFlag` default: `0` Exactly 1 means true; every
          other integer means false.

          subscribers array<`string`> — Elements are not validated at entry; a
          non-empty list is rejected for person Channels.

          CompatibilityFlag

          Exactly 1 means true; every other integer means false.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel","method":"post"}]} showDescription />
    </>
  );
}
