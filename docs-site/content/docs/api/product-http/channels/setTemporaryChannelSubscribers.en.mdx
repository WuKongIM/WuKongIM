---
title: Replace temporary Channel subscribers
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Replace temporary Channel subscribers
        id: replace-temporary-channel-subscribers
    contents:
      - content: Replaces the derived Type-8 temporary subscriber list; UID elements are
          not validated by the entry adapter.
        heading: replace-temporary-channel-subscribers
      - content: >-
          POST /tmpchannel/subscriber_set

          setTemporaryChannelSubscribers

          Replace temporary Channel subscribers

          Replaces the derived Type-8 temporary subscriber list; UID elements
          are not validated by the entry adapter.

          TemporarySubscriberSetRequest

          Complete temporary-subscriber snapshot.

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^[^#@]+$` Temporary
          Channel ID; it must not be empty or contain # or @.

          uids array<`string`> minItems: 1 Replacement temporary-subscriber
          UIDs; at least one is required and entry does not validate element
          content.

          200 Mutation accepted

          StatusEnvelope

          Legacy mutation-success response.

          additionalProperties false

          status `integer` const: `200` Application-level status fixed at 200.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Trusted backend only
- **Scope**: Temporary subscribers use full-replacement semantics.
- **Atomicity**: The service removes all members before chunked insertion; failure may leave only part of the new set.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/tmpchannel/subscriber_set","method":"post"}]} showDescription />
    </>
  );
}
