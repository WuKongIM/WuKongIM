---
title: Replace temporary Channel subscribers
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Replace temporary Channel subscribers
        id: replace-temporary-channel-subscribers
    contents:
      - content: Replaces the derived Type-8 temporary subscriber list; UID elements are
          not validated by the entry adapter.
        heading: replace-temporary-channel-subscribers
      - content: >-
          POST /tmpchannel/subscriber_set

          setTemporaryChannelSubscribers

          Replace temporary Channel subscribers

          Replaces the derived Type-8 temporary subscriber list; UID elements
          are not validated by the entry adapter.

          TemporarySubscriberSetRequest

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^[^#@]+$`

          uids array<`string`> minItems: 1

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/tmpchannel/subscriber_set","method":"post"}]} showDescription />
    </>
  );
}
