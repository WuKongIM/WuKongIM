---
title: Replace Channel denylist members
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Replace Channel denylist members
        id: replace-channel-denylist-members
    contents:
      - content: Removes the old list before adding the supplied values; only channel_id
          is validated at entry.
        heading: replace-channel-denylist-members
      - content: >-
          POST /channel/blacklist_set

          setChannelDenylistMembers

          Replace Channel denylist members

          Removes the old list before adding the supplied values; only
          channel_id is validated at entry.

          ChannelMemberSetRequest

          Complete allowlist or denylist replacement parameters with legacy weak
          validation.

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `\S` Target non-blank
          Channel ID.

          channel_type `integer:uint8` 0–255; default: `0` Target Channel type;
          it may be omitted or 0 and is not rejected at entry.

          uids `array | null` — Complete replacement UID set; it may be omitted,
          empty, or null, and elements are not validated at entry.

          200 Mutation accepted

          StatusEnvelope

          Legacy mutation-success response.

          additionalProperties false

          status `integer` const: `200` Application-level status fixed at 200.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Protected operator boundary only
- **Scope**: The denylist uses full-replacement semantics.
- **Atomicity**: The list is removed and then inserted; the two stages are not transactional.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/blacklist_set","method":"post"}]} showDescription />
    </>
  );
}
