---
title: List all Channel allowlist members
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: GET
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: Query Parameters
      url: '#parameters-query'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: List all Channel allowlist members
        id: list-all-channel-allowlist-members
    contents:
      - content: Returns an unbounded full list; missing or invalid channel_type is
          silently treated as 0.
        heading: list-all-channel-allowlist-members
      - content: >-
          GET /channel/whitelist

          listChannelAllowlistMembers

          List all Channel allowlist members

          Returns an unbounded full list; missing or invalid channel_type is
          silently treated as 0.

          channel_id query optional `string` default: `""` Channel ID whose
          allowlist is read; this legacy endpoint does not validate the query
          parameter.

          channel_type query optional `integer` 0–255; default: `0` Channel type
          whose allowlist is read; parsed as uint8, with missing, invalid, or
          out-of-range text silently treated as 0.

          200 Complete legacy allowlist member array

          ChannelMemberListResponse

          ChannelMember

          Member in the legacy allowlist response.

          additionalProperties false

          id `integer:uint64` 0–∞ Internal member-record ID; do not use it as
          the UID.

          uid `string` — Unique identifier of the target user.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Protected operator boundary only

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/whitelist","method":"get"}]} showDescription />
    </>
  );
}
