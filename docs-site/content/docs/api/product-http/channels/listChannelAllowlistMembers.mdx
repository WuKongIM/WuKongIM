---
title: 列出全部 Channel 允许列表成员
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: GET
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: 查询参数
      url: '#parameters-query'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 列出全部 Channel 允许列表成员
        id: 列出全部-channel-允许列表成员
    contents:
      - content: 返回无界完整列表；channel_type 缺失或无效时会静默视为 0。
        heading: 列出全部-channel-允许列表成员
      - content: >-
          GET /channel/whitelist

          listChannelAllowlistMembers

          列出全部 Channel 允许列表成员

          返回无界完整列表；channel_type 缺失或无效时会静默视为 0。

          channel_id query optional `string` default: `""` 要读取允许列表的 Channel
          ID；此旧接口不会校验该查询参数。

          channel_type query optional `integer` 0–255; default: `0` 要读取允许列表的
          Channel 类型；按 uint8 解析，缺失、无效或越界文本会静默按 0 处理。

          200 Complete legacy allowlist member array

          ChannelMemberListResponse

          ChannelMember

          旧式允许列表中的成员。

          additionalProperties false

          id `integer:uint64` 0–∞ 内部成员记录 ID；不要用作 UID。

          uid `string` — 目标用户的唯一标识。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受保护的运维边界

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/whitelist","method":"get"}]} showDescription />
    </>
  );
}
