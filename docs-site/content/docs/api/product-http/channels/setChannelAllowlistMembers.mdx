---
title: 替换 Channel 允许列表
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 替换 Channel 允许列表
        id: 替换-channel-允许列表
    contents:
      - content: 先移除旧列表再添加给定值；入口只校验 channel_id。
        heading: 替换-channel-允许列表
      - content: >-
          POST /channel/whitelist_set

          setChannelAllowlistMembers

          替换 Channel 允许列表

          先移除旧列表再添加给定值；入口只校验 channel_id。

          ChannelMemberSetRequest

          允许列表或拒绝列表的完整替换参数，保留旧式弱校验。

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `\S` 目标非空白 Channel ID。

          channel_type `integer:uint8` 0–255; default: `0` 目标 Channel 类型；可省略或为
          0，入口不会拒绝。

          uids `array | null` — 替换后的完整 UID 集合；可省略、为空或为 null，入口不校验元素。

          200 Mutation accepted

          StatusEnvelope

          旧式变更成功响应。

          additionalProperties false

          status `integer` const: `200` 固定为 200 的应用层状态。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受保护的运维边界
- **作用域**：允许列表采用全量替换语义。
- **原子性**：先清空再加入；两个阶段不构成事务。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/whitelist_set","method":"post"}]} showDescription />
    </>
  );
}
