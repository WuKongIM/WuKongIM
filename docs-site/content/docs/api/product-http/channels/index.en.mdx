---
title: Channels
description: Channel metadata, subscribers, and allow or deny lists.
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

<Cards>
<Card href="/en/api/product-http/channels/upsertChannel" title="Create or update Channel metadata" description="Creates or updates a Channel. reset=1 replaces durable subscribers; person Channels reject subscribers. Disband is terminal." />
<Card href="/en/api/product-http/channels/addChannelSubscribers" title="Add or replace durable subscribers" description="Adds durable subscribers, creating Channel metadata when absent. Missing or zero channel_type means type 2; temporary members use /tmpchannel/subscriber_set." />
<Card href="/en/api/product-http/channels/removeAllChannelSubscribers" title="Remove all durable Channel subscribers" description="Removes all durable subscribers without deleting or disbanding the Channel. Person Channels are rejected." />
<Card href="/en/api/product-http/channels/setTemporaryChannelSubscribers" title="Replace temporary Channel subscribers" description="Replaces all members of the temporary Channel (Type 8). Temporary and durable subscribers are separate." />
<Card href="/en/api/product-http/channels/addChannelDenylistMembers" title="Add Channel denylist members" description="Adds UIDs to the Channel denylist; the route retains its legacy blacklist name." />
<Card href="/en/api/product-http/channels/removeChannelDenylistMembers" title="Remove Channel denylist members" description="Removes the listed UIDs from the Channel-scoped denylist." />
<Card href="/en/api/product-http/channels/removeAllChannelDenylistMembers" title="Remove all Channel denylist members" description="Clears the Channel-scoped denylist through bounded internal traversal." />
<Card href="/en/api/product-http/channels/addChannelAllowlistMembers" title="Add Channel allowlist members" description="Adds non-blank UIDs to the Channel allowlist; the route retains its legacy whitelist name." />
<Card href="/en/api/product-http/channels/removeChannelAllowlistMembers" title="Remove Channel allowlist members" description="Removes the listed non-empty UIDs from the Channel-scoped allowlist." />
<Card href="/en/api/product-http/channels/removeAllChannelAllowlistMembers" title="Remove all Channel allowlist members" description="Clears the Channel-scoped allowlist through bounded internal traversal." />
</Cards>

<Callout type="info" title="Not published in this beta subset">

- `POST /channel/info` — It lacks field validation and replaces the full record instead of patching it.
- `POST /channel/delete` — It has weak key validation and terminal disband semantics.
- `POST /channel/subscriber_remove` — Type-zero behavior differs from subscriber add.
- `POST /channel/blacklist_set`, `POST /channel/whitelist_set` — Full-replacement input validation is weak.
- `GET /channel/whitelist` — Its query is unvalidated and returns an unbounded full list.

</Callout>
