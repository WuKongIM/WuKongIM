---
title: Remove Channel denylist members
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Remove Channel denylist members
        id: remove-channel-denylist-members
    contents:
      - content: Removes the supplied UID strings from the derived Channel denylist.
        heading: remove-channel-denylist-members
      - content: >-
          POST /channel/blacklist_remove

          removeChannelDenylistMembers

          Remove Channel denylist members

          Removes the supplied UID strings from the derived Channel denylist.

          ChannelMemberMutationRequest

          Denylist add or remove parameters.

          additionalProperties true

          channel_id `string` minLength: 1 Logical identifier of the target
          Channel; for a person Channel this is normally the other UID.

          channel_type `integer:uint8` 1–255 Numeric Channel type; 1 is a person
          Channel, 2 is a group Channel, and other values are extension types.

          uids array<`string`> minItems: 1 One or more UIDs to add or remove;
          entry does not validate element content.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/blacklist_remove","method":"post"}]} showDescription />
    </>
  );
}
