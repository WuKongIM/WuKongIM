---
title: 添加或替换持久订阅者
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 添加或替换持久订阅者
        id: 添加或替换持久订阅者
    contents:
      - content: 添加非空白订阅者；channel_type=0 转为群组类型 2，reset=1 替换快照。
        heading: 添加或替换持久订阅者
      - content: >-
          POST /channel/subscriber_add

          addChannelSubscribers

          添加或替换持久订阅者

          添加非空白订阅者；channel_type=0 转为群组类型 2，reset=1 替换快照。

          ChannelSubscriberAddRequest

          普通订阅者添加或替换参数。

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^(?!.*[#@])(?=.*\S).+$`
          目标非空白 Channel ID；不得包含 # 或 @。

          channel_type `integer:uint8` 0–255; default: `2`; not: `1` 目标 Channel
          类型；省略或为 0 时转为群组类型 2，类型 1 会被拒绝。

          reset `integer` default: `0` 订阅者快照替换兼容标志；仅 1 表示启用，其他整数均表示关闭。

          temp_subscriber `integer` not: `1` 已废弃的临时订阅者标志；值 1 会被拒绝，其他值被忽略。

          subscribers array<`string`> minItems: 1 一个或多个要添加的非空白订阅者 UID。

          200 Mutation accepted

          StatusEnvelope

          旧式变更成功响应。

          additionalProperties false

          status `integer` const: `200` 固定为 200 的应用层状态。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受信业务后端
- **作用域**：目标 Channel 不存在时会被隐式创建；省略或传 0 的 channel_type 会按群组类型 2 处理。
- **原子性**：reset 清空、分批加入和 large 刷新是多阶段操作，可能部分完成。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/subscriber_add","method":"post"}]} showDescription />
    </>
  );
}
