---
title: 添加或替换持久订阅者
full: true
_openapi:
  preload:
    - wukongim-product-http-management-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 添加或替换持久订阅者
        id: 添加或替换持久订阅者
    contents:
      - content: 添加持久订阅者；Channel 不存在时创建元数据。channel_type 缺省或为 0 时使用类型 2；临时成员使用
          /tmpchannel/subscriber_set。
        heading: 添加或替换持久订阅者
      - content: >-
          POST /channel/subscriber_add

          addChannelSubscribers

          添加或替换持久订阅者

          添加持久订阅者；Channel 不存在时创建元数据。channel_type 缺省或为 0 时使用类型 2；临时成员使用
          /tmpchannel/subscriber_set。

          ChannelSubscriberAddRequest

          additionalProperties false

          channel_id `ChannelID` minLength: 1; pattern: `^[^#@]+$` 业务 Channel
          标识；已发布合同排除 # 与 @ namespace 分隔符。

          channel_type `integer` 0–255; default: `2` 省略或传 0 会选择群组类型 2；类型 1 会被拒绝。

          reset `BinaryFlag` enum: `0`, `1`; default: `0` 1 表示替换当前持久订阅者快照。

          temp_subscriber `integer` const: `0`; default: `0` 必须保持为 0；临时订阅者使用
          /tmpchannel/subscriber_set。

          subscribers array<`NonBlankUID`> minItems: 1 非空且元素非空白的持久订阅者 UID。

          ChannelID

          业务 Channel 标识；已发布合同排除 # 与 @ namespace 分隔符。

          BinaryFlag

          1 表示替换当前持久订阅者快照。

          NonBlankUID

          至少包含一个非空白字符的业务用户身份。

          200 变更已完成

          MutationStatus

          status `integer` const: `200` 兼容成功状态，固定为 200。

          {"status":200}

          400 输入格式错误、校验失败、路由不可用，或 usecase/存储错误

          CompatibilityError

          msg `string` — 供人阅读的运行时消息；精确文本不是稳定的机器合同。

          status `integer` const: `400` 这些入口的兼容业务错误状态固定为 400。

          {"msg":"invalid request","status":400}

          503 恢复维护已启用；请退避并观察 /readyz

          MaintenanceError

          error `string` const: `"maintenance"` 机器可读的恢复维护类别。

          message `string` const: `"restore maintenance is active"` 当前维护详情。

          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-management-beta-zh" operations={[{"path":"/channel/subscriber_add","method":"post"}]} showDescription />
    </>
  );
}
