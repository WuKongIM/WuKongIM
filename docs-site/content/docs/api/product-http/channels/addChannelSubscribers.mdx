---
title: 添加或替换持久订阅者
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 添加或替换持久订阅者
        id: 添加或替换持久订阅者
    contents:
      - content: 添加非空白订阅者；channel_type=0 转为群组类型 2，reset=1 替换快照。
        heading: 添加或替换持久订阅者
      - content: >-
          POST /channel/subscriber_add

          addChannelSubscribers

          添加或替换持久订阅者

          添加非空白订阅者；channel_type=0 转为群组类型 2，reset=1 替换快照。

          ChannelSubscriberAddRequest

          普通订阅者添加或替换参数。

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^[^#@]*\S[^#@]*$` 目标非空白
          Channel ID；不得包含 # 或 @。

          channel_type `integer:uint8` 0–255; default: `2` 目标 Channel 类型；省略或为 0
          时转为群组类型 2，类型 1 会被拒绝。

          reset `CompatibilityFlag` default: `0` 订阅者快照替换兼容标志；仅 1 表示启用，其他整数均表示关闭。

          temp_subscriber `integer` — 已废弃的临时订阅者标志；值 1 会被拒绝，其他值被忽略。

          subscribers array<`string`> minItems: 1 一个或多个要添加的非空白订阅者 UID。

          CompatibilityFlag

          订阅者快照替换兼容标志；仅 1 表示启用，其他整数均表示关闭。

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/subscriber_add","method":"post"}]} showDescription />
    </>
  );
}
