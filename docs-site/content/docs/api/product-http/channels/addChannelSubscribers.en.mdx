---
title: Add or replace durable subscribers
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Add or replace durable subscribers
        id: add-or-replace-durable-subscribers
    contents:
      - content: Adds non-blank subscribers; channel_type 0 becomes group type 2 and
          reset=1 replaces the snapshot.
        heading: add-or-replace-durable-subscribers
      - content: >-
          POST /channel/subscriber_add

          addChannelSubscribers

          Add or replace durable subscribers

          Adds non-blank subscribers; channel_type 0 becomes group type 2 and
          reset=1 replaces the snapshot.

          ChannelSubscriberAddRequest

          Ordinary-subscriber add or replacement parameters.

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^[^#@]*\S[^#@]*$` Target
          non-blank Channel ID; it must not contain # or @.

          channel_type `integer:uint8` 0–255; default: `2` Target Channel type;
          omission or 0 becomes group type 2, while type 1 is rejected.

          reset `CompatibilityFlag` default: `0` Subscriber-snapshot replacement
          compatibility flag; exactly 1 enables it and every other integer
          disables it.

          temp_subscriber `integer` — Deprecated temporary-subscriber flag;
          value 1 is rejected and every other value is ignored.

          subscribers array<`string`> minItems: 1 One or more non-blank
          subscriber UIDs to add.

          CompatibilityFlag

          Subscriber-snapshot replacement compatibility flag; exactly 1 enables
          it and every other integer disables it.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/subscriber_add","method":"post"}]} showDescription />
    </>
  );
}
