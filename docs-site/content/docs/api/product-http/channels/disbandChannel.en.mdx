---
title: Terminally disband a Channel
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: Terminally disband a Channel
        id: terminally-disband-a-channel
    contents:
      - content: Sets the durable disband flag without deleting Channel identity;
          request-key validation is weak.
        heading: terminally-disband-a-channel
      - content: >-
          POST /channel/delete

          disbandChannel

          Terminally disband a Channel

          Sets the durable disband flag without deleting Channel identity;
          request-key validation is weak.

          WeakChannelKeyRequest

          Compatibility Channel key with deliberately weak validation.

          additionalProperties true

          channel_id `string` default: `""` Channel ID to disband; omission
          produces an empty string and the entry does not validate it.

          channel_type `integer:uint8` 0–255; default: `0` Channel type to
          disband; 1 is rejected while every other value, including 0, is passed
          to the use case.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/delete","method":"post"}]} showDescription />
    </>
  );
}
