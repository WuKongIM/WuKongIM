---
title: Remove durable subscribers
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Remove durable subscribers
        id: remove-durable-subscribers
    contents:
      - content: Removes non-blank subscribers; unlike subscriber_add, channel_type 0 is
          not normalized.
        heading: remove-durable-subscribers
      - content: >-
          POST /channel/subscriber_remove

          removeChannelSubscribers

          Remove durable subscribers

          Removes non-blank subscribers; unlike subscriber_add, channel_type 0
          is not normalized.

          ChannelSubscriberRemoveRequest

          Ordinary-subscriber removal parameters.

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^(?!.*[#@])(?=.*\S).+$`
          Target non-blank Channel ID; it must not contain # or @.

          channel_type `integer:uint8` 0–255; default: `0`; not: `1` Target
          Channel type; type 1 is rejected, while omission or 0 is passed
          through without normalization.

          reset `integer` default: `0` Integer flag accepted for compatibility
          but not used by the removal operation.

          temp_subscriber `integer` — Legacy temporary-subscriber flag accepted
          and ignored for compatibility.

          subscribers array<`string`> minItems: 1 One or more non-blank
          subscriber UIDs to remove.

          200 Mutation accepted

          StatusEnvelope

          Legacy mutation-success response.

          additionalProperties false

          status `integer` const: `200` Application-level status fixed at 200.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Protected operator boundary only
- **Scope**: Removes existing members without implicitly creating the Channel; channel_type=0 is passed through unchanged.
- **Atomicity**: Member removal and large refresh are separate stages and may partially complete.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/subscriber_remove","method":"post"}]} showDescription />
    </>
  );
}
