---
title: Remove durable subscribers
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: Remove durable subscribers
        id: remove-durable-subscribers
    contents:
      - content: Removes non-blank subscribers; unlike subscriber_add, channel_type 0 is
          not normalized.
        heading: remove-durable-subscribers
      - content: >-
          POST /channel/subscriber_remove

          removeChannelSubscribers

          Remove durable subscribers

          Removes non-blank subscribers; unlike subscriber_add, channel_type 0
          is not normalized.

          ChannelSubscriberRemoveRequest

          Ordinary-subscriber removal parameters.

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^[^#@]*\S[^#@]*$` Target
          non-blank Channel ID; it must not contain # or @.

          channel_type `integer:uint8` 0–255; default: `0` Target Channel type;
          type 1 is rejected, while omission or 0 is passed through without
          normalization.

          reset `CompatibilityFlag` default: `0` Integer flag accepted for
          compatibility but not used by the removal operation.

          temp_subscriber `integer` — Legacy temporary-subscriber flag accepted
          and ignored for compatibility.

          subscribers array<`string`> minItems: 1 One or more non-blank
          subscriber UIDs to remove.

          CompatibilityFlag

          Integer flag accepted for compatibility but not used by the removal
          operation.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/subscriber_remove","method":"post"}]} showDescription />
    </>
  );
}
