---
title: Create or update Channel metadata
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Create or update Channel metadata
        id: create-or-update-channel-metadata
    contents:
      - content: Upserts all metadata flags and optionally resets subscribers; disband
          is terminal.
        heading: create-or-update-channel-metadata
      - content: >-
          POST /channel

          upsertChannel

          Create or update Channel metadata

          Upserts all metadata flags and optionally resets subscribers; disband
          is terminal.

          ChannelUpsertRequest

          Full Channel metadata update plus optional subscriber replacement
          parameters.

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `^(?!.*[#@])(?=.*\S).+$`
          Non-blank Channel ID to create or update; it must not contain # or @.

          channel_type `integer:uint8` 1–255 Numeric Channel type; 1 is a person
          Channel, 2 is a group Channel, and other values are extension types.

          large `integer` default: `0` Large-group compatibility flag; exactly 1
          enables it and every other integer disables it.

          ban `integer` default: `0` Channel-ban compatibility flag; exactly 1
          enables it and every other integer disables it.

          disband `integer` default: `0` Terminal-disband compatibility flag;
          exactly 1 enables it and every other integer disables it.

          send_ban `integer` default: `0` Send-ban compatibility flag; exactly 1
          enables it and every other integer disables it.

          allow_stranger `integer` default: `0` Allow-stranger compatibility
          flag; exactly 1 enables it and every other integer disables it.

          reset `integer` default: `0` Subscriber-snapshot replacement
          compatibility flag; exactly 1 enables it and every other integer
          disables it.

          subscribers `array | null` — Ordinary subscriber UIDs to add; it may
          be omitted, empty, or null, a person Channel rejects a non-empty list,
          and elements are not validated at entry.

          200 Mutation accepted

          StatusEnvelope

          Legacy mutation-success response.

          additionalProperties false

          status `integer` const: `200` Application-level status fixed at 200.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Trusted backend only
- **Scope**: Channel metadata and membership are written to cluster storage; large is recomputed from the current member count after membership changes.
- **Atomicity**: Metadata update, old-member removal, chunked member insertion, and large refresh are separate stages, not one transaction.
- **Recovery**: After a timeout or 400/5xx, replay the desired business state and verify membership through the protected Manager query.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel","method":"post"}]} showDescription />
    </>
  );
}
