---
title: Replace Channel metadata
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: Replace Channel metadata
        id: replace-channel-metadata
    contents:
      - content: Passes the zero-valued full record to UpdateInfo when fields are
          omitted; retained for compatibility.
        heading: replace-channel-metadata
      - content: >-
          POST /channel/info

          updateChannelInfo

          Replace Channel metadata

          Passes the zero-valued full record to UpdateInfo when fields are
          omitted; retained for compatibility.

          ChannelInfoRequest

          Legacy Channel metadata with no required fields and full zero-value
          replacement semantics.

          additionalProperties true

          channel_id `string` default: `""` Channel ID to update; omission
          produces an empty string and the entry does not validate it.

          channel_type `integer:uint8` 0–255; default: `0` Channel type to
          update; omission produces 0 and the entry does not validate it.

          large `CompatibilityFlag` default: `0` Large-group compatibility flag;
          exactly 1 enables it and every other integer disables it.

          ban `CompatibilityFlag` default: `0` Channel-ban compatibility flag;
          exactly 1 enables it and every other integer disables it.

          disband `CompatibilityFlag` default: `0` Terminal-disband
          compatibility flag; exactly 1 enables it and every other integer
          disables it.

          send_ban `CompatibilityFlag` default: `0` Send-ban compatibility flag;
          exactly 1 enables it and every other integer disables it.

          allow_stranger `CompatibilityFlag` default: `0` Allow-stranger
          compatibility flag; exactly 1 enables it and every other integer
          disables it.

          CompatibilityFlag

          Large-group compatibility flag; exactly 1 enables it and every other
          integer disables it.

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/info","method":"post"}]} showDescription />
    </>
  );
}
