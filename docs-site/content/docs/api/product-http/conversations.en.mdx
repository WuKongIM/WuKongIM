---
title: Conversations (Canonical Beta Subset)
description: Synchronize the Conversation projection with bounded cursors, retry
  unresolved keys, and monotonically maintain unread, hide, and activation
  state.
full: true
_openapi:
  preload:
    - wukongim-product-http-management-beta-en
  toc:
    - depth: 2
      title: Synchronize one bounded Conversation page
      url: '#synchronize-one-bounded-conversation-page'
    - depth: 2
      title: Retry unresolved Conversation keys
      url: '#retry-unresolved-conversation-keys'
    - depth: 2
      title: Clear a Conversation unread badge
      url: '#clear-a-conversation-unread-badge'
    - depth: 2
      title: Reduce a Conversation unread badge
      url: '#reduce-a-conversation-unread-badge'
    - depth: 2
      title: Hide a Conversation through the current head
      url: '#hide-a-conversation-through-the-current-head'
    - depth: 2
      title: Record explicit Conversation activation
      url: '#record-explicit-conversation-activation'
  structuredData:
    headings:
      - content: Synchronize one bounded Conversation page
        id: synchronize-one-bounded-conversation-page
      - content: Retry unresolved Conversation keys
        id: retry-unresolved-conversation-keys
      - content: Clear a Conversation unread badge
        id: clear-a-conversation-unread-badge
      - content: Reduce a Conversation unread badge
        id: reduce-a-conversation-unread-badge
      - content: Hide a Conversation through the current head
        id: hide-a-conversation-through-the-current-head
      - content: Record explicit Conversation activation
        id: record-explicit-conversation-activation
    contents:
      - content: Canonical endpoint that incrementally scans a UID-owned membership
          page, hydrates current Channel heads, and returns visible
          conversations, deletions, and retryable keys. Treat next_cursor as
          opaque and continue until done=true; an empty page is not proof of
          completion.
        heading: synchronize-one-bounded-conversation-page
      - content: Re-reads at most 200 unresolved keys without rewinding the completed
          list coverage. Duplicate keys are coalesced. Person-Channel input uses
          the peer UID. The response separates rebuilt conversations, deleted
          keys, and keys that remain unresolved.
        heading: retry-unresolved-conversation-keys
      - content: Monotonically advances read_seq to the latest committed Channel head.
          The request does not accept message_seq; an extra legacy message_seq
          field is ignored by the runtime and is intentionally absent from this
          contract.
        heading: clear-a-conversation-unread-badge
      - content: Advances read_seq so at most unread messages remain. It never moves
          read_seq backwards, so it cannot increase an unread count. This
          contract requires unread explicitly even though omitted JSON currently
          decodes as zero.
        heading: reduce-a-conversation-unread-badge
      - content: Persists deleted_to_seq through the latest committed Channel head for
          this user. It does not delete the Channel or membership; a later
          committed message can make the Conversation visible again. message_seq
          is not a request field.
        heading: hide-a-conversation-through-the-current-head
      - content: Records an explicit open, switch, or restore action in activated_at so
          the Conversation is prioritized in later list synchronization. Send,
          receive, delivery, and pull paths do not activate it implicitly.
        heading: record-explicit-conversation-activation
      - content: |-
          POST /conversation/list
          listConversations
          Synchronize one bounded Conversation page
          Canonical endpoint that incrementally scans a UID-owned membership page, hydrates current Channel heads, and returns visible conversations, deletions, and retryable keys. Treat next_cursor as opaque and continue until done=true; an empty page is not proof of completion.
          ConversationListRequest
          additionalProperties false
          uid `UID` minLength: 1 Non-empty product user identity.
          cursor `string` default: `""` Opaque base64url next_cursor returned by the previous page. Do not construct or edit it.
          limit `integer` 0–200; default: `50` Membership candidates scanned per page. Omitted or 0 selects 50.
          completed_coverage `integer:int64` 0–∞; default: `0` Coverage saved from a previous pass only after that pass returned done=true.
          UID
          Non-empty product user identity.
          200 Bounded Conversation synchronization result
          ConversationListResponse
          conversations array<`ConversationListItem`> — Visible hydrated Conversations from this membership page.
          deletes array<`ConversationKey`> — Keys the client must remove from its local Conversation projection.
          unresolved array<`ConversationKey`> — Keys whose Channel head was temporarily unavailable; retry them with /conversation/retry.
          next_cursor `string` — Opaque cursor for the next page. It can be present on an empty page.
          done `boolean` — True only when this full membership traversal is complete.
          coverage `integer:int64` 0–∞ Nanosecond coverage token for the current pass. Persist it only after done=true.
          tombstones_retained_since `integer:int64` 0–∞ Oldest deletion coverage still guaranteed; 0 means tombstones have not been expired.
          reset_required `boolean` — True when completed_coverage predates retained tombstones and the local projection must be rebuilt.
          ConversationListItem
          channel_id `string` — Visible Channel ID; a person Conversation is projected to the peer UID.
          channel_type `integer` 1–255
          active_at `integer:int64` 0–∞ Nanosecond timestamp of the last explicit activation; 0 means not explicitly activated.
          read_seq `integer` 0–∞ Monotonic badge floor. It is not a message pull cursor or a read receipt.
          deleted_to_seq `integer` 0–∞ Messages through this committed sequence are hidden for this user.
          unread `integer` 0–∞ Unread count after applying join, retention, deletion, own-send, and read floors.
          last_message `ConversationLastMessage` | `null` — Latest visible committed message, or null when no visible message exists.
          ConversationLastMessage
          message_id `integer:uint64` 0–∞ Numeric server message ID. JavaScript consumers should prefer message_idstr.
          message_idstr `string` — Precision-safe decimal server message ID.
          message_seq `integer` 0–∞ Committed sequence within the Channel.
          from_uid `string` — Sender UID.
          client_msg_no `string` — Client-generated message identifier.
          server_timestamp_ms `integer:int64` 0–∞ Server timestamp in Unix milliseconds.
          payload `string | null` encoding: base64 Go JSON representation of payload bytes; nil bytes encode as null.
          ConversationKey
          channel_id `string` minLength: 1 Visible Channel ID; a person Conversation uses the peer UID.
          channel_type `integer` 1–255 Channel Type.
          {"conversations":[{"channel_id":"team-42","channel_type":2,"active_at":1720000000000000000,"read_seq":8,"deleted_to_seq":0,"unread":2,"last_message":{"message_id":1234567890,"message_idstr":"1234567890","message_seq":10,"from_uid":"bob","client_msg_no":"client-10","server_timestamp_ms":1720000000000,"payload":"eyJ0eXBlIjoxLCJjb250ZW50IjoiaGkifQ=="}}],"deletes":[],"unresolved":[],"next_cursor":"AQ...","done":false,"coverage":1720000000000000000,"tombstones_retained_since":0,"reset_required":false}
          400 Malformed input, failed validation, unavailable routing, or a use-case/storage error
          CompatibilityError
          msg `string` — Human-readable runtime message; exact text is not a stable machine contract.
          status `integer` const: `400` Compatibility business-error status, always 400 on these routes.
          {"msg":"invalid request","status":400}
          503 Restore maintenance is active; back off and observe /readyz
          MaintenanceError
          error `string` const: `"maintenance"` Machine-readable restore-maintenance category.
          message `string` const: `"restore maintenance is active"` Current maintenance detail.
          {"error":"maintenance","message":"restore maintenance is active"}
          POST /conversation/retry
          retryConversations
          Retry unresolved Conversation keys
          Re-reads at most 200 unresolved keys without rewinding the completed list coverage. Duplicate keys are coalesced. Person-Channel input uses the peer UID. The response separates rebuilt conversations, deleted keys, and keys that remain unresolved.
          ConversationRetryRequest
          channels array<`ConversationKey`> minItems: 1; maxItems: 200 Unresolved keys returned by /conversation/list or a previous retry.
          POST /conversations/clearUnread
          clearConversationUnread
          Clear a Conversation unread badge
          Monotonically advances read_seq to the latest committed Channel head. The request does not accept message_seq; an extra legacy message_seq field is ignored by the runtime and is intentionally absent from this contract.
          ConversationMutationRequest
          channel_id `string` minLength: 1 Visible Channel ID. For Channel Type 1, use the peer UID.
          200 Mutation completed
          MutationStatus
          status `integer` const: `200` Compatibility success status, always 200.
          {"status":200}
          POST /conversations/setUnread
          setConversationUnread
          Reduce a Conversation unread badge
          Advances read_seq so at most unread messages remain. It never moves read_seq backwards, so it cannot increase an unread count. This contract requires unread explicitly even though omitted JSON currently decodes as zero.
          ConversationSetUnreadRequest
          unread `integer` 0–∞ Maximum unread count to leave after monotonically advancing read_seq.
          POST /conversations/delete
          hideConversation
          Hide a Conversation through the current head
          Persists deleted_to_seq through the latest committed Channel head for this user. It does not delete the Channel or membership; a later committed message can make the Conversation visible again. message_seq is not a request field.
          POST /conversations/activate
          activateConversation
          Record explicit Conversation activation
          Records an explicit open, switch, or restore action in activated_at so the Conversation is prioritized in later list synchronization. Send, receive, delivery, and pull paths do not activate it implicitly.
---

{/* This file is generated from the Product HTTP management OpenAPI contract and its locale supplement. Run `bun run openapi:write` instead of editing it directly. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-management-beta-en" webhooks={[]} operations={[{"path":"/conversation/list","method":"post"},{"path":"/conversation/retry","method":"post"},{"path":"/conversations/clearUnread","method":"post"},{"path":"/conversations/setUnread","method":"post"},{"path":"/conversations/delete","method":"post"},{"path":"/conversations/activate","method":"post"}]} showTitle showDescription />
    </>
  );
}

## Goal and completion criteria

This page publishes six operations for the canonical Conversation projection: bounded list traversal, unresolved-key retry, clear unread, set an unread ceiling, hide, and explicit activation. The trusted product backend should continue until `done=true`, handle `deletes` and `unresolved` independently, and rebuild its local projection when required.

## List synchronization state machine

1. Call `/conversation/list` first with an empty `cursor`, a bounded `limit`, and the saved `completed_coverage`;
2. apply `conversations`, remove `deletes`, and send `unresolved` to `/conversation/retry`;
3. even when the page arrays are empty, pass the opaque `next_cursor` back unchanged while `done=false`;
4. persist this pass's `coverage` only after `done=true`;
5. when `reset_required=true`, discard the stale local Conversation projection and rebuild from an empty cursor.

`read_seq` is a monotonic badge floor, not a message-pull cursor or read receipt. `unread` also applies join, retention, deletion, and current-user-last-send visibility floors. A person Channel projects `channel_id` to the peer UID at the HTTP boundary.

`ConversationListResponse` exposes `tombstones_retained_since` for reset decisions. Each visible item can carry a nullable `ConversationLastMessage`; use its string-safe `message_idstr` as the message identity, while `payload` is a nullable base64-encoded string in JSON. Compatibility failures use `CompatibilityError` (`400 {"msg","status":400}`); restore maintenance uses `MaintenanceError` (`503 {"error":"maintenance","message":"restore maintenance is active"}`).

## Mutation semantics

- `/conversations/clearUnread` advances to the latest committed head at call time and accepts no `message_seq`;
- `/conversations/setUnread` only advances `read_seq`, so it can reduce unread to at most the target but cannot increase unread;
- `/conversations/delete` hides this user's projection through the current committed head; it deletes neither the Channel nor membership, and a later message can make it visible again;
- `/conversations/activate` records an explicit open, switch, or restore action; send, receive, delivery, and pull paths do not call it implicitly.

## Legacy synchronization deferred

`POST /conversation/sync` remains a legacy compatibility route in the runtime, but it is outside this canonical Beta contract. It uses a delimited `last_msg_seqs` string, a bare-array response, legacy message projection, and a compatibility scan of at most 1,000 candidates without completion or truncation indicators. New integrations should use `/conversation/list` and `/conversation/retry`; existing callers should remain pinned until that route receives a separate exact-version calibration.

## Errors and troubleshooting

- **An empty page ends the loop early:** only `done=true` proves traversal completion; an empty `conversations` array does not.
- **A cursor returns 400:** the cursor is a versioned base64url value; echo it unchanged instead of decoding and reconstructing it.
- **Keys remain unresolved:** retry at most 200 keys and retain failures in a bounded backoff queue; do not rewind full-list coverage.
- **reset_required=true:** saved coverage predates retained tombstones, so rebuild the local projection.
- **503 maintenance:** pause business calls and observe `/readyz`.

## Security and OpenAPI contract

The request `uid` is merely a field, not authenticated identity. Default Product HTTP has no business-authentication middleware, so the product backend must prove authority to represent that UID and access each Channel. The page is generated from the management contract's `Conversations` tag; download the [management subset contract](/contracts/product-http-management.openapi.json).
