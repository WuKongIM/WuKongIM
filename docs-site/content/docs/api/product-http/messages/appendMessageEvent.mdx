---
title: 追加消息事件
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 追加消息事件
        id: 追加消息事件
    contents:
      - content: 校验并应用一次消息级事件投影；不支持非 null headers。
        heading: 追加消息事件
      - content: |-
          POST /message/event
          appendMessageEvent
          追加消息事件
          校验并应用一次消息级事件投影；不支持非 null headers。
          AppendMessageEventRequest
          消息事件投影参数。
          additionalProperties true
          channel_id `string` minLength: 1; pattern: `\S` 目标非空白 Channel ID；个人和 Agent Channel 会在用例层归一化。
          channel_type `integer:uint8` 1–255 Channel 类型编号；1 表示个人 Channel，2 表示群组 Channel，其他值属于扩展类型。
          from_uid `string` — 事件发起者 UID；个人或未编码 Agent Channel 的归一化可能需要它。
          message_id `integer:int64` — 可选服务端消息 ID；正数转为 uint64，0 或负数不会传入事件命令。
          client_msg_no `string` minLength: 1; pattern: `\S` 事件所对应消息的非空白客户端消息编号。
          event_id `string` minLength: 1; pattern: `\S` 该事件的非空白唯一标识。
          event_type `string` minLength: 1; pattern: `^\s*[sS][tT][rR][eE][aA][mM]\.(?:[oO][pP][eE][nN]|[dD][eE][lL][tT][aA]|[cC][lL][oO][sS][eE]|[eE][rR][rR][oO][rR]|[cC][aA][nN][cC][eE][lL]|[sS][nN][aA][pP][sS][hH][oO][tT]|[fF][iI][nN][iI][sS][hH])\s*$`; examples: `"stream.open"`, `"stream.delta"`, `"stream.close"`, `"stream.error"`, `"stream.cancel"`, `"stream.snapshot"`, `"stream.finish"` 事件类型；会去除首尾空白并转为小写。
          event_key `string` — 事件 lane Key；空值使用默认 lane，stream.finish 强制使用 finish lane。
          visibility `string` — 调用方提供并去除首尾空白后保存的事件元数据；当前 Product HTTP 消息同步不会用它做访问控制过滤。
          occurred_at `integer:int64` — 调用方提供的事件发生时间整数；入口不解释时间单位。
          payload `unknown` — 任意 JSON 事件 Payload；服务端按原始 JSON 字节传给事件存储。
          headers `null` — 预留字段；只能省略或传 null，任何非 null JSON 值都会被拒绝。
          200 Applied event projection
          AppendMessageEventResponse
          事件投影成功响应。
          additionalProperties false
          status `integer` const: `200` 固定为 200 的应用层状态。
          data `AppendMessageEventData` additionalProperties: `false` 已应用事件的规范化数据。
          AppendMessageEventData
          已应用事件的规范化数据。
          client_msg_no `string` — 目标消息的客户端消息编号。
          event_key `string` — 规范化后的事件 lane Key。
          event_id `string` — 已接受的事件唯一标识。
          msg_event_seq `integer:uint64` 0–∞ 该消息事件流内的递增序号。
          stream_status `string` enum: `"open"`, `"closed"`, `"error"`, `"cancelled"` 投影后的流状态。
          channel_id `string` — 规范化后的 Channel ID。
          channel_type `integer:uint8` 0–255 规范化后的 Channel 类型。
          from_uid `string` — 规范化后的事件发起者 UID。
          400 Input, validation, dependency, or downstream error
          CompatibilityError
          多个旧式入口共用的 HTTP 400 错误体。
          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。
          status `integer` const: `400` 固定为 400 的应用层状态。
          503 Controller restore maintenance is active
          MaintenanceError
          集群处于恢复维护期间的 503 错误体。
          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。
          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受信业务后端
- **作用域**：visibility 作为事件元数据保存；当前 Product HTTP 消息同步不会用它做访问控制过滤。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/message/event","method":"post"}]} showDescription />
    </>
  );
}
