---
title: 追加消息事件
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 追加消息事件
        id: 追加消息事件
    contents:
      - content: 校验并应用一次消息级事件投影；不支持非 null headers。
        heading: 追加消息事件
      - content: >-
          POST /message/event

          appendMessageEvent

          追加消息事件

          校验并应用一次消息级事件投影；不支持非 null headers。

          AppendMessageEventRequest

          additionalProperties true

          channel_id `string` minLength: 1

          channel_type `integer:uint8` 1–255

          from_uid `string` —

          message_id `integer:int64` — Positive values are converted to uint64;
          zero and negative values are omitted from the command.

          client_msg_no `string` minLength: 1

          event_id `string` minLength: 1

          event_type `string` minLength: 1

          event_key `string` — Empty selects the default lane; stream.finish
          forces the finish lane.

          visibility `string` —

          occurred_at `integer:int64` —

          payload `unknown` —

          headers `null` — Only absent or null is accepted.

          200 Applied event projection

          AppendMessageEventResponse

          additionalProperties false

          status `integer` const: `200`

          data `AppendMessageEventData` additionalProperties: `false`

          AppendMessageEventData

          client_msg_no `string` —

          event_key `string` —

          event_id `string` —

          msg_event_seq `integer:uint64` 0–∞

          stream_status `string` —

          channel_id `string` —

          channel_type `integer:uint8` 0–255

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/message/event","method":"post"}]} showDescription />
    </>
  );
}
