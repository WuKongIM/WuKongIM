---
title: Bind command-channel discovery
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Bind command-channel discovery
        id: bind-command-channel-discovery
    contents:
      - content: Starts bounded recipient discovery after one committed command tail.
        heading: bind-command-channel-discovery
      - content: >-
          POST /message/cmd/bind

          bindCommandChannel

          Bind command-channel discovery

          Starts bounded recipient discovery after one committed command tail.

          MessageCMDBindingRequest

          Command-Channel offline-discovery binding parameters.

          additionalProperties true

          uid `string` minLength: 1; pattern: `\S` Unique identifier of the
          target user.

          channel_id `string` minLength: 1; pattern: `\S` Logical identifier of
          the target Channel; for a person Channel this is normally the other
          UID.

          channel_type `integer:uint8` 1–255 Numeric Channel type; 1 is a person
          Channel, 2 is a group Channel, and other values are extension types.

          uids array<`string`> minItems: 1; maxItems: 1000 Source-Channel
          recipients, at most 1000 entries; trims blanks and duplicates.
          Exclusive with uid and subscribers.

          subscribers array<`string`> minItems: 1; maxItems: 1000 Use this field
          alone to bind the SEND temporary recipient scope; at most 1000
          entries, in the same order as SEND. Exclusive with source-Channel
          fields.

          200 Mutation accepted

          StatusEnvelope

          Legacy mutation-success response.

          additionalProperties false

          status `integer` const: `200` Application-level status fixed at 200.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Trusted backend only
- **Scope**: Persists discovery binding between a user and a command Channel; later /message/sync acknowledgement still depends on the current-process generation record.
- **Atomicity**: Choose uid plus source, uids plus source, or subscribers only. At most 1000 raw recipients and a 256 KiB body; subscribers must match SEND order.
- **Success**: Retry preserves live start and ack positions. Cross-Slot failure can be partial; retry the complete batch successfully before SEND.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/message/cmd/bind","method":"post"}]} showDescription />
    </>
  );
}
