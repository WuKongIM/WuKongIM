---
title: Bind command-channel discovery
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: Bind command-channel discovery
        id: bind-command-channel-discovery
    contents:
      - content: Starts durable offline discovery after the current command-channel
          tail.
        heading: bind-command-channel-discovery
      - content: >-
          POST /message/cmd/bind

          bindCommandChannel

          Bind command-channel discovery

          Starts durable offline discovery after the current command-channel
          tail.

          MessageCMDBindingRequest

          additionalProperties true

          uid `string` minLength: 1; pattern: `\S`

          channel_id `string` minLength: 1; pattern: `\S`

          channel_type `integer:uint8` 1–255

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/message/cmd/bind","method":"post"}]} showDescription />
    </>
  );
}
