---
title: 同步最多 200 个 Channel
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 同步最多 200 个 Channel
        id: 同步最多-200-个-channel
    contents:
      - content: 批量读取前校验全部成员关系；单项失败嵌入 HTTP 200 响应。
        heading: 同步最多-200-个-channel
      - content: >-
          POST /channel/messagesyncbatch

          syncChannelMessagesBatch

          同步最多 200 个 Channel

          批量读取前校验全部成员关系；单项失败嵌入 HTTP 200 响应。

          ChannelMessageSyncBatchRequest

          additionalProperties true

          login_uid `string` minLength: 1; pattern: `\S`

          items array<`ChannelMessageSyncBatchItemRequest`> minItems: 1;
          maxItems: 200

          ChannelMessageSyncBatchItemRequest

          login_uid `string` — Accepted by JSON decoding but ignored; the
          top-level login_uid is used.

          channel_id `string` minLength: 1; pattern: `\S`

          channel_type `integer:uint8` 1–255

          start_message_seq `integer:uint64` 0–∞; default: `0`

          end_message_seq `integer:uint64` 0–∞; default: `0`

          limit `integer` default: `100` Values <=0 select 100; values above
          10000 are capped.

          pull_mode `integer:uint8` 0–255; default: `0`

          include_event_meta `integer` default: `0`

          event_summary_mode `string` default: `""`

          200 Aligned batch result

          ChannelMessageSyncBatchResponse

          additionalProperties false

          items array<`ChannelMessageSyncBatchItemResponse`> —

          ChannelMessageSyncBatchItemResponse

          channel_id `string` —

          channel_type `integer:uint8` 0–255

          start_message_seq `integer:uint64` 0–∞

          end_message_seq `integer:uint64` 0–∞

          more `integer` enum: `0`, `1`

          messages array<`LegacyMessage`> —

          error `string` — Raw item-scoped error text; omitted on success.

          LegacyMessage

          header `LegacyMessageHeader` additionalProperties: `false`

          setting `integer:uint8` 0–255

          message_id `integer:int64` —

          message_idstr `string` —

          client_msg_no `string` —

          end `integer:uint8` 0–255

          end_reason `integer:uint8` 0–255

          error `string` —

          stream_data `string | null` encoding: base64

          event_meta `LegacyMessageEventMeta` additionalProperties: `false`

          event_sync_hint `LegacyMessageEventSyncHint` additionalProperties:
          `false`

          message_seq `integer:uint64` 0–∞

          from_uid `string` —

          topic `string` —

          expire `integer:uint32` 0–∞

          timestamp `integer:int32` —

          payload `string | null` encoding: base64

          LegacyMessageHeader

          no_persist `integer` enum: `0`, `1`

          red_dot `integer` enum: `0`, `1`

          sync_once `integer` enum: `0`, `1`

          LegacyMessageEventMeta

          has_events `boolean` —

          completed `boolean` —

          event_version `integer:uint64` 0–∞

          last_msg_event_seq `integer:uint64` 0–∞

          event_count `integer` 0–∞

          open_event_count `integer` 0–∞

          events array<`LegacyMessageEventKeyMeta`> —

          LegacyMessageEventKeyMeta

          event_key `string` —

          status `string` —

          snapshot `unknown` —

          LegacyMessageEventSyncHint

          from_msg_event_seq `integer:uint64` 0–∞

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/messagesyncbatch","method":"post"}]} showDescription />
    </>
  );
}
