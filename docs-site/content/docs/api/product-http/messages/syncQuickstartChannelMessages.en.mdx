---
title: Synchronize one Channel's committed messages
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Synchronize one Channel's committed messages
        id: synchronize-one-channels-committed-messages
    contents:
      - content: Checks membership visibility and returns an ascending page; limit is
          100 by default and capped at 10000.
        heading: synchronize-one-channels-committed-messages
      - content: >-
          POST /channel/messagesync

          syncQuickstartChannelMessages

          Synchronize one Channel's committed messages

          Checks membership visibility and returns an ascending page; limit is
          100 by default and capped at 10000.

          ChannelMessageSyncRequest

          Single-Channel committed-message synchronization parameters.

          additionalProperties true

          login_uid `string` minLength: 1; pattern: `\S` User identity
          performing the read and required to own the corresponding Channel
          membership.

          channel_id `string` minLength: 1; pattern: `\S` Logical identifier of
          the target Channel; for a person Channel this is normally the other
          UID.

          channel_type `integer:uint8` 1–255 Numeric Channel type; 1 is a person
          Channel, 2 is a group Channel, and other values are extension types.

          start_message_seq `integer:uint64` 0–∞; default: `0` Channel message
          sequence used as the read starting point; 0 means no explicit starting
          point.

          end_message_seq `integer:uint64` 0–∞; default: `0` Channel message
          sequence used as the read ending point; 0 means no explicit ending
          point.

          limit `integer` default: `100` Maximum messages returned by this read;
          values at or below 0 use 100 and values above 10000 are capped at
          10000.

          pull_mode `integer:uint8` 0–255; default: `0` Pull direction; 1 reads
          toward newer messages and every other uint8 value uses the
          compatibility branch for older messages.

          include_event_meta `integer` default: `0` Message-event summary
          switch; a non-zero value requests event metadata and defaults an empty
          event_summary_mode to full.

          event_summary_mode `string` default: `""` Event-summary mode; empty
          disables enrichment, full includes snapshots, and any other non-empty
          value returns the basic summary without snapshots.

          200 One committed-message page

          ChannelMessageSyncResponse

          additionalProperties false

          start_message_seq `integer:uint64` 0–∞

          end_message_seq `integer:uint64` 0–∞

          more `integer` enum: `0`, `1`

          messages array<`LegacyMessage`> —

          LegacyMessage

          header `LegacyMessageHeader` additionalProperties: `false`

          setting `integer:uint8` 0–255

          message_id `integer:int64` —

          message_idstr `string` —

          client_msg_no `string` —

          end `integer:uint8` 0–255

          end_reason `integer:uint8` 0–255

          error `string` —

          stream_data `string | null` encoding: base64

          event_meta `LegacyMessageEventMeta` additionalProperties: `false`

          event_sync_hint `LegacyMessageEventSyncHint` additionalProperties:
          `false`

          message_seq `integer:uint64` 0–∞

          from_uid `string` —

          channel_id `string` —

          channel_type `integer:uint8` 0–255

          topic `string` —

          expire `integer:uint32` 0–∞

          timestamp `integer:int32` —

          payload `string | null` encoding: base64

          LegacyMessageHeader

          no_persist `integer` enum: `0`, `1`

          red_dot `integer` enum: `0`, `1`

          sync_once `integer` enum: `0`, `1`

          LegacyMessageEventMeta

          has_events `boolean` —

          completed `boolean` —

          event_version `integer:uint64` 0–∞

          last_msg_event_seq `integer:uint64` 0–∞

          event_count `integer` 0–∞

          open_event_count `integer` 0–∞

          events array<`LegacyMessageEventKeyMeta`> —

          LegacyMessageEventKeyMeta

          event_key `string` —

          status `string` —

          snapshot `unknown` —

          LegacyMessageEventSyncHint

          from_msg_event_seq `integer:uint64` 0–∞

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/messagesync","method":"post"}]} showDescription />
    </>
  );
}
