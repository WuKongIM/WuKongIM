---
title: Synchronize up to 200 Channels
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Synchronize up to 200 Channels
        id: synchronize-up-to-200-channels
    contents:
      - content: Validates all memberships before one aligned batch read; item failures
          are embedded in the HTTP-200 response.
        heading: synchronize-up-to-200-channels
      - content: >-
          POST /channel/messagesyncbatch

          syncChannelMessagesBatch

          Synchronize up to 200 Channels

          Validates all memberships before one aligned batch read; item failures
          are embedded in the HTTP-200 response.

          ChannelMessageSyncBatchRequest

          Batch Channel committed-message synchronization parameters.

          additionalProperties true

          login_uid `string` minLength: 1; pattern: `\S` User identity
          performing the read and required to own the corresponding Channel
          membership.

          items array<`ChannelMessageSyncBatchItemRequest`> minItems: 1;
          maxItems: 200 Channels to synchronize; between 1 and 200 items are
          accepted and response order matches request order.

          ChannelMessageSyncBatchItemRequest

          Read criteria for one Channel inside a batch synchronization request.

          login_uid `string` — Accepted for compatibility but ignored; batch
          requests always use the top-level login_uid.

          channel_id `string` minLength: 1; pattern: `\S` Logical identifier of
          the target Channel; for a person Channel this is normally the other
          UID.

          channel_type `integer:uint8` 1–255 Numeric Channel type; 1 is a person
          Channel, 2 is a group Channel, and other values are extension types.

          start_message_seq `integer:uint64` 0–∞; default: `0` Inclusive Channel
          message-sequence starting boundary; 0 selects the open end, and newer
          reads also raise it to the membership visibility floor.

          end_message_seq `integer:uint64` 0–∞; default: `0` Exclusive Channel
          message-sequence ending boundary; 0 selects the open end.

          limit `integer` default: `100` Maximum messages returned by this read;
          values at or below 0 use 100 and values above 10000 are capped at
          10000.

          pull_mode `integer:uint8` 0–255; default: `0` Pull direction; 1 reads
          toward newer messages and every other uint8 value uses the
          compatibility branch for older messages.

          include_event_meta `integer` default: `0` Message-event summary
          switch; a non-zero value requests event metadata and defaults an empty
          event_summary_mode to full.

          event_summary_mode `string` default: `""` Event-summary mode; empty
          disables enrichment, full includes snapshots, and any other non-empty
          value returns the basic summary without snapshots.

          200 Aligned batch result

          ChannelMessageSyncBatchResponse

          Batch result aligned in order with request items.

          additionalProperties false

          items array<`ChannelMessageSyncBatchItemResponse`> — Independent
          per-Channel synchronization results.

          ChannelMessageSyncBatchItemResponse

          Independent result for one Channel in a batch sync.

          channel_id `string` — Channel ID from the corresponding request item.

          channel_type `integer:uint8` 0–255 Channel type from the corresponding
          request item.

          start_message_seq `integer:uint64` 0–∞ Starting sequence of this item
          effective range.

          end_message_seq `integer:uint64` 0–∞ Ending sequence of this item
          effective range.

          more `integer` enum: `0`, `1` Whether another page may exist for this
          item.

          messages array<`LegacyMessage`> — Messages returned when this item
          succeeds.

          error `string` — Per-item error text that must be checked even with
          HTTP 200; empty means no item error.

          LegacyMessage

          Legacy message projection used by Product HTTP synchronization.

          header `LegacyMessageHeader` additionalProperties: `false` Legacy
          message header.

          setting `integer:uint8` 0–255 WKProto Setting bitset.

          message_id `integer:int64` — Server int64 message ID.

          message_idstr `string` — Decimal string mirror of message_id only; it
          is not a replacement for message_seq.

          client_msg_no `string` — Client message number.

          end `integer:uint8` 0–255 Stream-message terminal compatibility flag.

          end_reason `integer:uint8` 0–255 Stream-message terminal Reason Code.

          error `string` — Stream-message error description.

          stream_data `string | null` encoding: base64 Base64-encoded stream
          data; it may be null.

          event_meta `LegacyMessageEventMeta` additionalProperties: `false`
          Optional message-event summary.

          event_sync_hint `LegacyMessageEventSyncHint` additionalProperties:
          `false` Optional message-event continuation hint.

          message_seq `integer:uint64` 0–∞ uint64 sequence of the message within
          this Channel.

          from_uid `string` — Message sender UID.

          channel_id `string` — Channel ID containing the message.

          channel_type `integer:uint8` 0–255 Channel type containing the
          message.

          topic `string` — Optional message topic.

          expire `integer:uint32` 0–∞ Message expiration in seconds; 0 means
          unset.

          timestamp `integer:int32` — Legacy server timestamp in seconds.

          payload `string | null` encoding: base64 Base64-encoded message
          payload; it may be null.

          LegacyMessageHeader

          Legacy message header.

          no_persist `integer` enum: `0`, `1` 1 means the message is
          non-durable.

          red_dot `integer` enum: `0`, `1` 1 means the client should show a
          red-dot indicator.

          sync_once `integer` enum: `0`, `1` 1 means a one-shot command message.

          LegacyMessageEventMeta

          Optional message-event summary.

          has_events `boolean` — Whether events exist.

          completed `boolean` — Whether all known event lanes have completed.

          event_version `integer:uint64` 0–∞ Event-summary version.

          last_msg_event_seq `integer:uint64` 0–∞ Latest event sequence across
          all lanes.

          event_count `integer` 0–∞ Total event count.

          open_event_count `integer` 0–∞ Number of event lanes still open.

          events array<`LegacyMessageEventKeyMeta`> — Per-lane event summaries.

          LegacyMessageEventKeyMeta

          Summary of one event lane.

          event_key `string` — Event lane key.

          status `string` — Current state of the lane.

          last_msg_event_seq `integer:uint64` 0–∞ Latest known event sequence
          for the lane.

          snapshot `unknown` — Arbitrary JSON snapshot in full summary mode.

          end_reason `integer:uint8` 0–255 Event-stream terminal Reason Code.

          error `string` — Event-stream error description.

          LegacyMessageEventSyncHint

          Optional message-event continuation hint.

          client_msg_no `string` — Client message number of the target message.

          from_msg_event_seq `integer:uint64` 0–∞ Starting sequence for the next
          event synchronization.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Trusted backend only
- **Scope**: Each items entry runs independently; response order matches request order.
- **Success**: Even with HTTP 200, inspect each item error; one failed item does not make the whole batch non-2xx.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/channel/messagesyncbatch","method":"post"}]} showDescription />
    </>
  );
}
