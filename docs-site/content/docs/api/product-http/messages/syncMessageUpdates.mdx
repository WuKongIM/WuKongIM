---
title: 拉取单频道消息修改
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 拉取单频道消息修改
        id: 拉取单频道消息修改
    contents:
      - content: 先用空游标获取基线，再加载历史；拉完 more 分页，在同一事务中合并版本并保存游标。此接口返回最新状态。
        heading: 拉取单频道消息修改
      - content: |-
          POST /channel/messageupdates
          syncMessageUpdates
          拉取单频道消息修改
          先用空游标获取基线，再加载历史；拉完 more 分页，在同一事务中合并版本并保存游标。此接口返回最新状态。
          MessageUpdatesRequest
          拉取当前可见的单个频道的最新修改。
          additionalProperties false
          login_uid `string` — 用户标识；个人频道规范化和增量同步必填，不代表修改权限。
          channel_id `string` minLength: 1 频道 ID；个人频道使用对方 UID。
          channel_type `integer` 1–255 频道类型：1 个人，2 群组。
          update_cursor `string` maxLength: 4096 不透明游标，最多 4096 字节；空值先获取基线，再加载历史。
          limit `integer` 0–200; default: `100` 最多更新条数；省略或 0 使用 100。
          {"login_uid":"bob","channel_id":"alice","channel_type":1,"update_cursor":"","limit":100}
          200 Success
          MessageUpdatesResponse
          最新状态增量页。
          updates array<`UpdatedMessage`> maxItems: 200 最新状态而非完整修改历史；more 为 true 时也可能为空。
          next_update_cursor `string` — 与合并后的消息在同一本地事务中保存。
          more `boolean` — 为 true 时继续拉取此频道。
          reset_required `boolean` — 先保存此基线再加载可见历史；恢复代数变化后重置旧版本比较。
          UpdatedMessage
          Product HTTP 同步接口使用的旧式消息投影。
          header `LegacyMessageHeader` additionalProperties: `false` 旧式消息 Header。
          setting `integer:uint8` 0–255 WKProto Setting 位图。
          message_id `string` pattern: `^[0-9]+$` 服务端 int64 消息 ID。
          message_idstr `string` — 仅 message_id 的十进制字符串镜像；不是 message_seq 的替代字段。
          client_msg_no `string` — 原客户端编号；旧记录为空且有消息 ID 时，返回稳定的 wk3-legacy-<message_id> 只读兼容编号。可用于 /messages 查询，不是发送幂等键或事件写入键；原始记录不改写。
          end `integer:uint8` 0–255 流消息结束兼容标志。
          end_reason `integer:uint8` 0–255 流消息结束 Reason Code。
          error `string` — 流消息错误说明。
          stream_data `string | null` encoding: base64 Base64 编码的流数据；可能为 null。
          event_meta `LegacyMessageEventMeta` additionalProperties: `false` 可选消息事件摘要。
          event_sync_hint `LegacyMessageEventSyncHint` additionalProperties: `false` 可选消息事件继续同步提示。
          message_seq `string` pattern: `^[0-9]+$` 消息在该 Channel 内的 uint64 序号。
          from_uid `string` — 消息发送者 UID。
          channel_id `string` — 消息所属 Channel ID。
          channel_type `integer:uint8` 0–255 消息所属 Channel 类型。
          topic `string` — 可选消息 Topic。
          expire `integer:uint32` 0–∞ 消息过期秒数；0 表示未设置。
          timestamp `integer:int32` — 服务端旧式秒级时间戳。
          payload `string | null` encoding: base64 Base64 编码消息 Payload；可能为 null。
          version `string` pattern: `^[0-9]+$` 内容修改版本；同一恢复代数内省略表示 0。
          updated_at_ms `integer:int64` 0–∞ 服务端修改时间，Unix 毫秒；不改变原消息排序。
          LegacyMessageHeader
          旧式消息 Header。
          no_persist `integer` enum: `0`, `1` 1 表示消息不持久化。
          red_dot `integer` enum: `0`, `1` 1 表示客户端应显示红点。
          sync_once `integer` enum: `0`, `1` 1 表示一次性命令消息。
          LegacyMessageEventMeta
          可选消息事件摘要。
          has_events `boolean` — 是否存在事件。
          completed `boolean` — 所有已知事件 lane 是否已结束。
          event_version `integer:uint64` 0–∞ 事件摘要版本。
          last_msg_event_seq `integer:uint64` 0–∞ 所有 lane 中最新的事件序号。
          event_count `integer` 0–∞ 事件总数。
          open_event_count `integer` 0–∞ 仍未结束的事件 lane 数量。
          events array<`LegacyMessageEventKeyMeta`> — 按事件 lane 返回的摘要。
          LegacyMessageEventKeyMeta
          一条事件 lane 的摘要。
          event_key `string` — 事件 lane Key。
          status `string` — 该 lane 的当前状态。
          last_msg_event_seq `integer:uint64` 0–∞ 该 lane 已知的最新事件序号。
          snapshot `unknown` — full 摘要模式下的任意 JSON 快照。
          end_reason `integer:uint8` 0–255 事件流结束 Reason Code。
          error `string` — 事件流错误说明。
          LegacyMessageEventSyncHint
          可选消息事件继续同步提示。
          client_msg_no `string` — 目标消息的客户端消息编号。
          from_msg_event_seq `integer:uint64` 0–∞ 下一次事件同步的起始序号。
          400 invalid_request
          MessageUpdateError
          修改或临时读取失败；不返回部分结果。
          status `integer` — HTTP 状态。
          code `string` — 稳定错误码；结果不确定的写入保留完整重试身份。
          msg `string` — 可选旧式诊断文本。
          403 channel_not_accessible
          404 message_not_found
          409 version_conflict, content_epoch_conflict, idempotency_conflict, reset_required or stale_meta
          422 message_not_updatable
          429 resource_exhausted
          503 Temporary dependency failure or restore maintenance
          MaintenanceError
          集群处于恢复维护期间的 503 错误体。
          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。
          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受信业务后端
- **作用域**：仅在进入、重连或继续查看当前频道时同步；不是所有会话的更新发现接口。
- **成功判定**：先获得基线再读取可见历史；more=true 时继续分页，即使 updates 为空。消息与游标在同一事务合并；旧消息修改不能覆盖新末条摘要。
- **恢复**：reset_required=true 时按新基线重载可见历史；503 保留原游标并退避重试。忽略旧恢复代数的延迟响应。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/channel/messageupdates","method":"post"}]} showDescription />
    </>
  );
}
