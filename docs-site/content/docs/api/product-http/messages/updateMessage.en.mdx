---
title: Replace a message payload
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Replace a message payload
        id: replace-a-message-payload
    contents:
      - content: Replaces an ordinary retained payload with version and restore-epoch
          checks. CMD, stream and nonpersistent messages cannot be edited.
        heading: replace-a-message-payload
      - content: |-
          POST /message/update
          updateMessage
          Replace a message payload
          Replaces an ordinary retained payload with version and restore-epoch checks. CMD, stream and nonpersistent messages cannot be edited.
          UpdateMessageRequest
          Replace an ordinary retained message; the backend owns editing permission.
          additionalProperties false
          login_uid `string` — User identity; required for person-channel normalization and edit sync. It does not authorize editing.
          channel_id `string` minLength: 1 Channel ID; use the peer UID for a person channel.
          channel_type `integer` 1–255 Channel type: 1 person, 2 group.
          message_id `string` pattern: `^[0-9]+$` Existing nonzero message ID.
          expected_content_epoch `string` pattern: `^[0-9]+$` Epoch from the response that supplied the displayed message.
          expected_version `string` pattern: `^[0-9]+$` Displayed version; use 0 for an unedited original.
          request_id `string` minLength: 1; maxLength: 128 Stable retry ID, at most 128 UTF-8 bytes.
          payload `string` minLength: 1; encoding: base64 Complete replacement; 1 to 1 MiB decoded bytes, 2 MiB request-body limit.
          {"login_uid":"alice","channel_id":"bob","channel_type":1,"message_id":"12345","expected_version":"0","expected_content_epoch":"0","request_id":"5893e137-1994-4377-b4ed-1f0c9e8fe4bd","payload":"bmV3"}
          200 Success
          UpdateMessageResponse
          Successful edit envelope.
          status `integer` const: `200` HTTP status.
          data `MessageUpdateResult` additionalProperties: `false` Committed edit result.
          MessageUpdateResult
          Committed edit result.
          message_id `string` pattern: `^[0-9]+$` Original message ID.
          message_seq `string` pattern: `^[0-9]+$` Original channel sequence.
          version `string` pattern: `^[0-9]+$` Edited content version; absent means zero in the same content epoch.
          updated_at_ms `integer:int64` 0–∞ Server edit time in Unix milliseconds; original ordering is unchanged.
          400 invalid_request
          MessageUpdateError
          Edit or transient read failure; no partial results.
          status `integer` — HTTP status.
          code `string` — Stable rejection code; uncertain writes retain their exact retry identity.
          msg `string` — Optional legacy diagnostic message.
          403 channel_not_accessible
          404 message_not_found
          409 version_conflict, content_epoch_conflict, idempotency_conflict, reset_required or stale_meta
          422 message_not_updatable
          429 resource_exhausted
          503 Temporary dependency failure or restore maintenance
          MaintenanceError
          503 error body returned while cluster restore maintenance is active.
          error `string` const: `"maintenance"` Stable maintenance error category.
          message `string` const: `"restore maintenance is active"` Current restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Trusted backend only
- **Scope**: The backend owns editing permissions and time windows; message ID, sequence, original timestamp, unread and ordering stay unchanged.
- **Success**: 200 means the edit and idempotency result were quorum committed and applied by the Slot; it does not wait for devices.
- **Recovery**: Retry uncertain outcomes with identical request_id, version, content epoch and payload. After a version or epoch conflict, reload and make a new editing decision.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/message/update","method":"post"}]} showDescription />
    </>
  );
}
