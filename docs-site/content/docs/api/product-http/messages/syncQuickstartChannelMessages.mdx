---
title: 拉取一个 Channel 的持久消息
full: true
_openapi:
  preload:
    - wukongim-product-http-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 拉取一个 Channel 的持久消息
        id: 拉取一个-channel-的持久消息
    contents:
      - content: 重连后有界拉取已提交消息；快速开始使用个人 Channel（channel_type 1）。
        heading: 拉取一个-channel-的持久消息
      - content: |-
          POST /channel/messagesync
          syncQuickstartChannelMessages
          拉取一个 Channel 的持久消息
          重连后有界拉取已提交消息；快速开始使用个人 Channel（channel_type 1）。
          ChannelMessageSyncRequest
          针对一个可见 Channel 的有界已提交消息恢复请求。
          additionalProperties false
          login_uid `string` minLength: 1 受信后端代表的当前用户；用于个人 Channel 归一化和 membership 可见性。
          channel_id `string` minLength: 1 个人 Channel 快速开始使用对端 UID。
          channel_type `integer` 0–255 快速开始使用 1 表示个人 Channel。
          start_message_seq `integer` 0–∞; default: `0` 非零时为包含式起点；结束边界也为 0 时，全零边界选择最近页兼容行为。
          end_message_seq `integer` 0–∞; default: `0` 非零时为排除式结束边界；起点也为 0 时，最近页不设置上界。
          limit `integer` 1–100; default: `100` 黄金路径 BFF 边界；兼容运行时内部上限更宽。
          pull_mode `integer` enum: `0`, `1`; default: `1` 拉取方向：0 拉取更旧消息，1 拉取更新消息。
          include_event_meta `integer` enum: `0`, `1`; default: `0` 设为 1 时请求可用的紧凑事件元数据；黄金路径使用 0。
          event_summary_mode `string` default: `""` 兼容事件摘要选择器；黄金路径保持为空。
          {"login_uid":"bob","channel_id":"alice","channel_type":1,"start_message_seq":0,"end_message_seq":0,"limit":100,"pull_mode":1,"include_event_meta":0,"event_summary_mode":""}
          200 有界的持久消息分页
          ChannelMessageSyncResponse
          按 sequence 升序排列的一页有界已提交消息。
          start_message_seq `integer` 0–∞ 从请求回显的起始边界。
          end_message_seq `integer` 0–∞ 从请求回显的结束边界。
          more `integer` enum: `0`, `1` 1 表示可能还有下一页；再次请求前必须推进游标。
          messages array<`SyncedMessage`> — 按 message_seq 升序排列的已提交可见消息。
          SyncedMessage
          投影为兼容响应结构的一条已提交消息。
          header `MessageHeader` — 固定消息行为标志。
          setting `integer` 0–255 WKProto 消息 Setting 位集合；按共享消息标志字典解释。
          message_id `integer:int64` — 数值型服务端消息 ID；JavaScript 消费者应优先使用 message_idstr，避免精度损失。
          message_idstr `string` — 不会丢失精度的服务端消息 ID 十进制字符串；恢复去重的首选键。
          client_msg_no `string` — 客户端生成的消息标识；作为次级去重键。
          message_seq `integer` 0–∞ 消息在 Channel 内的已提交序号；响应按此值升序排列。
          from_uid `string` — 消息发送者 UID。
          channel_id `string` — 可见的 Channel 标识；个人 Channel 响应会把 canonical ID 投影回对端 UID。
          channel_type `integer` 0–255 Wire Channel Type；黄金路径使用个人 Channel 值 1。
          topic `string` — 可选的兼容 Topic。
          expire `integer` 0–∞ 兼容响应中的消息过期值。
          timestamp `integer:int32` — 服务端消息时间戳。
          payload `string` encoding: base64 Wire Payload 字节的 Go JSON 编码。
          MessageHeader
          固定消息行为标志。
          no_persist `integer` enum: `0`, `1` 1 表示非持久消息。
          red_dot `integer` enum: `0`, `1` 1 表示请求未读/红点处理。
          sync_once `integer` enum: `0`, `1` 1 表示仅同步一次的消息。
          400 兼容错误信封
          ErrorEnvelope
          msg `string` — 供人阅读的兼容错误消息。
          status `integer` 400–599 与 HTTP 对齐的错误状态。
          {"msg":"invalid request","status":400}
          503 集群处于恢复维护栅栏后；请退避并观察 /readyz
          MaintenanceError
          error `string` const: `"maintenance"` 机器可读的恢复维护类别。
          message `string` const: `"restore maintenance is active"` 当前维护详情。
          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-beta-zh" operations={[{"path":"/channel/messagesync","method":"post"}]} showDescription />
    </>
  );
}
