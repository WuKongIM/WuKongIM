---
title: Acknowledge the latest command sync
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Acknowledge the latest command sync
        id: acknowledge-the-latest-command-sync
    contents:
      - content: Requires a positive last_message_seq but acknowledges the server's
          latest recorded generation, not that supplied value.
        heading: acknowledge-the-latest-command-sync
      - content: >-
          POST /message/syncack

          ackCommandMessages

          Acknowledge the latest command sync

          Requires a positive last_message_seq but acknowledges the server's
          latest recorded generation, not that supplied value.

          MessageSyncAckRequest

          Legacy command-message synchronization acknowledgement parameters.

          additionalProperties true

          uid `string` minLength: 1; pattern: `\S` Non-blank user ID
          acknowledging command-message synchronization.

          last_message_seq `integer:uint64` 1–∞ Compatibility field that must be
          positive; acknowledgement uses the latest server-recorded generation
          rather than this supplied value.

          200 Mutation accepted

          StatusEnvelope

          Legacy mutation-success response.

          additionalProperties false

          status `integer` const: `200` Application-level status fixed at 200.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Trusted backend only
- **Scope**: Acknowledgement consumes only the generation recorded by the latest sync in this process; last_message_seq is only validated as positive and is not used for acknowledgement.
- **Success**: If no local generation exists, the endpoint still returns 200 and acknowledges nothing.

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/message/syncack","method":"post"}]} showDescription />
    </>
  );
}
