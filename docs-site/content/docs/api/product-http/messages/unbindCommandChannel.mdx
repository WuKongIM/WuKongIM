---
title: 解绑命令 Channel 发现
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 解绑命令 Channel 发现
        id: 解绑命令-channel-发现
    contents:
      - content: 为有上限的接收者批次写入发现关系墓碑，不删除 CMD 消息。
        heading: 解绑命令-channel-发现
      - content: >-
          POST /message/cmd/unbind

          unbindCommandChannel

          解绑命令 Channel 发现

          为有上限的接收者批次写入发现关系墓碑，不删除 CMD 消息。

          MessageCMDBindingRequest

          命令 Channel 离线发现绑定参数。

          additionalProperties true

          uid `string` minLength: 1; pattern: `\S` 目标用户的唯一标识。

          channel_id `string` minLength: 1; pattern: `\S` 目标 Channel 的逻辑标识；个人
          Channel 场景通常填写对方 UID。

          channel_type `integer:uint8` 1–255 Channel 类型编号；1 表示个人 Channel，2 表示群组
          Channel，其他值属于扩展类型。

          uids array<`string`> minItems: 1; maxItems: 1000 源频道的接收者列表，最多 1000
          项；去空白和重复项。不能与 uid 或 subscribers 同用。

          subscribers array<`string`> minItems: 1; maxItems: 1000 仅使用此字段绑定 SEND
          的临时接收者范围；最多 1000 项，顺序必须与 SEND 一致。不能与频道字段同用。

          200 Mutation accepted

          StatusEnvelope

          旧式变更成功响应。

          additionalProperties false

          status `integer` const: `200` 固定为 200 的应用层状态。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受信业务后端
- **作用域**：使用与 bind 相同的三种互斥形式及 1000 项、256 KiB 上限；临时范围顺序须与原 SEND 相同。
- **成功判定**：跨 Slot 失败可能部分完成；重试整个批次。解绑不删除消息。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/message/cmd/unbind","method":"post"}]} showDescription />
    </>
  );
}
