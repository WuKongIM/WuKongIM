---
title: Append a message event
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Append a message event
        id: append-a-message-event
    contents:
      - content: Validates and applies one message-scoped event projection; non-null
          headers are unsupported.
        heading: append-a-message-event
      - content: >-
          POST /message/event

          appendMessageEvent

          Append a message event

          Validates and applies one message-scoped event projection; non-null
          headers are unsupported.

          AppendMessageEventRequest

          Message-event projection parameters.

          additionalProperties true

          channel_id `string` minLength: 1; pattern: `\S` Target non-blank
          Channel ID; person and Agent Channels are normalized by the use case.

          channel_type `integer:uint8` 1–255 Numeric Channel type; 1 is a person
          Channel, 2 is a group Channel, and other values are extension types.

          from_uid `string` — Event actor UID; person or unencoded Agent Channel
          normalization may require it.

          message_id `integer:int64` — Optional server message ID; positive
          values become uint64, while 0 or negative values are omitted from the
          event command.

          client_msg_no `string` minLength: 1; pattern: `\S` Non-blank client
          message number of the message receiving the event.

          event_id `string` minLength: 1; pattern: `\S` Non-blank unique
          identifier of this event.

          event_type `string` minLength: 1; pattern: `\S` Event type;
          surrounding whitespace is removed and the value is lowercased.

          event_key `string` — Event lane key; empty selects the default lane
          and stream.finish forces the finish lane.

          visibility `string` — Caller-supplied visibility marker; surrounding
          whitespace is removed.

          occurred_at `integer:int64` — Caller-supplied integer event time; the
          entry does not interpret its unit.

          payload `unknown` — Arbitrary JSON event payload passed to event
          storage as raw JSON bytes.

          headers `null` — Reserved field; it must be omitted or null, and every
          non-null JSON value is rejected.

          200 Applied event projection

          AppendMessageEventResponse

          additionalProperties false

          status `integer` const: `200`

          data `AppendMessageEventData` additionalProperties: `false`

          AppendMessageEventData

          client_msg_no `string` —

          event_key `string` —

          event_id `string` —

          msg_event_seq `integer:uint64` 0–∞

          stream_status `string` —

          channel_id `string` —

          channel_type `integer:uint8` 0–255

          from_uid `string` —

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/message/event","method":"post"}]} showDescription />
    </>
  );
}
