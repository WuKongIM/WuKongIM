---
title: 批量解析 UID 地址组
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: 批量解析 UID 地址组
        id: 批量解析-uid-地址组
    contents:
      - content: 在所选地址组中回显无上限 UID 数组；该接口仅为兼容保留。
        heading: 批量解析-uid-地址组
      - content: >-
          POST /route/batch

          getGatewayRoutesBatch

          批量解析 UID 地址组

          在所选地址组中回显无上限 UID 数组；该接口仅为兼容保留。

          node_id query optional `integer:uint64` 1–∞ 首选节点选择参数；填写时必须是已配置且大于 0
          的节点 ID。

          nodeId query optional `integer:uint64` 1–∞ 旧版 nodeId 别名；仅在未提供 node_id
          时读取。

          nodeID query optional `integer:uint64` 1–∞ 旧版 nodeID 别名；仅在 node_id 与
          nodeId 都未提供时读取。

          intranet query optional `integer` default: `0` 地址范围选择参数；任何可解析的非 0
          整数选择内网地址，无效文本按 0 处理并选择公网地址。

          RouteBatchRequest

          UID 字符串数组；JSON null 也会按空数组兼容接收，当前入口不限制数量。

          200 One selected address group

          RouteBatchResponse

          RouteBatchItem

          additionalProperties false

          tcp_addr `string` —

          ws_addr `string` —

          wss_addr `string` —

          uids array<`string`> —

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/route/batch","method":"post"}]} showDescription />
    </>
  );
}
