---
title: Resolve one address group for UIDs
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  deprecated: true
  toc:
    - depth: 2
      title: Query Parameters
      url: '#parameters-query'
    - depth: 2
      title: Request Body
      url: '#request-body'
    - depth: 2
      title: Response Body
      url: '#response-body'
  structuredData:
    headings:
      - content: Resolve one address group for UIDs
        id: resolve-one-address-group-for-uids
    contents:
      - content: Echoes an unbounded UID array inside the selected address group;
          retained for compatibility.
        heading: resolve-one-address-group-for-uids
      - content: >-
          POST /route/batch

          getGatewayRoutesBatch

          Resolve one address group for UIDs

          Echoes an unbounded UID array inside the selected address group;
          retained for compatibility.

          node_id query optional `integer:uint64` 1–∞ Preferred node selector;
          when supplied it must identify a configured node with an ID greater
          than 0.

          nodeId query optional `integer:uint64` 1–∞ Legacy nodeId alias, read
          only when node_id is absent.

          nodeID query optional `integer:uint64` 1–∞ Legacy nodeID alias, read
          only when both node_id and nodeId are absent.

          intranet query optional `integer` default: `0` Address-scope selector;
          any parseable non-zero integer selects intranet addresses, while
          invalid text behaves as 0 and selects public addresses.

          RouteBatchRequest

          Array of UID strings; JSON null is also accepted as an empty array,
          and the current entry does not bound its size.

          200 One selected address group

          RouteBatchResponse

          RouteBatchItem

          A group of UIDs sharing one Gateway address set.

          additionalProperties false

          tcp_addr `string` — WKProto TCP ingress address.

          ws_addr `string` — Non-TLS WebSocket ingress address.

          wss_addr `string` — TLS WebSocket ingress address.

          uids array<`string`> — UIDs assigned to this address set;
          compatibility responses may omit it.

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          HTTP 400 error body shared by multiple legacy entries.

          msg `string` — Human-readable error text with no stability guarantee;
          do not branch on it.

          status `integer` const: `400` Application-level status fixed at 400.

          503 Controller restore maintenance is active

          MaintenanceError

          503 error body returned while cluster restore maintenance is active.

          error `string` const: `"maintenance"` Stable maintenance error
          category.

          message `string` const: `"restore maintenance is active"` Current
          restore-maintenance message.
---

<Callout type="warn" title="Operation boundary">

- **Caller**: Trusted backend only

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/route/batch","method":"post"}]} showDescription />
    </>
  );
}
