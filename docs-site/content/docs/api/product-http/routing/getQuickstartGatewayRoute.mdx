---
title: 解析 Gateway 地址
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 解析 Gateway 地址
        id: 解析-gateway-地址
    contents:
      - content: 返回已配置的公网或内网地址；节点别名按 node_id、nodeId、nodeID 顺序读取。
        heading: 解析-gateway-地址
      - content: >-
          GET /route

          getQuickstartGatewayRoute

          解析 Gateway 地址

          返回已配置的公网或内网地址；节点别名按 node_id、nodeId、nodeID 顺序读取。

          node_id query optional `integer:uint64` 1–∞ 首选节点选择参数；填写时必须是已配置且大于 0
          的节点 ID。

          nodeId query optional `integer:uint64` 1–∞ 旧版 nodeId 别名；仅在未提供 node_id
          时读取。

          nodeID query optional `integer:uint64` 1–∞ 旧版 nodeID 别名；仅在 node_id 与
          nodeId 都未提供时读取。

          intranet query optional `integer` default: `0` 地址范围选择参数；任何可解析的非 0
          整数选择内网地址，无效文本按 0 处理并选择公网地址。

          200 Selected Gateway addresses

          RouteResponse

          additionalProperties false

          tcp_addr `string` —

          ws_addr `string` —

          wss_addr `string` —

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/route","method":"get"}]} showDescription />
    </>
  );
}
