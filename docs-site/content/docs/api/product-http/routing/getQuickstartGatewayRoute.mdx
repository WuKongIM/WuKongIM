---
title: 解析面向浏览器的 Gateway 地址
full: true
_openapi:
  preload:
    - wukongim-product-http-beta-zh
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 解析面向浏览器的 Gateway 地址
        id: 解析面向浏览器的-gateway-地址
    contents:
      - content: 返回已配置的 Gateway 地址；BFF 只转发浏览器 SDK 所需的 WebSocket 地址。
        heading: 解析面向浏览器的-gateway-地址
      - content: |-
          GET /route
          getQuickstartGatewayRoute
          解析面向浏览器的 Gateway 地址
          返回已配置的 Gateway 地址；BFF 只转发浏览器 SDK 所需的 WebSocket 地址。
          200 已配置的 Gateway 地址
          RouteResponse
          tcp_addr `string` — TCP 客户端接入地址；浏览器快速开始不使用它。
          ws_addr `string` — 仅在安全地址不可用且页面允许时使用的普通 WebSocket 入口。
          wss_addr `string` — 非空时由 HTTPS 浏览器应用优先使用的 TLS WebSocket 入口。
          {"tcp_addr":"127.0.0.1:5100","ws_addr":"ws://127.0.0.1:5200","wss_addr":""}
          400 兼容错误信封
          ErrorEnvelope
          msg `string` — 供人阅读的兼容错误消息。
          status `integer` 400–599 与 HTTP 对齐的错误状态。
          {"msg":"invalid request","status":400}
          503 集群处于恢复维护栅栏后；请退避并观察 /readyz
          MaintenanceError
          additionalProperties false
          error `string` const: `"maintenance"` 机器可读的恢复维护类别。
          message `string` const: `"restore maintenance is active"` 当前维护详情。
          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-beta-zh" operations={[{"path":"/route","method":"get"}]} showDescription />
    </>
  );
}
