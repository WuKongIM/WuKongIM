---
title: Resolve the browser-facing gateway addresses
full: true
_openapi:
  preload:
    - wukongim-product-http-beta-en
  method: GET
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Resolve the browser-facing gateway addresses
        id: resolve-the-browser-facing-gateway-addresses
    contents:
      - content: Returns configured Gateway addresses; the BFF forwards the WebSocket
          address required by the browser SDK.
        heading: resolve-the-browser-facing-gateway-addresses
      - content: |-
          GET /route
          getQuickstartGatewayRoute
          Resolve the browser-facing gateway addresses
          Returns configured Gateway addresses; the BFF forwards the WebSocket address required by the browser SDK.
          200 Configured gateway addresses
          RouteResponse
          tcp_addr `string` — TCP client-ingress address. The browser quickstart does not use it.
          ws_addr `string` — Plain WebSocket ingress used only when a secure address is unavailable and the page permits it.
          wss_addr `string` — TLS WebSocket ingress preferred by HTTPS browser applications when non-empty.
          {"tcp_addr":"127.0.0.1:5100","ws_addr":"ws://127.0.0.1:5200","wss_addr":""}
          400 Compatibility error envelope
          ErrorEnvelope
          msg `string` — Human-readable compatibility error message.
          status `integer` 400–599 HTTP-compatible error status.
          {"msg":"invalid request","status":400}
          503 The cluster is behind the restore-maintenance fence; back off and observe /readyz
          MaintenanceError
          additionalProperties false
          error `string` const: `"maintenance"` Machine-readable restore-maintenance category.
          message `string` const: `"restore maintenance is active"` Current maintenance detail.
          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-beta-en" operations={[{"path":"/route","method":"get"}]} showDescription />
    </>
  );
}
