---
title: Hide Conversation through the current head
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Hide Conversation through the current head
        id: hide-conversation-through-the-current-head
    contents:
      - content: Advances deleted_to_seq to the current committed head without deleting
          Channel membership.
        heading: hide-conversation-through-the-current-head
      - content: >-
          POST /conversations/delete

          hideConversation

          Hide Conversation through the current head

          Advances deleted_to_seq to the current committed head without deleting
          Channel membership.

          ConversationMutationRequest

          additionalProperties true

          uid `string` minLength: 1

          channel_id `string` minLength: 1

          channel_type `integer:uint8` 1–255

          200 Mutation accepted

          StatusEnvelope

          additionalProperties false

          status `integer` const: `200`

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-en" operations={[{"path":"/conversations/delete","method":"post"}]} showDescription />
    </>
  );
}
