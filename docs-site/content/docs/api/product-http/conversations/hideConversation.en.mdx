---
title: Hide Conversation through the current head
full: true
_openapi:
  preload:
    - wukongim-product-http-management-beta-en
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: Hide Conversation through the current head
        id: hide-conversation-through-the-current-head
    contents:
      - content: Hides this Conversation through the current committed head. It does not
          delete the Channel or membership.
        heading: hide-conversation-through-the-current-head
      - content: >-
          POST /conversations/delete

          hideConversation

          Hide Conversation through the current head

          Hides this Conversation through the current committed head. It does
          not delete the Channel or membership.

          ConversationMutationRequest

          additionalProperties false

          uid `UID` minLength: 1 Non-empty product user identity.

          channel_id `string` minLength: 1 Visible Channel ID. For Channel Type
          1, use the peer UID.

          channel_type `integer` 1–255 Channel Type.

          UID

          Non-empty product user identity.

          200 Mutation completed

          MutationStatus

          status `integer` const: `200` Compatibility success status, always
          200.

          {"status":200}

          400 Malformed input, failed validation, unavailable routing, or a
          use-case/storage error

          CompatibilityError

          msg `string` — Human-readable runtime message; exact text is not a
          stable machine contract.

          status `integer` const: `400` Compatibility business-error status,
          always 400 on these routes.

          {"msg":"invalid request","status":400}

          503 Restore maintenance is active; back off and observe /readyz

          MaintenanceError

          error `string` const: `"maintenance"` Machine-readable
          restore-maintenance category.

          message `string` const: `"restore maintenance is active"` Current
          maintenance detail.

          {"error":"maintenance","message":"restore maintenance is active"}
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-management-beta-en" operations={[{"path":"/conversations/delete","method":"post"}]} showDescription />
    </>
  );
}
