---
title: 同步一页会话
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 同步一页会话
        id: 同步一页会话
    contents:
      - content: 返回一页有界 membership；只有 done=true 才表示本轮完成。
        heading: 同步一页会话
      - content: >-
          POST /conversation/list

          listConversations

          同步一页会话

          返回一页有界 membership；只有 done=true 才表示本轮完成。

          ConversationListRequest

          会话目录分页参数。

          additionalProperties true

          uid `string` minLength: 1 要读取会话目录的用户 ID。

          cursor `string` default: `""` 上页返回的不透明 next_cursor；首轮请求省略或传空字符串。

          limit `integer` 0–200; default: `50` 本页最多扫描的 membership 条目数；0 使用默认值
          50，最大 200。

          completed_coverage `integer:int64` 0–∞; default: `0` 客户端最近一次 done=true
          完整轮次保存的 coverage；首次同步传 0。

          200 Canonical Conversation page

          ConversationListResponse

          规范会话目录的一页及其同步控制状态。

          additionalProperties false

          conversations array<`ConversationListItem`> — 本页已解析的会话。

          deletes array<`ConversationKey`> — 客户端应删除的会话 Key。

          unresolved array<`ConversationKey`> — 本页暂未解析的 Channel Key，可交给
          /conversation/retry 有界重试。

          next_cursor `string` — 下一页不透明游标；必须原样回传。

          done `boolean` — true 表示本轮完整扫描结束，此时才能保存 coverage。

          coverage `integer:int64` 0–∞ 服务端为本轮同步观察到的覆盖版本。

          tombstones_retained_since `integer:int64` 0–∞ 服务端仍保留删除墓碑的最早 coverage。

          reset_required `boolean` — true 表示客户端覆盖已落后于保留窗口，必须重建本地会话目录。

          ConversationListItem

          一个用户的规范会话状态。

          channel_id `string` — 会话 Channel ID。

          channel_type `integer` 1–255 会话 Channel 类型。

          active_at `integer:int64` — 会话最近激活时间。

          read_seq `integer:uint64` 0–∞ 用户已读到的最大消息序号。

          deleted_to_seq `integer:uint64` 0–∞ 用户已删除到的最大消息序号。

          unread `integer:uint64` 0–∞ 基于当前末条消息和 read_seq 计算的未读数。

          last_message `ConversationLastMessage` | `null` — 当前可见的末条消息；没有时为 null。

          ConversationLastMessage

          会话目录中的末条消息摘要。

          message_id `integer:uint64` 0–∞ 服务端 int64 消息 ID。

          message_idstr `string` — 仅 message_id 的十进制字符串镜像。

          message_seq `integer:uint64` 0–∞ 该消息在 Channel 内的序号。

          from_uid `string` — 消息发送者 UID。

          client_msg_no `string` — 客户端消息编号。

          server_timestamp_ms `integer:int64` — 服务端毫秒时间戳。

          payload `string | null` encoding: base64 Base64 编码消息 Payload；可能为 null。

          ConversationKey

          一个会话的 Channel Key。

          channel_id `string` minLength: 1 目标 Channel 的逻辑标识；个人 Channel 场景通常填写对方
          UID。

          channel_type `integer` 1–255 Channel 类型编号；1 表示个人 Channel，2 表示群组
          Channel，其他值属于扩展类型。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受信业务后端
- **作用域**：limit 限制本页扫描的 membership 条目数，不保证 conversations 数组达到该数量；墓碑进入 deletes，暂时无法补水的条目进入 unresolved。
- **成功判定**：只以 done=true 结束完整轮次并保存 coverage；reset_required=true 时必须重建本地会话目录。

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/conversation/list","method":"post"}]} showDescription />
    </>
  );
}
