---
title: 重试未解析会话
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc: []
  structuredData:
    headings:
      - content: 重试未解析会话
        id: 重试未解析会话
    contents:
      - content: 重新 hydrate 最多 200 个 Key，不回退已完成 coverage；重复 Key 会合并。
        heading: 重试未解析会话
      - content: |-
          POST /conversation/retry
          retryConversations
          重试未解析会话
          重新 hydrate 最多 200 个 Key，不回退已完成 coverage；重复 Key 会合并。
          ConversationRetryRequest
          additionalProperties true
          uid `string` minLength: 1
          channels array<`ConversationKey`> minItems: 1; maxItems: 200
          ConversationKey
          channel_id `string` minLength: 1
          channel_type `integer` 1–255
          200 Canonical Conversation page
          ConversationListResponse
          additionalProperties false
          conversations array<`ConversationListItem`> —
          deletes array<`ConversationKey`> —
          unresolved array<`ConversationKey`> —
          next_cursor `string` —
          done `boolean` —
          coverage `integer:int64` 0–∞
          tombstones_retained_since `integer:int64` 0–∞
          reset_required `boolean` —
          ConversationListItem
          channel_id `string` —
          active_at `integer:int64` —
          read_seq `integer:uint64` 0–∞
          deleted_to_seq `integer:uint64` 0–∞
          unread `integer:uint64` 0–∞
          last_message `ConversationLastMessage` | `null` —
          ConversationLastMessage
          message_id `integer:uint64` 0–∞
          message_idstr `string` —
          message_seq `integer:uint64` 0–∞
          from_uid `string` —
          client_msg_no `string` —
          server_timestamp_ms `integer:int64` —
          payload `string | null` encoding: base64
          400 Input, validation, dependency, or downstream error
          CompatibilityError
          msg `string` — Human-readable and not stable for machine matching.
          status `integer` const: `400`
          503 Controller restore maintenance is active
          MaintenanceError
          error `string` const: `"maintenance"`
          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/conversation/retry","method":"post"}]} showDescription />
    </>
  );
}
