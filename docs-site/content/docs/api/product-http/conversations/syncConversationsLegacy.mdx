---
title: 同步旧式会话
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  deprecated: true
  toc: []
  structuredData:
    headings:
      - content: 同步旧式会话
        id: 同步旧式会话
    contents:
      - content: 解析分隔式逐 Channel 游标并返回没有完成标记的旧式裸数组。
        heading: 同步旧式会话
      - content: >-
          POST /conversation/sync

          syncConversationsLegacy

          同步旧式会话

          解析分隔式逐 Channel 游标并返回没有完成标记的旧式裸数组。

          ConversationSyncLegacyRequest

          v2.2 兼容会话同步参数。

          additionalProperties true

          uid `string` minLength: 1; pattern: `\S` 要同步旧式会话的非空白用户 ID。

          version `integer:int64` default: `0` 客户端已知的会话版本；0 表示不按版本过滤。

          last_msg_seqs `string` default: `""` 以 | 分隔的
          channel_id:channel_type:message_seq 游标；格式错误的条目会被跳过。

          msg_count `integer` default: `0` 每个会话读取的最近消息数；小于等于 0 返回空数组，正数最大按 10000
          处理。

          only_unread `integer:uint8` 0–255; default: `0` 未读过滤兼容标志；仅 1
          表示只读取未读会话。

          exclude_channel_types `array | null` — 要排除的 Channel 类型数组；可省略、为空或为
          null；显式 Channel 游标优先于该过滤。

          page `integer` default: `0` 旧式页码；小于等于 0 时禁用旧式分页切片。

          page_size `integer` default: `100` 旧式每页数量；page>0 时，小于等于 0 使用 100，大于
          500 截断为 500。

          200 Bare legacy Conversation array

          ConversationSyncLegacyItem

          additionalProperties false

          channel_id `string` —

          channel_type `integer:uint8` 0–255

          unread `integer:uint64` 0–∞

          timestamp `integer:int64` —

          last_msg_seq `integer:uint64` 0–∞

          last_client_msg_no `string` —

          offset_msg_seq `integer:int64` —

          readed_to_msg_seq `integer:uint64` 0–∞

          version `integer:int64` —

          recents array<`LegacyMessage`> —

          LegacyMessage

          header `LegacyMessageHeader` additionalProperties: `false`

          setting `integer:uint8` 0–255

          message_id `integer:int64` —

          message_idstr `string` —

          client_msg_no `string` —

          end `integer:uint8` 0–255

          end_reason `integer:uint8` 0–255

          error `string` —

          stream_data `string | null` encoding: base64

          event_meta `LegacyMessageEventMeta` additionalProperties: `false`

          event_sync_hint `LegacyMessageEventSyncHint` additionalProperties:
          `false`

          message_seq `integer:uint64` 0–∞

          from_uid `string` —

          topic `string` —

          expire `integer:uint32` 0–∞

          timestamp `integer:int32` —

          payload `string | null` encoding: base64

          LegacyMessageHeader

          no_persist `integer` enum: `0`, `1`

          red_dot `integer` enum: `0`, `1`

          sync_once `integer` enum: `0`, `1`

          LegacyMessageEventMeta

          has_events `boolean` —

          completed `boolean` —

          event_version `integer:uint64` 0–∞

          last_msg_event_seq `integer:uint64` 0–∞

          event_count `integer` 0–∞

          open_event_count `integer` 0–∞

          events array<`LegacyMessageEventKeyMeta`> —

          LegacyMessageEventKeyMeta

          event_key `string` —

          status `string` —

          snapshot `unknown` —

          LegacyMessageEventSyncHint

          from_msg_event_seq `integer:uint64` 0–∞

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          msg `string` — Human-readable and not stable for machine matching.

          status `integer` const: `400`

          503 Controller restore maintenance is active

          MaintenanceError

          error `string` const: `"maintenance"`

          message `string` const: `"restore maintenance is active"`
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/conversation/sync","method":"post"}]} showDescription />
    </>
  );
}
