---
title: 设置会话最大未读数
full: true
_openapi:
  preload:
    - wukongim-product-http-complete-beta-zh
  method: POST
  webhook: false
  toc:
    - depth: 2
      title: 请求体
      url: '#request-body'
    - depth: 2
      title: 响应体
      url: '#response-body'
  structuredData:
    headings:
      - content: 设置会话最大未读数
        id: 设置会话最大未读数
    contents:
      - content: 单调推进 read_seq，使剩余未读消息不超过 unread。
        heading: 设置会话最大未读数
      - content: >-
          POST /conversations/setUnread

          setConversationUnread

          设置会话最大未读数

          单调推进 read_seq，使剩余未读消息不超过 unread。

          ConversationSetUnreadRequest

          设置会话最大未读数的参数。

          additionalProperties true

          uid `string` minLength: 1 目标用户的唯一标识。

          channel_id `string` minLength: 1 目标 Channel 的逻辑标识；个人 Channel 场景通常填写对方
          UID。

          channel_type `integer:uint8` 1–255 Channel 类型编号；1 表示个人 Channel，2 表示群组
          Channel，其他值属于扩展类型。

          unread `integer` 0–∞ 希望最多保留的未读消息数量；必须大于等于 0，服务端只会单调推进 read_seq。

          200 Mutation accepted

          StatusEnvelope

          旧式变更成功响应。

          additionalProperties false

          status `integer` const: `200` 固定为 200 的应用层状态。

          400 Input, validation, dependency, or downstream error

          CompatibilityError

          多个旧式入口共用的 HTTP 400 错误体。

          msg `string` — 供人阅读且不保证稳定的错误文本；不要据此做机器分支。

          status `integer` const: `400` 固定为 400 的应用层状态。

          503 Controller restore maintenance is active

          MaintenanceError

          集群处于恢复维护期间的 503 错误体。

          error `string` const: `"maintenance"` 稳定的 maintenance 错误类别。

          message `string` const: `"restore maintenance is active"` 当前恢复维护说明。
---

<Callout type="warn" title="接口边界">

- **调用方**：仅限受信业务后端

</Callout>

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

export default function Layout(props) {
  const { APIPage, OpenAPIPage } = props.components ?? {};
  // "APIPage" is the old name from v10, this allows both for backward compatibility
  const Comp = OpenAPIPage ?? APIPage;
  return (
    <>
      {props.children}
      <Comp document="wukongim-product-http-complete-beta-zh" operations={[{"path":"/conversations/setUnread","method":"post"}]} showDescription />
    </>
  );
}
