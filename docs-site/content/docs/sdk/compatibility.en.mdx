---
title: Versions & Compatibility
description: Check the server, SDK, Node.js, and browser compatibility target plus its exact receipt status.
---

This page records the combination targeted by the **JavaScript/Web golden path**. Only the shared receipt status can attest that the exact build tuple completed the scenario. This is not a compatibility matrix for every client SDK, and it does not imply that the current source has a final v3 release.

<Callout type="info" title="Complete references are published without broadening the receipt">
  Platform-capability, API-reference, and upgrade chapters for Android, iOS, Flutter, and HarmonyOS, plus the JavaScript [API Reference](/en/sdk/javascript/api-reference) and [Upgrade Guide](/en/sdk/javascript/upgrade), are now published. They are pinned source/artifact references; publication adds no platform, device, API, or production-readiness claim to this receipt.
</Callout>

<Callout type="warn" title="EasySDK tutorials are published without a runtime receipt">
  The [EasySDK iOS, Android, Flutter, and Web tutorials](/en/sdk/easy) are published as source-aligned material and appear in search and the sitemap; they remain outside this receipt. EasySDK uses WebSocket JSON-RPC, and the current Product Gateway does not support JSON-RPC CONNECT. Actual integration still requires a runtime fix and end-to-end verification; tutorial publication and source alignment are not executable compatibility.
</Callout>

<Callout type="info" title="The iOS tutorial does not broaden this compatibility snapshot">
  The [full iOS tutorial](/en/sdk/ios) pins `WuKongIMSDK` `1.1.1` and aligns source, CocoaPods metadata, and distributed public headers. It **is not covered by this receipt**. Until an exact Xcode/device message scenario and attestation exist, the JavaScript/Web `verified` state cannot be projected onto iOS.
</Callout>

<Callout type="info" title="The Android tutorial also does not broaden this compatibility snapshot">
  The [full Android tutorial](/en/sdk/android) pins `WuKongIMAndroidSDK` `1.5.5`. Its source, JitPack build revision, AAR SHA, and public signatures are aligned, but the path **is not covered by this receipt**. A user-side “verified” result can become a separate Android attestation only after an exact AGP/JDK, build variant, device/system, server revision, and Alice/Bob result are submitted and reviewed.
</Callout>

<Callout type="info" title="The Flutter tutorial also has no runtime receipt">
  The [full Flutter tutorial](/en/sdk/flutter) pins pub.dev `wukongimfluttersdk` `1.7.9` and aligns its archive hash, matching source revision, public Dart API, static analysis, and macOS Release build. It **is not covered by this receipt** and has no site Android, iOS, or macOS runtime receipt. A separate Flutter attestation requires an exact `pubspec.lock`, Flutter/Dart/native toolchain, device/system, server revision, and Alice/Bob result to be submitted and reviewed.
</Callout>

<Callout type="info" title="The HarmonyOS tutorial also has no install, compile, or runtime receipt">
  The [full HarmonyOS tutorial](/en/sdk/harmonyos) pins OHPM `@wukong/wkim` `1.1.7` and aligns its HAR SHA, matching source revision, and packaged ArkTS API. It **is not covered by this receipt** and has no site HarmonyOS install, compile, or device runtime receipt. A separate attestation requires an exact `oh-package-lock.json5`, DevEco/HarmonyOS SDK/ohpm/hvigor tuple, HAP/signing result, device/system, server revision, and Alice/Bob result to be submitted and reviewed.
</Callout>

<Callout type="info" title="UniApp migration is also outside the browser receipt">
  The old `WuKongIMUniappSDK` repository and `wukongimuniappsdk` are deprecated. The [UniApp retirement and migration guide](/en/sdk/uniapp) pins the target to `wukongimjssdk@1.3.5`. That package checks global `uni.connectSocket`, then `wx.connectSocket`, and otherwise uses native `WebSocket`, but this site's Chromium scenario proves only the browser path—not App, H5, or mini-program targets under HBuilderX. Retain separate build and Alice/Bob runtime evidence for each target.
</Callout>

## Goal and completion criteria

Before integration, confirm that your server revision, `wukongimjssdk`, Node.js, and browser belong to one compatibility snapshot. When finished, you should be able to classify the combination as verified, unverified, or drifted instead of guessing from `latest`.

## Compatibility snapshot

<CompatibilitySnapshot locale="en" />

Without an attestation file, the shared snapshot reports `verified: false` and `verification.status: "missing"`. Only a successful receipt whose complete source, lockfile, scenario, SDK, Node.js, Playwright, and Chromium tuple exactly matches this build can report `verified: true`. The target values below are not evidence by themselves; malformed or drifted receipts stay unverified.

| Component | Current compatibility target |
| --- | --- |
| WuKongIM server | Source revision injected at build time; runs full cluster semantics |
| JavaScript SDK | Exactly `wukongimjssdk@1.3.5` |
| Golden sample | `docs-site/examples/javascript-web-quickstart/` |
| Node.js | Receipt target `22.12.0`; the sample declares `20.11` as its minimum |
| Browser | Receipt target `@playwright/test@1.62.1`, Chromium revision `1234` / `151.0.7922.34` |

The static site also publishes `/compatibility.json`. The page, sample, and CI must consume the same facts. The build injects the source revision so prose never carries a stale handwritten SHA.

```bash
curl --fail https://docs.githubim.com/compatibility.json
```

## Browser scope

| Engine | Status |
| --- | --- |
| Chromium `151.0.7922.34` (revision `1234`) | The only blocking smoke target; the `verified` attestation above decides whether this build carries complete evidence |
| Firefox | Not verified by the current snapshot |
| WebKit / Safari | Not verified by the current snapshot |

“Not verified” does not mean “known to fail,” but it is not a support guarantee either. Do not summarize this page as support for all modern browsers.

## Prerequisites

- Access to the current build's `/compatibility.json`;
- ability to check out the server revision recorded by the manifest;
- the sample's committed npm lockfile instead of rolling dependencies;
- a policy that marks only combinations carrying complete smoke evidence as verified.

## Use the snapshot

1. Read the server revision and exact SDK version from `compatibility.json`.
2. Check out that server source instead of a moving `main` or SDK `latest`.
3. Run `npm ci` in the golden-sample directory and preserve its lockfile.
4. Run the sample checks, then complete the real single-node cluster smoke scenario in the [Quickstart](/en/sdk/javascript/quickstart).
5. After any component changes, rerun the entire scenario and let the integration gate emit a new exact receipt; do not mark the combination manually.

## Expected result

The same snapshot should repeatedly complete connection, persistent two-way messages between Alice and Bob, Bob's disconnect, reconnect, and offline synchronization. This page promises only that narrow scenario—not the complete SDK API, background behavior, push, custom messages, or every browser.

## Troubleshooting

- **The SDK is not `1.3.5`:** remove the manual upgrade and rerun `npm ci`.
- **The server revision differs:** treat the combination as unverified; a successful CONNECT does not prove every behavior is compatible.
- **Only one browser fails:** record the exact engine and version. Chromium evidence cannot be projected onto Firefox or WebKit/Safari.
- **The JSON and page disagree:** this is a publication error that compatibility validation should reject; do not choose one manually.

## Security and responsibility boundary

Compatibility verification does not complete production authentication. The default v3 Beta Gateway does not automatically validate CONNECT against stored tokens, and Product HTTP has no general business-authentication middleware. Matching versions do not replace product identity, TLS, access-control, capacity, or failure-recovery validation.

## Next step

For this receipt, prepare the pinned Web dependency in [Installation](/en/sdk/javascript/installation), then complete the [JavaScript/Web Quickstart](/en/sdk/javascript/quickstart). For Android, iOS, Flutter, or HarmonyOS, use the corresponding [Android](/en/sdk/android), [iOS](/en/sdk/ios), [Flutter](/en/sdk/flutter), or [HarmonyOS](/en/sdk/harmonyos) tutorial and retain separate evidence for your exact runtime tuple. Existing UniApp projects should start with the [retirement and migration guide](/en/sdk/uniapp) and must not reuse this Chromium receipt. See [API Compatibility](/en/api/compatibility) for the HTTP-subset boundary.
