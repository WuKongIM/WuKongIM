---
title: SDK Tutorials
description: Choose a client SDK, prove the first message, then add identity, reconnect, offline, conversation, and release behavior.
---

A WuKongIM SDK manages the connection, message exchange, and protocol state between a client and the Gateway. You do not need to learn the wire protocol first. Start from your platform and outcome, prove a minimal loop, and then add production behavior.

<Callout type="info" title="Start your first integration here">
  If you have not selected a repository, begin with [Choose an SDK](/en/sdk/choose-sdk). Android `1.5.5`, iOS `1.1.1`, Flutter `1.7.9`, and HarmonyOS `1.1.7` now have full-SDK tutorials aligned to source/distribution artifacts, but none has a corresponding platform runtime receipt on this site. For a browser path carrying this site's executable scenario evidence, begin with [WuKongIMJSSDK Installation](/en/sdk/javascript/installation). The old UniApp SDK is deprecated; existing projects should use the [UniApp migration guide](/en/sdk/uniapp) to evaluate migration to `wukongimjssdk@1.3.5`. WuKongEasySDK tutorials remain planned because the current Product Gateway does not support their JSON-RPC CONNECT path.
</Callout>

## What do you want to accomplish now?

<Cards>
  <Card title="Choose the right SDK" description="Find candidates by platform, then inspect each tag's capabilities, requirements, and verification status." href="/en/sdk/choose-sdk" />
  <Card title="Evaluate EasySDK source" description="Confirm the JSON-RPC CONNECT blocker first; the source examples are not a current integration path." href="/en/sdk/choose-sdk" />
  <Card title="Send the first Android message" description="Pin 1.5.5 and prove the conversation-sync gate, local insert, SENDACK, and online receipt." href="/en/sdk/android" />
  <Card title="Send the first iOS message" description="Pin 1.1.1 and use its public Objective-C API for connection, send acknowledgement, and online receipt." href="/en/sdk/ios" />
  <Card title="Send the first Flutter message" description="Pin 1.7.9 and use the real Dart API for the sync gate, SENDACK, and online receipt." href="/en/sdk/flutter" />
  <Card title="Send the first HarmonyOS message" description="Pin OHPM 1.1.7 and use the real ArkTS API for the sync gate, SENDACK, and online receipt." href="/en/sdk/harmonyos" />
  <Card title="Migrate a UniApp project" description="Remove the deprecated standalone package, pin JSSDK 1.3.5, and accept App, H5, and mini-program targets separately." href="/en/sdk/uniapp" />
  <Card title="Send the first Web message" description="Install a pinned version, connect Alice and Bob, and prove online messaging plus offline recovery." href="/en/sdk/javascript/quickstart" />
  <Card title="Design a complete client" description="Implement identity, connection, messaging, conversations, push, multi-device, and failure behavior." href="/en/sdk/common-guides" />
  <Card title="Confirm version evidence" description="Check whether server, SDK, Node.js, and Chromium belong to one compatibility snapshot." href="/en/sdk/compatibility" />
</Cards>

## What a complete integration looks like

1. **Choose and pin an SDK:** confirm the platform, required capabilities, and exact version instead of guessing from `latest`.
2. **Prepare product identity:** the user signs in to your product first; a trusted backend then returns the UID, short-lived token, and connection material.
3. **Initialize and register listeners:** configure the current user and attach connection, message, and send-status listeners before connecting.
4. **Connect and recover state:** wait for a real SDK connection; after reconnect, synchronize and deduplicate missing messages before marking the UI ready.
5. **Send and receive messages:** handle send acknowledgement, online receipt, and product processing as distinct outcomes.
6. **Complete the product experience:** add custom messages, conversations and unread state, mobile push, and multi-device policy as required.
7. **Finish release acceptance:** exercise failure and recovery paths, then separately close authentication, TLS, rate-limit, capacity, audit, and rollback gates.

Class names and callback shapes differ by platform, but these responsibilities and acceptance questions remain. The [SDK Common Guides](/en/sdk/common-guides) explain shared behavior in this order; each platform repository owns its exact API.

## Current tutorial coverage

| Content | Current status | Best for |
| --- | --- | --- |
| [Choose an SDK](/en/sdk/choose-sdk) and official source directory | Published | Developers still comparing platforms or SDK families |
| WuKongEasySDK for iOS, Android, Flutter, and Web | Planned; source tags recorded, but JSON-RPC CONNECT is unsupported by the current Gateway | Source evaluation only |
| [WuKongIMSDK Android](/en/sdk/android) `1.5.5` | Overview, installation, and quickstart published; source and JitPack AAR are aligned, with no site Android runtime receipt | Android teams retaining acceptance evidence on their own AGP/device/server tuple |
| [WuKongIMSDK iOS](/en/sdk/ios) `1.1.1` | Overview, installation, and quickstart published; source and public headers are aligned, with no site runtime receipt | Native iOS teams retaining acceptance evidence on their own Xcode/device tuple |
| [WuKongIMSDK Flutter](/en/sdk/flutter) `1.7.9` | Overview, installation, and quickstart published; pub.dev archive and matching source aligned, with consumer analyze/macOS Release build evidence but no site runtime receipt | Cross-platform teams retaining acceptance evidence on their own Flutter/Dart, device, and server tuple |
| [WuKongIMSDK HarmonyOS](/en/sdk/harmonyos) `1.1.7` | Overview, installation, and quickstart published; OHPM HAR and matching source aligned, with no site install, compile, or device runtime receipt | HarmonyOS NEXT teams retaining acceptance evidence on their own DevEco/device/server tuple |
| [JavaScript / Web](/en/sdk/javascript) | Published with a scenario-covered executable sample | Developers proving the first browser client loop |
| [Common Guides](/en/sdk/common-guides) | Published and platform-neutral | Teams designing a production client lifecycle |
| [WuKongIMSDK UniApp migration](/en/sdk/uniapp) | The old repository and `wukongimuniappsdk` are deprecated; the migration target is exactly `wukongimjssdk@1.3.5` | Existing projects that must remove the old package and retain separate App, H5, and mini-program acceptance evidence |
| Complete JavaScript API reference and upgrade guide | Planned | Stay within the tutorial-covered APIs and pinned compatibility snapshot |

<Callout type="warn" title="Do not confuse three statuses">
  “Official source exists” means a repository can be evaluated. “A tutorial is published” means this site exposes the content. “Verified” means one exact version tuple passed an executable scenario. This site's executable client evidence currently covers only the JavaScript/Web path with `wukongimjssdk@1.3.5`.
</Callout>

## What the Web tutorial proves

After [Installation](/en/sdk/javascript/installation) and the [Quickstart](/en/sdk/javascript/quickstart), you will observe:

- Alice and Bob each complete a connection;
- Alice receives SENDACK after sending, while Bob separately receives the realtime message;
- Alice can still commit a persistent message while Bob is disconnected;
- Bob recovers that message through synchronization after reconnect, without labeling recovery as realtime receipt.

This is more useful than a single “send succeeded” result because durable commit, online delivery, and offline recovery are genuinely different outcomes.

## Common questions

- **Which repository should I choose?** List the platform, minimum runtime, and required capabilities, then use [Choose an SDK](/en/sdk/choose-sdk) to compare them.
- **I need only connection and online messages:** do not select EasySDK as a current v3 integration path. Read the [JSON-RPC support matrix](/en/api/client-protocols/json-rpc), or use the JavaScript/Web path with executable evidence.
- **I need local messages, conversations, and unread counts on iOS:** begin with the [iOS installation and quickstart](/en/sdk/ios) for the `1.1.1` online path, then use the Common Guides. Offline and conversation recovery do not yet have site runtime evidence.
- **I need local messages, conversations, and unread counts on Android:** begin with the [Android installation and quickstart](/en/sdk/android) for the `1.5.5` synchronization gate and online path. Source/AAR alignment is not a device runtime receipt.
- **I need local messages, conversations, and unread counts on Flutter:** begin with the [Flutter installation and quickstart](/en/sdk/flutter) for the `1.7.9` singleton, synchronization, and send path. A macOS build is not an Android/iOS/macOS runtime receipt.
- **I need local messages, conversations, and unread counts on HarmonyOS:** begin with the [HarmonyOS installation and quickstart](/en/sdk/harmonyos) for the `1.1.7` HAR, deep-path API, synchronization, and send path. Source/HAR alignment is not a DevEco or device runtime receipt.
- **How should I integrate UniApp?** The official `WuKongIMUniappSDK` repository is deprecated and points to the JavaScript SDK. Follow the [UniApp migration guide](/en/sdk/uniapp) to remove the old package and pin `wukongimjssdk@1.3.5`, then validate each target runtime independently instead of projecting browser support.
- **The Web sample builds but does not connect:** follow the Quickstart checks for cluster `/readyz`, route address, Gateway port, and BFF output.
- **Sending succeeds but the peer shows nothing:** separate SENDACK, online receipt, and synchronized recovery, then diagnose each path.

## Security and responsibility boundary

An SDK does not own product accounts, authorization, or token issuance. A client must not receive WuKongIM Product HTTP management capability; those APIs belong behind a trusted product backend. The default v3 Beta Gateway also does not automatically turn stored tokens into production-grade identity verification. Android `1.5.5`, iOS `1.1.1`, Flutter `1.7.9`, and HarmonyOS `1.1.7` all carry raw-TCP, local-data/credential, queue-isolation, or Payload-logging blockers. Add authentication, TLS, network isolation, log redaction, local-data protection, rate limits, audit, and revocation before release.

## Next step

Start with [Choose an SDK](/en/sdk/choose-sdk). For Android, iOS, Flutter, or HarmonyOS, use the corresponding [Android](/en/sdk/android), [iOS](/en/sdk/ios), [Flutter](/en/sdk/flutter), or [HarmonyOS](/en/sdk/harmonyos) tutorial to pin `1.5.5`, `1.1.1`, `1.7.9`, or `1.1.7`, then retain runtime evidence in your own environment. Existing UniApp projects should begin with the [retirement and migration entry](/en/sdk/uniapp). For this site's existing executable receipt, complete Web [Installation](/en/sdk/javascript/installation) and the [Quickstart](/en/sdk/javascript/quickstart). EasySDK must still wait for a JSON-RPC runtime fix and end-to-end verification.
