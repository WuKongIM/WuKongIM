---
title: Choose an SDK
description: Choose a WuKongIM SDK by platform, capability needs, and verification status, then find its source and tutorial entry point.
---

Choose by runtime and required capabilities before choosing by repository name. The legacy documentation grouped clients into EasySDK and WuKongIMSDK families. This page preserves that useful discovery path, but no longer treats “chat app or non-chat app” as the only decision.

<Callout type="warn" title="Source availability is not verification">
  An official repository, a tutorial on this site, and an executable compatibility receipt are three different things. Android `1.5.5`, iOS `1.1.1`, Flutter `1.7.9`, and HarmonyOS `1.1.7` are aligned to source and their corresponding distribution artifacts, but none of those tutorials has a site runtime receipt. Only the JavaScript/Web golden path with `wukongimjssdk@1.3.5` currently has an executable verification entry point on this site.
</Callout>

## Write requirements before choosing a family

Family names are only discovery labels. They do not establish the capabilities of any repository or version. Prepare the following information, then inspect every candidate in the platform table individually:

| Question to answer first | What to verify |
| --- | --- |
| Where will the application run? | Browser, native system, or cross-platform framework, plus minimum runtime and build-tool versions |
| What must the first release do? | Required connection, messaging, conversations, unread state, offline recovery, push, multi-device, and custom-message behavior |
| Which capabilities does the product own? | Accounts, contacts, policy, business data, push devices, receipts, and UI state |
| What would prove adoption is safe? | The candidate repository's exact tag, installable artifact, included example, and your minimal acceptance result |

Inspect the README, APIs, examples, and changelog for the exact tag of each candidate before deciding. If your target is a browser and you want to run this site's existing sample first, use the WuKongIMJSSDK path marked as published below. That is the current tutorial entry, not a ranking of SDK families.

## Find the entry point for your platform

| Application platform | Official source to evaluate | Tutorial status on this site |
| --- | --- | --- |
| Browser / Web | [WuKongIMJSSDK](https://github.com/WuKongIM/WuKongIMJSSDK), [WuKongEasySDK-JS](https://github.com/WuKongIM/WuKongEasySDK-JS) | The [JavaScript/Web tutorial](/en/sdk/javascript) has a complete browser snapshot. Pinned EasySDK Web v2.0.2 has a server wire receipt for JSON-RPC CONNECT and online bidirectional messaging; the browser artifact still requires deployment-owned execution evidence |
| iOS / Apple platforms | [WuKongIMiOSSDK](https://github.com/WuKongIM/WuKongIMiOSSDK), [WuKongEasySDK-iOS](https://github.com/WuKongIM/WuKongEasySDK-iOS) | The full-SDK tutorial is published for `1.1.1`. Pinned EasySDK iOS v1.0.3 has a server wire receipt for JSON-RPC CONNECT and online bidirectional messaging; both still require iOS build and device evidence |
| Android | [WuKongIMAndroidSDK](https://github.com/WuKongIM/WuKongIMAndroidSDK), [WuKongEasySDK-Android](https://github.com/WuKongIM/WuKongEasySDK-Android) | The full-SDK tutorial is published for `1.5.5`. Pinned EasySDK Android v1.0.3 has a server wire receipt for JSON-RPC CONNECT and online bidirectional messaging; both still require Android build and device evidence |
| Flutter | [WuKongIMFlutterSDK](https://github.com/WuKongIM/WuKongIMFlutterSDK), [WuKongEasySDK-Flutter](https://github.com/WuKongIM/WuKongEasySDK-Flutter) | The full-SDK tutorial is published for `1.7.9`. Pinned EasySDK Flutter v1.0.4 has a server wire receipt for JSON-RPC CONNECT and online bidirectional messaging; both still require Flutter build and device evidence |
| HarmonyOS | [WuKongIMHarmonyOSSDK](https://github.com/WuKongIM/WuKongIMHarmonyOSSDK) | The full-SDK [entry](/en/sdk/harmonyos), installation, quickstart, [Platform Capabilities](/en/sdk/harmonyos/platform-capabilities), [API Reference](/en/sdk/harmonyos/api-reference), and [Upgrade Guide](/en/sdk/harmonyos/upgrade) are published against the OHPM `@wukong/wkim 1.1.7` HAR and matching source, without a site install, compile, or device runtime receipt |
| Mini programs, UniApp, Node.js, or another runtime | Check the README, examples, and release notes for the exact version of both JavaScript candidates; [WuKongIMUniappSDK](https://github.com/WuKongIM/WuKongIMUniappSDK) is deprecated and points to JSSDK | Existing UniApp projects should use the [retirement and migration guide](/en/sdk/uniapp) to pin `wukongimjssdk@1.3.5`; no matching runtime receipt exists here, so do not infer support from browser notes |

## Official source directory

The legacy source links have been checked against the current WuKongIM GitHub organization. They are grouped only by repository name for discovery. Treat the tag you plan to adopt—not this table—as the authority for versions, installation, system requirements, and licensing.

### EasySDK

These exact source snapshots and distribution entries were checked against server evidence: all four have wire fixtures, while the real-process E2E uses the iOS and Android profiles. They are not a floating “latest” alias or proof of platform execution or production readiness:

- [WuKongEasySDK-JS](https://github.com/WuKongIM/WuKongEasySDK-JS): `v2.0.2`, [npm package](https://www.npmjs.com/package/easyjssdk/v/2.0.2); [tutorial and JSON-RPC CONNECT online receipt](/en/sdk/easy/javascript/getting-started)
- [WuKongEasySDK-iOS](https://github.com/WuKongIM/WuKongEasySDK-iOS): `v1.0.3`, [CocoaPods](https://cocoapods.org/pods/WuKongEasySDK); [tutorial and JSON-RPC CONNECT online receipt](/en/sdk/easy/ios/getting-started)
- [WuKongEasySDK-Android](https://github.com/WuKongIM/WuKongEasySDK-Android): `v1.0.3`, [Maven Central](https://central.sonatype.com/artifact/com.githubim/easysdk-android/1.0.3); [tutorial and JSON-RPC CONNECT online receipt](/en/sdk/easy/android/getting-started)
- [WuKongEasySDK-Flutter](https://github.com/WuKongIM/WuKongEasySDK-Flutter): `v1.0.4`, [pub.dev](https://pub.dev/packages/wukong_easy_sdk/versions/1.0.4); [tutorial and JSON-RPC CONNECT online receipt](/en/sdk/easy/flutter/getting-started)

### WuKongIMSDK

- [WuKongIMJSSDK](https://github.com/WuKongIM/WuKongIMJSSDK)
- [WuKongIMiOSSDK](https://github.com/WuKongIM/WuKongIMiOSSDK): tag `1.1.1`, source revision `89bf9a1b95ce374caabdd8031d69cc8844d825ae`; [CocoaPods `WuKongIMSDK` `1.1.1`](https://cocoapods.org/pods/WuKongIMSDK), framework revision `0cbfb99f18010fe76b7e13ed31b5d1ad4664b10c`; [tutorial](/en/sdk/ios) published without a runtime receipt
- [WuKongIMAndroidSDK](https://github.com/WuKongIM/WuKongIMAndroidSDK): tag `1.5.5`, source revision `662a559a50d181540a0448454beb57e939b0c50e`; JitPack `com.github.WuKongIM:WuKongIMAndroidSDK:1.5.5` aligns to the same revision; [tutorial](/en/sdk/android) published without an Android runtime receipt
- [WuKongIMFlutterSDK](https://github.com/WuKongIM/WuKongIMFlutterSDK): pub.dev `wukongimfluttersdk` `1.7.9`, archive SHA-256 `b6191a86cd1e4caacaa4652e95709310eb1493f159fee65e1dd53c2a3ff9e80a`, matching source revision `de1024276523119e38305c49a3a873caae4d5c59`; [tutorial](/en/sdk/flutter) published without a runtime receipt
- [WuKongIMHarmonyOSSDK](https://github.com/WuKongIM/WuKongIMHarmonyOSSDK): OHPM `@wukong/wkim` `1.1.7`, HAR SHA-256 `d98d1523bc60ad204dd74d9cfa776935a5547fc3ab352322dfa17f5dbc7a3cd8`, matching source revision `0c41810a1e0a5fc2936929d63ca32a50ffb11bec`; [tutorial](/en/sdk/harmonyos) published without a runtime receipt
- [WuKongIMUniappSDK](https://github.com/WuKongIM/WuKongIMUniappSDK): the official repository is deprecated and directs users to JSSDK; do not select it as a new standalone SDK, and start existing projects from the [UniApp migration guide](/en/sdk/uniapp)

Before integration, inspect the repository README, examples, tags or releases when the repository provides them, changelog, LICENSE, and open issues. Pin an exact distribution version or artifact and record its matching source revision. Do not assume every repository has a tag; a default branch or `latest` is not a compatibility statement.

## Answer six questions before choosing

1. **Where will it run?** Record the browser, native system, cross-platform framework, and minimum runtime version.
2. **What must the first release do?** Mark required direct chat, groups, custom messages, conversations, unread state, offline recovery, push, and multi-device behavior.
3. **Which state belongs to the product?** Assign owners for accounts, contacts, policy, push devices, business receipts, and UI state.
4. **Which exact version will you pin?** Record the server revision, SDK tag or package version, runtime, and build tools together.
5. **How will you accept the minimal loop?** At minimum prove connection, send acknowledgement, online receipt, reconnect, offline recovery, and deduplication.
6. **What remains before release?** Review token verification, TLS, network isolation, rate limits, audit, capacity, upgrades, and rollback separately.

If a candidate repository cannot answer one of these questions, build a small proof first instead of expanding the choice directly into a production application.

## Follow this path after choosing

### EasySDK

1. Read the [EasySDK overview](/en/sdk/easy) and the relevant platform tutorial to inspect the pinned API, lifecycle, and adoption blockers.
2. Then read the [JSON-RPC support matrix](/en/api/client-protocols/json-rpc). The current Product Gateway supports pinned-version CONNECT, ping, online SEND/SENDACK, and RECV/RECVACK. Complete platform builds, device execution, WSS, log redaction, and production token verification before release.

### JavaScript / Web

1. Run the locked TypeScript sample in [Installation](/en/sdk/javascript/installation).
2. Use the [Quickstart](/en/sdk/javascript/quickstart) to connect Alice and Bob and prove online messaging plus offline recovery.
3. Use the [API Reference](/en/sdk/javascript/api-reference) to check the pinned `1.3.5` classes, configuration, events, messages, and errors.
4. Before changing the pin, use the [Upgrade Guide](/en/sdk/javascript/upgrade) to record migration boundaries, the test matrix, and rollback point.
5. Use the [Common Guides](/en/sdk/common-guides) to design identity, connection, conversations, push, and multi-device behavior.
6. Close release gates with [Platform Capabilities](/en/sdk/javascript/platform-capabilities) and [Integration Acceptance](/en/guide/integration/acceptance).

### iOS

1. Use [iOS Installation](/en/sdk/ios/installation) to pin CocoaPods `1.1.1` and review deployment-target and simulator-architecture limits.
2. Use the [iOS Quickstart](/en/sdk/ios/quickstart) in two independent processes to prove Alice/Bob connection, send acknowledgement, and online receipt.
3. Use [Platform Capabilities](/en/sdk/ios/platform-capabilities) to divide SDK and application ownership, check call sites against the [API Reference](/en/sdk/ios/api-reference), and follow the [Upgrade Guide](/en/sdk/ios/upgrade) before changing versions.
4. Retain runtime evidence for your Xcode, device, and server revision instead of treating this source/header material as the site's receipt.
5. Before release, close raw-TCP, TLS, RECV Payload logging, and production-authentication blockers, then separately verify offline, conversations, push, and background behavior.

### Android

1. Use [Android Installation](/en/sdk/android/installation) to pin JitPack `1.5.5`, revision `662a559a50d181540a0448454beb57e939b0c50e`, and the AAR checksum.
2. Use the [Android Quickstart](/en/sdk/android/quickstart) in two independent processes to prove the conversation-sync gate, local insert, SENDACK, and online receipt.
3. Use [Platform Capabilities](/en/sdk/android/platform-capabilities) to divide SDK and application ownership, check call sites against the [API Reference](/en/sdk/android/api-reference), and follow the [Upgrade Guide](/en/sdk/android/upgrade) before changing versions.
4. Retain a runtime receipt for your own AGP, JDK, device, system, and server revision instead of treating this source/AAR material as Android execution evidence.
5. Before release, close raw-TCP/TLS, SharedPreferences-token, UID-SQLCipher-key, and Payload-logging blockers, then verify offline, push, background, and multi-device behavior separately.

### Flutter

1. Use [Flutter Installation](/en/sdk/flutter/installation) to pin pub.dev `wukongimfluttersdk 1.7.9`, the archive hash, and the application `pubspec.lock`.
2. Use the [Flutter Quickstart](/en/sdk/flutter/quickstart) in two independent processes to prove the conversation-sync gate, local insert, SENDACK, and online receipt.
3. Use [Platform Capabilities](/en/sdk/flutter/platform-capabilities) to divide Dart and host-platform ownership, check call sites against the [API Reference](/en/sdk/flutter/api-reference), and follow the [Upgrade Guide](/en/sdk/flutter/upgrade) before changing versions.
4. Retain a runtime receipt for your own Flutter, Dart, target toolchain, device, and server revision instead of treating this source material or static analysis/macOS build evidence as Android, iOS, or macOS execution proof.
5. Before release, close raw-TCP/TLS, sqflite/SharedPreferences data protection, Payload logging, reconnect-generation, and account-queue isolation blockers, then verify offline, push, background, and multi-device behavior separately.

### HarmonyOS

1. Use [HarmonyOS Installation](/en/sdk/harmonyos/installation) to pin OHPM `@wukong/wkim 1.1.7`, the HAR hash, application `oh-package-lock.json5`, and API 20.
2. Use the [HarmonyOS Quickstart](/en/sdk/harmonyos/quickstart) in two independent processes to prove the conversation-sync gate, local insert, SENDACK, and online receipt.
3. Use [Platform Capabilities](/en/sdk/harmonyos/platform-capabilities) to divide ArkTS and application ownership, check root exports and deep imports against the [API Reference](/en/sdk/harmonyos/api-reference), and follow the [Upgrade Guide](/en/sdk/harmonyos/upgrade) before changing versions.
4. Retain a runtime receipt for your own DevEco, HarmonyOS SDK, ohpm/hvigor, device, and server revision instead of treating this HAR/source material as install, compile, or device execution proof.
5. Before release, close raw-TCP/TLS, RelationalStore `encrypt: false`, Payload/`hilog.info` logging, database-error propagation, and `sendingMsgMap` account-isolation blockers, then verify offline, push, background, and multi-device behavior separately.

### UniApp migration

1. Follow the [UniApp retirement and migration guide](/en/sdk/uniapp) to remove deprecated `wukongimuniappsdk`, install exactly `wukongimjssdk@1.3.5`, and retain the lockfile.
2. Import only from the `wukongimjssdk` package root and let `1.3.5` detect global `uni`, then `wx`, then native `WebSocket`; do not treat the currently broken explicit `config.platform` adapter path as a fix.
3. In HBuilderX, record build, connection, SENDACK, online-receipt, and recovery results separately for App, H5, and every mini-program target. A Chromium receipt cannot replace that evidence.
4. Let a trusted product backend issue identity and connection material. Unmodified `1.3.5` unconditionally logs plaintext Payload, so review a logging patch and close WSS, authentication, local-data, upgrade, and rollback gates before release.

### Other WuKongIMSDK platforms

1. Select an exact tag or package version from the official repository above and run its included example first. The UniApp repository is deprecated, must follow the migration path above, and is not a new-project candidate.
2. Map [Identity & Token](/en/sdk/common-guides/identity-and-token), [Initialization & Connection](/en/sdk/common-guides/initialization-and-connection), and [Messaging](/en/sdk/common-guides/messaging) to that version's API.
3. Retain your own execution evidence for connection, send acknowledgement, online receipt, and offline recovery.
4. Until this site publishes that platform tutorial, use the repository and your proof as the authority for platform APIs.

## Common mistakes

- **Choosing only from “chat app / non-chat app”:** applications in the same category can have very different local-data, conversation, and push requirements.
- **Treating a reachable repository as compatibility:** the tag, package, server version, and target runtime still need validation.
- **Copying APIs before designing identity:** UID, token, and policy must come from a trusted product backend; a client cannot grant them to itself.
- **Calling Product HTTP directly from a browser:** give the Web client only connection material and keep management APIs behind the trusted backend.

## Next step

For a full Android, iOS, Flutter, or HarmonyOS SDK, use the corresponding tutorial and create a receipt for your exact runtime tuple. Existing UniApp projects should use the [retirement and migration guide](/en/sdk/uniapp). For the complete Web golden path, continue to [Installation](/en/sdk/javascript/installation). For lightweight online APIs, use the [EasySDK tutorials](/en/sdk/easy) with the existing server JSON-RPC CONNECT and E2E evidence, then add platform and production acceptance.
