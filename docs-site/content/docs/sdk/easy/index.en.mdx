---
title: WuKongEasySDK
description: Read the pinned EasySDK APIs for iOS, Android, Flutter, and Web, then use the current Product Gateway for JSON-RPC CONNECT and online bidirectional messaging.
---

WuKongEasySDK is for applications that need realtime communication while keeping their own UI and product state. These tutorials use pinned source to explain how it models connection, events, and sending over WebSocket JSON-RPC; the current Product Gateway supports the core online path for these four pinned versions.

<Callout type="warn" title="A server-side wire receipt is not platform runtime verification">
  These tutorials were checked against exact release tags and source revisions in the four official repositories. Server fixtures cover all four wire profiles; the real `cmd/wukongim` 256-slot single-node cluster E2E uses the iOS and Android profiles for its Alice/Bob bidirectional loop. It does not compile or execute the published iOS, Android, Flutter, or Web artifacts, so platform builds, device runs, and proxy configuration remain deployment-owned evidence.
</Callout>

<Callout type="info" title="The current Product Gateway supports the EasySDK core path">
  The current server supports JSON-RPC CONNECT, request correlation, ping, online SEND/SENDACK, RECV/RECVACK, and reconnect for these four pinned versions. It accepts camelCase, Android snake_case, JSON text-string payloads, JSON object payloads, Base64 payloads, and JS requests without `jsonrpc`; iOS `v1.0.3` sends a camelCase RECVACK containing both `messageId` and `messageSeq`. This receipt excludes offline sync, push, subscription RPC, batch RPC, and production-grade token verification in the default composition.
</Callout>

“5 minutes” describes the shape of the path: install, initialize, listen, connect, and send. It is not a completion-time promise and does not include product accounts, authorization, group membership, offline synchronization, push, capacity, or release acceptance.

## Choose your platform

<Cards>
  <Card title="iOS" description="Install v1.0.3 with Swift Package Manager or CocoaPods." href="/en/sdk/easy/ios/getting-started" />
  <Card title="Android" description="Install v1.0.3 from Maven Central and clean up listeners with the Activity lifecycle." href="/en/sdk/easy/android/getting-started" />
  <Card title="Flutter" description="Install v1.0.4 from pub.dev and release resources from State.dispose." href="/en/sdk/easy/flutter/getting-started" />
  <Card title="Web" description="Pin easyjssdk v2.0.2 and obtain browser connection material through a product BFF." href="/en/sdk/easy/javascript/getting-started" />
</Cards>

## What EasySDK owns

| Current EasySDK source provides | Your application still owns |
| --- | --- |
| WebSocket setup and JSON-RPC CONNECT | Product login, UID allocation, token issuance, and revocation |
| Sends to person or group Channels | Contacts, membership, permissions, content policy, and product receipts |
| Connect, disconnect, message, error, and related events | View state, rendering, persistence, and unknown-payload fallback |
| Send results and realtime receive | Offline sync, conversations, unread counts, push, and multi-device policy |
| Bounded automatic reconnect behavior | TLS/WSS, rate limits, capacity, observability, upgrade, and rollback evidence |

EasySDK is neither a chat UI nor a complete product backend. A successful `send` result means the server returned a result for that send. It still does not prove that the peer received, displayed, or processed the message. Continue with [Messaging](/en/guide/integration/messaging) and model send acknowledgement, realtime receipt, and product completion separately.

## One flow, different lifecycle owners

| Task | iOS | Android | Flutter | Web |
| --- | --- | --- | --- | --- |
| Create and initialize | Application-owned `WuKongEasySDK` instance | Process singleton via `getInstance()` + `init` | Application-owned singleton + `init` | One `WKIM` instance per identity/browser context |
| Listener ownership | Retain each `EventListener` token | Retain the same listener object | Retain the same callback reference | Pass the same function reference to `on` and `off` |
| Connection readiness | Enable send only after `onConnect` | Enable send only after `CONNECT` | Enable send only after the `connect` event | Enable send only after the `Connect` event |
| Account exit | Remove tokens and `disconnect` | Process/account owner removes listeners and disconnects | Remove listeners, `disconnect`, then `dispose` | `off`, then `destroy` |

A view can subscribe to state, but a view rebuild must not create a second connection. Each platform page identifies who creates and who cleans up the client without prescribing an application UI architecture.

## Tutorial basis and calibration rules

This group preserves the learning sequence from the [legacy WuKongEasySDK overview](https://wukong.mintlify.app/en/sdk/easy/overview) and checks the corresponding legacy [iOS](https://wukong.mintlify.app/en/sdk/easy/ios/getting-started), [Android](https://wukong.mintlify.app/en/sdk/easy/android/getting-started), [Flutter](https://wukong.mintlify.app/en/sdk/easy/flutter/getting-started), and [Web](https://wukong.mintlify.app/en/sdk/easy/javascript/getting-started) pages. The legacy site determines only the install → initialize → listen → connect → send learning sequence. Method signatures, versions, platform floors, protocol shapes, and security boundaries come from the pinned release source below. Floating versions, “zero configuration,” and unproven compatibility claims from the old pages are not copied forward.

## Source snapshots used by the four tutorials

| Platform | Release tag | Source revision | Distribution |
| --- | --- | --- | --- |
| [iOS](https://github.com/WuKongIM/WuKongEasySDK-iOS) | `v1.0.3` | `643848f85be70e3e3f2be22fceb86ae428b6cc38` | [Swift Package Manager / CocoaPods](https://cocoapods.org/pods/WuKongEasySDK) |
| [Android](https://github.com/WuKongIM/WuKongEasySDK-Android) | `v1.0.3` | `62084632cd8d1f26c751b053b0fb82d6aaa63892` | [Maven Central](https://central.sonatype.com/artifact/com.githubim/easysdk-android/1.0.3) |
| [Flutter](https://github.com/WuKongIM/WuKongEasySDK-Flutter) | `v1.0.4` | `6179251b49414401fe0eac4bfa3fec3f9b13a9fc` | [pub.dev](https://pub.dev/packages/wukong_easy_sdk/versions/1.0.4) |
| [Web](https://github.com/WuKongIM/WuKongEasySDK-JS) | `v2.0.2` | `c59c80551944c9e5d9b4a902ebd2629d3defb2e6` | [npm](https://www.npmjs.com/package/easyjssdk/v/2.0.2) |

The tutorials deliberately pin these versions instead of using ranges or `latest`. The server-side wire receipt covers only the versions in this table. Re-run the Alice/Bob loop before updating the application lockfile and your compatibility record for an SDK upgrade.

## Logging-security fix status

| Platform | Merged fix | Current distribution status |
| --- | --- | --- |
| iOS | [PR #3](https://github.com/WuKongIM/WuKongEasySDK-iOS/pull/3) / [`b7ec4440b940539bee213f95a3be74948f4b9fb8`](https://github.com/WuKongIM/WuKongEasySDK-iOS/commit/b7ec4440b940539bee213f95a3be74948f4b9fb8) | Not included in `v1.0.3` |
| Android | [PR #3](https://github.com/WuKongIM/WuKongEasySDK-Android/pull/3) / [`e984c7374a0e11f5d109ad3dbfdea599907735ff`](https://github.com/WuKongIM/WuKongEasySDK-Android/commit/e984c7374a0e11f5d109ad3dbfdea599907735ff) | Not included in `v1.0.3` |
| Flutter | [PR #3](https://github.com/WuKongIM/WuKongEasySDK-Flutter/pull/3) / [`d7758c301e5289ddfa09cd09b6976c2479584b1c`](https://github.com/WuKongIM/WuKongEasySDK-Flutter/commit/d7758c301e5289ddfa09cd09b6976c2479584b1c) | Not included in `v1.0.4` |
| Web | [PR #6](https://github.com/WuKongIM/WuKongEasySDK-JS/pull/6) / [`3ebf505734c5b6764b30eac011f0b7a5024c89e8`](https://github.com/WuKongIM/WuKongEasySDK-JS/commit/3ebf505734c5b6764b30eac011f0b7a5024c89e8) | Not included in `v2.0.2` |

<Callout type="warn" title="Security fixes are merged but not yet released">
  The official `main` branch in all four repositories now keeps diagnostics off by default, emits only redacted low-sensitivity metadata when explicitly enabled, and redacts public model string representations. The currently distributed CocoaPods, Maven, pub.dev, and npm releases in the table above are still pre-fix snapshots, so the release-specific logging risks in each platform tutorial still apply. Production users should wait for the next official release and upgrade. Early evaluation must pin the exact commit shown above, build it independently, and repeat Release-log and platform acceptance. Do not use a floating `main` reference or present a main-only option as released API.
</Callout>

## Prepare connection material

A client must not grant its own identity. The user first signs in to your product. A trusted product backend then returns only the material needed for this connection:

```json
{
  "uid": "alice",
  "token": "short-lived-token",
  "websocketUrl": "wss://im.example.com/ws"
}
```

- `uid` is the stable user identity in your product.
- `token` is short-lived and revocable and grants no Product HTTP management authority.
- `websocketUrl` comes from deployment configuration or from a safe backend selection after calling `/route`.
- Production uses `wss://`, and its reverse proxy supports WebSocket Upgrade correctly.

Complete [Identity & Token](/en/guide/integration/authentication) first. In the default v3 Beta composition, storing data through `/user/token` does not automatically provide production-grade CONNECT validation. A deployment must wire a trusted verifier and prove rejection of invalid, expired, and revoked tokens.

<Callout type="info" title="Device categories are aligned; application code should still use enums">
  All four pinned versions use the same wire values as the [Device Flag dictionary](/en/api/dictionaries/device-flags): APP `0`, WEB `1`, and PC `2`. Mobile clients use `.app` / `APP`, while the browser defaults to WEB; do not copy integers into product code. The server normalizes each JSON-RPC field and payload shape at the protocol boundary; applications must still apply the lifecycle and logging safeguards in each platform tutorial.
</Callout>

## Accept online bidirectional messaging with Alice and Bob

Run the following minimal loop in your deployment:

1. Obtain a UID, short-lived token, and WebSocket address separately for Alice and Bob.
2. Create two independent SDK instances or run two independent application processes.
3. After both clients observe a successful connection, have Alice send a text payload to Bob's person Channel.
4. On Alice, record the returned message ID, sequence, and Reason Code when exposed by that SDK.
5. On Bob, independently observe the realtime message event and deduplicate by `messageId`.
6. Send once from Bob to Alice and verify the reverse direction.
7. Remove listeners and disconnect when the page, Activity, Widget, or application exits.

This loop proves only connection and online messaging in the tested environment. Offline recovery, conversations, push, multi-device behavior, reconnect boundaries, and production security require separate evidence.

## Next

Choose a platform above and send the first message, run that page's Alice/Bob acceptance, and continue with [Messaging](/en/guide/integration/messaging), [Reconnect & Errors](/en/guide/integration/acceptance), and [Integration Acceptance](/en/guide/integration/acceptance). If you need a local message store, conversations, unread counts, and a broader platform API, return to [Choose an SDK](/en/sdk) and evaluate the WuKongIMSDK family.
